# AGENTS.md — 穂乃味タイムカード（timecard）

このファイルは timecard を Claude Code / 各種 Agent で手放し運用するための単一の参照元（Single Source of Truth）です。
`.claude/agents/*.md` の各 Agent はこのファイルの「禁止事項」「QA手順」「commit/push/deploy確認ルール」に従います。

> 本ファイルは**確認できた事実のみ**を記載しています。確認できていない項目は明示的に「未確認」と記載しています。
> 推測でルールを足さないでください。事実が変わったら本ファイルを更新してください。
> リポジトリ名はローカルでは `timecard-git`、GitHub remote は `rsb79692-create/timecard`。

---

## 共通運用ルール（_shared_claude 参照）

> **本リポジトリは `_shared_claude` 参照運用の Type C（Firebase + GitHub Pages）基準リポジトリであり、全7リポジトリ移行の最後（7例目）である。**
> 全リポジトリ共通の運用ルール（姿勢・禁止事項・agent 役割・報告の手順）は
> [`../_shared_claude/`](../_shared_claude/) を **single source of truth** として参照する。本 AGENTS.md には
> **timecard 固有の事実とルール** を残し、共通項は重複させず参照に寄せる。共通ルールと矛盾した場合は、
> **timecard 固有の「事実」（Firebase/GitHub Pages/index.html 単体/通知系など。下記各セクション）を優先**する。
> **ただし「レビュー範囲」は本例外の対象外である。** 初回レビューと再確認の範囲、および
> **広いレビューへ戻す条件**は [`../_shared_claude/AGENTS.md`](../_shared_claude/AGENTS.md)「レビュー範囲」を正本とし、
> 本書・`CLAUDE.md`・`.claude/**`・`docs/**` で同節より狭い範囲を定めない（同節が timecard 側の記載に優先する）。

作業開始時、まず以下の共通ファイルを前提として読む。timecard は技術構成タイプ **Type C（Firebase + GitHub Pages）**。Type A/B（Supabase/Neon・Vercel・npm ビルド）の前提を持ち込まない。

| 参照ファイル | 適用範囲 | timecard での扱い |
|---|---|---|
| [`../_shared_claude/RULES.md`](../_shared_claude/RULES.md) | 最優先ルール・共通禁止事項（推測禁止・指示外/削除禁止・secret 非出力・破壊的変更の事前説明・orchestrator 起点） | そのまま適用（「禁止事項」節で固有を上乗せ） |
| [`../_shared_claude/AGENTS.md`](../_shared_claude/AGENTS.md) | 共通の agent 役割定義・標準チェーン・完了報告にチェーン記載 | 適用。ただし **DB 担当は `migration-agent` ではなく `firebase-agent`**（下記）。チェーンは「Agent の役割と正しい流れ」節が正 |
| [`../_shared_claude/DEPLOY.md`](../_shared_claude/DEPLOY.md) | commit→push→Vercel READY→health→commit ID 必須 | **⚠ 読み替え適用**。**「Vercel READY」は GitHub Pages 自動デプロイ／GitHub Actions の確認に読み替える**（下記「DEPLOY.md の読み替え」） |
| [`../_shared_claude/DB.md`](../_shared_claude/DB.md) | Supabase/migration/RLS/rollback/破壊的 DDL 原則禁止 | **⚠ ほぼ非適用**。**Supabase/RLS/migration 前提は適用しない**。汎用の安全原則のみ参照（下記「DB.md の適用範囲」） |
| [`../_shared_claude/REPORT.md`](../_shared_claude/REPORT.md) | 完了報告の標準フォーマット | 適用（timecard 版テンプレートを併用） |
| [`../_shared_claude/PROJECT_TYPES.md`](../_shared_claude/PROJECT_TYPES.md) | タイプ別方針（timecard = Type C） | **Type C（Firebase + GitHub Pages）の分岐に従う** |

### DB.md の適用範囲（Type C / Firebase 固有・重要）

`_shared_claude/DB.md` は Supabase（PostgreSQL）を前提に書かれているため、timecard では **汎用の安全原則のみを参照**し、Supabase/RLS/migration 固有部は適用しない。

- ✅ **適用する汎用安全原則**: **本番 DB（Firebase RTDB の `tc5_*`）を変更しない**／**データの破壊的操作（削除・上書き）は事前にリスク提示・人間承認必須**／**変更前に現状確認**／**secret・接続情報・トークンを出力しない**。
- ❌ **適用しない Supabase/SQL 固有部**: **migration ファイル・`supabase/migrations/`・連番/タイムスタンプ命名**（timecard に **migration の概念は無い**）／**RLS / `CREATE POLICY` / `auth.uid()` / service role**／**`supabase db push` / `db diff` / `db reset`**／`@supabase/*` クライアント。
- timecard の実態: **Firebase Realtime Database**（REST `…/honomi/<path>.json`）／認証は **Firebase Auth**（Anonymous ＋ `/api/auth/*` が発行するカスタムトークン）／**スキーマ相当＝`database.rules.json`**（変更は **`firebase-agent` が方針提示し人間が確認**）。
  ⚠⚠ **`database.rules.json` は honomi-board と同居している。**（下記「RTDB のルールは honomi-board と共有」）
  ⚠ **`/honomi` の `.read`/`.write` はもう `auth != null` ではない**（2026-09-06 に変更）。同節を必ず読むこと。

### DEPLOY.md の読み替え（Type C）

`_shared_claude/DEPLOY.md` の骨子（commit→push→デプロイ確認→health→commit ID 必須）は流用するが、デプロイ確認手段を読み替える。

- **「Vercel READY 待機」→ GitHub Pages の自動デプロイ反映を待つ**（push `origin main` 後、30秒〜数分）。
- **デプロイ状況の確認先**: **GitHub Actions**（`https://github.com/rsb79692-create/timecard/actions`）の成否＋本番 URL。
- **health check 先**: 本番 URL **`https://rsb79692-create.github.io/timecard/`**（GitHub Pages。Vercel ではない）。通知 API のみ別 Vercel（`timecard-rho.vercel.app`）だが**出荷対象ではない**。
- **手動 `vercel --prod` はしない**。`agent:ship`／`ship.mjs` は**存在しない**（出荷は `git push origin main` → GitHub Pages 自動配信）。

### migration-agent ではなく firebase-agent を正とする

- 本リポジトリに **`migration-agent` は存在しない**。`_shared_claude/AGENTS.md` の「migration-agent（DB変更担当）」の役割は、timecard では **`firebase-agent`** が担う（Firebase RTDB・`database.rules.json`・FCM・PWA・GitHub Actions のインフラ層）。
- DB/スキーマ相当の検討が要る場合の入口は **`firebase-agent`**。ただし `database.rules.json`・Firebase データ・Secrets・ワークフローの**変更は人間の確認必須**（本タスクでは変更禁止）。

## プロジェクト概要

- **名称**: 穂乃味タイムカード（リポジトリ `timecard`）
- **種別**: バニラ JavaScript（ビルドなし）+ Firebase の **PWA**。単一の `index.html` にアプリ本体（JS）を内包。
- **用途**: 打刻・打刻修正申請・有給申請/付与・月別出勤日数管理・各種通知を行う勤怠アプリ。
- **配信構成（確認できた実態）**:
  - **アプリ本体（静的サイト）= GitHub Pages**。本番 URL: `https://rsb79692-create.github.io/timecard/`（`api/line-notify.js` / `api/discord-notify.js` の `ALLOWED_ORIGIN`・`ADMIN_URL`、`?token=all` で確認）。
  - **通知用 API = 別の Vercel デプロイ**。`index.html` の `NOTIFY_API_URL="https://timecard-rho.vercel.app/api/discord-notify"`。`api/*.js` は Vercel サーバーレス関数。
  - **定期実行 = GitHub Actions**（`.github/workflows/`）。
- `package.json` は**存在しない**（npm プロジェクトではない）。

---

## RTDB のルールは honomi-board と共有（2026-09-06 に整理）

⚠⚠ **Firebase プロジェクト `honomi-timecard` は honomi-board と同じものを使っている。**
`database.rules.json` は**両方のルールが入った1ファイル**で、
`firebase deploy --only database` は**全体を置換する**。片方だけを deploy すると、もう片方が即死する。

| トップレベル | 持ち主 |
|---|---|
| `honomi` | timecard（`tc5_*` / `config` / `master` / `documents` / 各種トークン） |
| `rooms` / `members` / `config` / `field` / `shares` / `shareKeys` / `guestOf` | honomi-board |
| `authz` / `ratelimit` / `mileage` | ルール未定義＝デフォルト拒否。Admin SDK 経由でのみ使う |

⚠ 2026-09-06 まで、**このリポジトリのコピーにはボード側のルールが1つも入っていなかった。**
ここから deploy していたら `rooms` / `members` / `shares` が全消えしていた。
いまは両リポジトリで**同一内容**（md5 一致）にしてある。**片方だけを編集しないこと。**

手順は必ず「本番の現行ルールを取得 → 既存キーを保持してマージ → deploy → 取り直して照合」。

### `/honomi` のアクセス条件（`auth != null` ではない）

| 領域 | 読み | 書き |
|---|---|---|
| `tc5_staff` / `tc_master_depts` / `master/locations` | 起動面 | `s`/`a` だけ |
| `tc5_pins` / `tc5_records` | 起動面 | 起動面。ただし読み取り専用の役割（`v`/`d`/`x`）は除外、**丸ごと消す操作は拒否** |
| `viewerTokens/$t` `demoTokens/$t` `staffDemoTokens/$t` | 起動面（**1件ずつ**。親に `.read` が無いので一覧不可） | `viewerTokens` は `a`/`d` |
| `demoIssuedViewerTokens` | 役割つき | `a`/`d` |
| **それ以外すべて** | **役割つき** | **`s`/`a` だけ** |

- **起動面** … `auth != null && guestOf に居ない`。打刻端末（匿名）はここ。
- **役割つき** … 上に加えて `auth.token.r != null` かつ `sx`（期限・UNIX秒）が切れていないこと。
  `r` は `/api/auth/{staff,admin,share}` が入れるクレーム
  （`s`=スタッフ / `a`=管理者 / `v`=労務士 / `d`=デモ / `x`=サンドボックス）。

⚠ **起動面をなぜ開けてあるか。** 打刻端末は起動時に役割を持てない（PIN を入れる前だから）。
`tc5_staff` / `tc5_pins` / `tc_master_depts` / `master/locations` / `tc5_records` は
PIN 画面を出すまでに必ず要る。ここを締めると打刻ができなくなる。

⚠ **役割つきの領域を読む前には `afterElevation()` で昇格を待つこと。**
`elevateStaffSession` / `elevateShareSession` は画面遷移を待たせないため裏で走らせている。
待たずに読むと匿名トークンのまま 401 になる。

⚠⚠ **残っている穴（次に直すならここ）。**
`tc5_pins` には PIN の平文（`plain`）が入っており、起動面なので匿名で読める。
① 匿名アカウントを作る → ② `tc5_pins` を読む → ③ `/api/auth/staff` を1回叩く、で
**役割つきの領域まで入れてしまう**。`plain` を消しても 4桁 PIN の非ソルト SHA-256 は
1万通りで戻せるので同じ。塞ぐには
(a) 「PIN 登録済みか」の真偽だけを持つ別ノードを起動面へ置き、`tc5_pins` の読みを役割つきへ戻す、
(b) `sOk` のローカル照合をやめて `/api/auth/staff` の応答で判定する
（⚠ **打刻が Vercel に依存するようになる。ここが最大の判断どころ**）、
(c) 既存 PIN の入れ替え、の3つが要る。
なお `tc5_pins` は localStorage へ保存していないので、**オフラインでの PIN ログインは今もできない**。

⚠ `s`（一般スタッフ）の役割でも `/honomi` 全体を読める（`.read` は `r` の値を見ていない）。
ノードごとに `a` / `s` を分けるのは次の作業。

⚠⚠ **`FIREBASE_API_KEY` の GitHub Secret は消さないこと。** `morning-check` だけが今も使う。
⚠ サービスアカウントのアクセストークンは**ルールを全部迂回**し、同居しているボード側
（`rooms` / `shares` / `members` / `field`）まで届く。この鍵を置く場所を増やさないこと。

---

## 主要機能

- **打刻**: Realtime Database `tc5_records`（`index.html`）。端末（IndexedDB）へ保存してから非同期送信（下記「打刻の端末永続保存と自動再送」）。
- **打刻修正申請**: `tc5_correction_requests`。承認は `tc5_approvals`。未対応申請は FCM Push で通知（`scripts/fcm-check.js`）。
- **有給**: 申請 `tc5_paid_leave_requests`、残数 `tc5_paid_leave_balances`。付与・承認時にスタッフ本人へアプリ内通知（直近コミットで実装）。
- **月別出勤日数**: `tc5_monthly_days_import`（`scripts/fix-monthly-days-year.js` は年度補正用の保守スクリプト）。関連: ルートの `勤務日数管理.xlsx`。
- **ピン留め等**: `tc5_pins`。
- **通知系**:
  - 朝出勤確認 LINE通知（`scripts/morning-check.js`、`.github/workflows/morning-check.yml`、**判定時刻ごとに1日2回**）
    - **未打刻の判定時刻は施設ごとに持つ**（2026-09-11 追加）。6時の回と7時の回で対象施設が排他に分かれるため、同じ施設へ二重通知されない。
      - 現在の割り当て: **ハルイロ / ミュゲの泉 = 7時**、その他 = 6時（ハーベストは従来どおり `NOTIFY_EXCLUDE` で通知対象外）。
      - ⚠⚠ **判定時刻の正本は `morning-check.js` の `LATE_CHECK_FACILITIES`（コード側）である。施設マスタ側には持たせない。**
        `master/locations` は `/honomi` の `.write` を継承し、管理者だけでなく**一般スタッフ（`auth.token.r === 's'`）でも書ける**。
        そこへ判定時刻を置くと、6時の回の直前に「全施設=7時」、7時の回の直前に「全施設=6時」と書き換えるだけで、
        **その日の未打刻通知を1件も出さずに Actions を success のまま終わらせられる**（画面に出ないフィールドなので誰も気づけない）。
        移動距離申請が `master/locations` をサーバ側の判断材料にしていないのと同じ理由。施設マスタは「どの施設が在るか」の正本としてだけ使う。
      - 施設マスタの施設名との突き合わせは `normalizeFacility`（NFKC＋空白除去）で行い、`LATE_CHECK_FACILITIES` に書いた施設名が
        マスタに見当たらない場合は `findMissingLateFacilities` が `[WARN]` を出す（改名・表記ゆれの検知）。
      - 有効な判定時刻は `CHECK_HOURS = [6, 7]` だけ。`CHECK_HOUR` が不正なら**既定へ倒さず `exit 1`**（黙って6時として走ると、6時の施設へ二重通知しつつ7時の施設が誰にも判定されない）。
      - ⚠ **`CHECK_HOURS` へ時刻を足すときは `morning-check.yml` の cron も必ず足す。** cron の無い時刻を指定した施設は誰にも判定されず、通知が消える。
        この対応は `scripts/test-morning-check.js` が `CHECK_HOURS` から導出して機械的に検証する。
      - ⚠⚠ **`scripts/morning-check.js` の `DEFAULT_FACILITIES` と `index.html` の `DEFAULT_FACILITIES` は
        同名だが役割が違う。同期してはならない。**
        `index.html` 側は**空配列**である（実施設名を配布物へ埋め込まないため）。
        一方 `morning-check.js` 側は ①施設マスタを取得できなかったときのフォールバック、
        ②`expectedFacilityCountForHour()` が使う「この判定時刻に何件在るはず」の期待値、の2役を持つ。
        施設マスタは一般スタッフでも書けるので、期待値だけは改ざんされない場所に置く必要がある。
        - `morning-check.js` 側を空にすると `expected` が常に 0 になり、`selectedCount < expected` が永久に偽になって
          **施設を全部消されても「設定異常」を検知できなくなる**（下記の監視が無言で無効化される）。
        - `index.html` 側へ実施設名を書くと、**配布物へ施設名が埋め込まれる**。
        - **施設が増減したときは `morning-check.js` 側を更新する。** 理由の詳細は同ファイル冒頭のコメントを参照。
      - **監視が黙って落ちないようにしてある**（2026-09-11）。施設マスタの改名・削除・件数異常で「対象0件 → 静かに正常終了」になると、
        その日の未打刻通知が消えたことを誰も知れないため、次はログではなく **LINE へ「設定異常」通知**を送る:
        - `LATE_CHECK_FACILITIES` の施設名が施設マスタに見当たらない（改名・表記ゆれ・削除）
        - コード側の期待件数（`DEFAULT_FACILITIES` から導出）が 1 件以上あるのに、その回の対象が 0 件

        施設マスタが `MAX_FACILITIES`（50件）を超えた場合は採用せず `DEFAULT_FACILITIES` へ倒す（黙って切り詰めない）。
      - 施設名は LINE 本文とログへ入る前に `safeFacilityLabel()` を通す（改行・制御文字・ゼロ幅・双方向制御の除去、URL スキームの無害化、40コードポイントで切り詰め）。**照合には使わない**（照合は `normalizeFacility`）。
      - ⚠⚠ **これで「朝通知の抑止」が防げるようになったわけではない。** `tc5_records` の `.write` は `auth != null`（**匿名でも可**）なので、
        施設ごとに偽の `clockIn` を1件ずつ書けば、その日の未打刻通知は今も消せる。打刻自体を止められない以上ここは塞げず、既存リスクとして許容している。
        判定時刻をコード側へ移したのは、**画面に一切現れない隠しフィールドで**同じことができる状態を作らないため、という限定的な目的である。
      - どの回かは起動経路（cron 式 / `inputs.checkHour`）から決めて `CHECK_HOUR` で渡す。**実行時の現在時刻からは決めない**（下記の遅延のため、6時の回が7時台に走ると6時の施設が飛ばされ7時の施設へ二重通知される）。未知の cron 式ならワークフローが `exit 1` する。
    - ⚠⚠ **時刻どおりの通知は cron ではなく外部スケジューラからの `workflow_dispatch` が担っている**（2026-09-11 実測）。
      `schedule` の cron は**このリポジトリでは全く当てにならない**:

      | 指定 | 実際の起動（直近10日・UTC） | 遅れ |
      |---|---|---|
      | `"3 21 * * *"`（21:03 UTC） | 22:48 / 22:51 / 22:58 / 23:04 / 23:08 / 23:12 / 23:14 / 23:14 / 23:18 / 23:25 | **1時間45分〜2時間22分** |

      過去には 8時間遅れも観測されている（2026-08-28）。`fcm-notify` は30分間隔指定に対し1日数回しか走っておらず、**取りこぼしも起きている**。
      PUBLIC リポジトリの scheduled run は GitHub 側の負荷で遅延・ドロップされるためで、こちらでは制御できない。
      - **6時分**: 外部スケジューラが毎日 **21:03:03 UTC ちょうど**に `workflow_dispatch`（`inputs` 空 → 6時判定）。これが確実な経路。
      - **7時分**: 外部スケジューラ（**cron-job.org**）に **22:03 UTC / `inputs: {"checkHour": "7"}`** を登録済み（2026-09-11 にユーザーが登録）。
      - **確認済み（2026-09-11 実測）**: cron-job.org と同じ REST ペイロード
        （`POST /repos/.../actions/workflows/{id}/dispatches` に `{"ref":"main","inputs":{"checkHour":"7"}}`）で
        本番の `morning-check.yml` を起動し、`判定時刻: 7時（決定元: inputs.checkHour）` → 7時の対象2件で完走することを確認した。
        **ワークフロー側の写像は検証済み**である。
      - **未確認（2026-09-11 時点）**: cron-job.org が実際に 22:03 UTC に撃つかどうか。
        **初回発火は 2026-09-12 07:03 JST（= 2026-09-11 22:03 UTC）**で、それまで観測できない。
        初回発火後に `判定時刻: 7時（決定元: inputs.checkHour）` を1度だけ目視確認するまで、
        「登録済みで稼働確認済み」と扱わない。
        - 確認コマンド: `gh run list --workflow=morning-check.yml --limit 10 --json event,createdAt,conclusion`
          → 22:03 UTC 付近に `workflow_dispatch` の行が出ていること。
          `gh` が使えない場合は https://github.com/rsb79692-create/timecard/actions を見る。
      - ⚠ **登録値は文字列 `"7"` ちょうど**であること。誤った値の壊れ方は2種類あり、**どちらも LINE には何も出ない**
        （`morning-check.js` へ到達しないため `findMonitoringAnomalies` も働かない）:

        | 登録値 | 何が起きるか | どこで気づけるか |
        |---|---|---|
        | `"07"` / `" 7 "` / `"6.0"` 等の文字列 | run は作られ、allowlist（`case "${HOUR}" in 6\|7`）が `exit 1` | **Actions の run が赤くなる** |
        | JSON 数値 `7`・真偽値など非文字列 | GitHub API が `422 Invalid value for input 'checkHour'` を返し、**run が作られない**（2026-09-11 実測） | Actions には何も出ない。**cron-job.org 側の実行履歴（HTTP ステータス）でしか気づけない** |
      - `morning-check.yml` の cron 2本は、外部スケジューラが止まったときの取りこぼし防止（遅れてでも通知する）として残す。**時刻の正確さを cron に期待しない。**
      - ⚠ **これは「停止時の保険」ではなく「常時二重化」である。** 外部スケジューラが正常な平常時も cron は毎日起動しており
        （上の実測表がその記録）、定常状態は **判定時刻2種 × 各2 run ＝ 1日最大4 run**。
        そのため、定刻（06:03 / 07:03 JST）に未打刻だった施設がその約2時間後もなお未打刻なら、
        **同じ施設・同じ判定時刻で2通目の LINE が届く**。これは既知・許容の挙動である。
        本節が保証する「二重通知しない」は**6時の回と7時の回のあいだ**の話であり、同一判定時刻の二重実行は対象外。
        1日1通に統一したい場合は cron 2本を外すことになるが、そのとき外部スケジューラが止まると通知が完全に消える。
      - ⚠ **外部スケジューラ（cron-job.org 等）の設定はリポジトリ外にあり、Agent からは変更しない**（本書の禁止事項）。必要なら人間に依頼する。
  - 通知確認 LINE通知（`scripts/notify-check.js`、`.github/workflows/notify-check.yml`、手動 `workflow_dispatch` のみ）
  - 打刻修正申請 FCM Push通知（`scripts/fcm-check.js`、`.github/workflows/fcm-notify.yml`、JST 8–22時に30分間隔）
  - 写真アップロード通知（管理者向け。LINE: `api/line-notify.js` / Discord: `api/discord-notify.js`、いずれも Vercel）
- **移動距離申請**: 職員別ON/OFFの独立モジュール（2026-08 追加）。詳細は下記「移動距離申請」節。
- **打刻時の顔撮影**: スタッフ別ON/OFF（既定OFF）。心理的な不正抑止のみで、写真は保存も送信もしない（2026-09-12 追加）。詳細は下記「打刻時の顔撮影」節。
- **施設端末の持ち出し検知**: 施設の設定範囲外へ持ち出されたら管理者へLINE通知（2026-09-12 追加）。判定はサーバ側 `/devmon`、検知は専用アプリ `devicewatch/`。確定の主経路として外部スケジューラ（cron-job.org・1分間隔）からの `POST /api/device-report` `action:"sweep"` を**実装した**（2026-09-13）。`DEVICE_SWEEP_KEY` と cron-job.org のジョブは **2026-09-13 に登録済み**（cron が毎分発火していることを Vercel の runtime ログで実測。誤った鍵で 403 が返るので鍵も登録済み）。⚠ **ただし cron が送る本文が JSON として読めておらず、確定の主経路はまだ機能していない**（下記「本文が JSON でないときは 400 `bad_json` を返す」）。詳細は下記「施設端末の持ち出し検知」節。
- **PWA**: `manifest.json` + Service Worker `sw.js`（オフライン対応・キャッシュ）。

> 各機能の業務上の詳細仕様（締め日・集計ロジック等）は本リポジトリからは断定できない部分があり、その範囲は**未確認**。

---

## 移動距離申請（2026-08 追加・独立モジュール）

**2026-08-14 に方式を変更した。応援（施設変更）打刻を移動経路の正本とし、自動集計する。**
職員の毎日の申請操作は不要になった（手入力は打刻で拾えない例外のときだけ使う）。
既存の打刻・出退勤計算・有給・賄い・スタッフ管理・管理者PIN・Service Worker には手を入れていない。

### 自動集計（応援打刻が正本）— 2026-08-14

対象は **勤務中の施設間移動だけ**。自宅→最初の勤務施設、最後の勤務施設→自宅、通常の通勤は対象外。

**経路の作り方**

1. **起点施設 ＝ その日の最初の `clockIn` の `workFacility`**（未記録の旧データのみ `facilityName` で補う）。
   応援先へ直行した日は応援先が起点になる（自宅からの移動は対象外なので、これが正しい）。
2. `facilityChange` を timestamp 昇順に並べ、`workFacility`（移動先）を順に追加する。
   ★ `facilityChange` では `facilityName` へフォールバックしてはならない。`facilityName` は
   打刻した端末の施設＝**移動元**であり、補うと実際と逆の経路を作る。
3. **最後は必ず起点施設へ戻す。** 実際の退勤打刻が応援先でも、帰りの移動は必ず発生している。
   したがって **退勤打刻の有無は経路に影響しない**（未退勤は注記だけ残す）。
4. 同じ日に出勤が2回以上ある場合は、勤務ごとに「起点→…→起点」を作って合算する。

**推測で確定しないこと（打刻忘れ・不整合の扱い）**

| 状況 | status | 挙動 |
|---|---|---|
| 正常 | `auto` | 自動計算済み。合計に入る |
| 出勤打刻が無い／出勤施設が未記録 | `no_origin` | 起点未確定。**距離も金額も確定しない**（応援打刻から起点を推測しない） |
| 応援打刻だけある（出勤なし） | `no_origin` | 同上 |
| 区間距離が未登録／施設が地点マスタに未対応 | `missing_leg` | **0km にしない**。対象区間・対象施設を管理画面に明示 |
| 移動先が記録されていない施設変更打刻がある | `broken_punch` | 区間が1つ欠けたまま「正常」に見せない。管理者が打刻を修正または削除する |
| その日の打刻修正申請が未処理 | `pending_correction` | 確定しない（承認・却下後に再計算される） |
| その日の手入力申請が未処理 | `pending_request` | 確定しない |
| 利用ON職員の氏名を打刻と突き合わせできない | `name_unresolved` | 黙って支給漏れにせず要確認にする |
| 施設間の移動が無い | （行を作らない） | — |

`auto` / `manual` 以外はすべて **要確認**であり、**1件でも残っていると月次確定できない**（サーバが 409 `unresolved`）。

★ **`no_origin` の復旧は「打刻修正の承認」ではできない。** 打刻修正申請の承認処理
（`index.html` の `.btn-crq-approve`）は**時刻しか書かず、`workFacility` を付けない**。
そのため承認で作られた出勤打刻は施設が空のままで、`no_origin` は解消しない。
既存レコードの `workFacility` を後から設定できる管理UIも存在しない。
**実際に効く復旧経路は「打刻に無い移動の手入力（例外）」を職員が登録し、管理者が承認すること**だけである
（承認済み手入力はその日の自動集計を上書きする）。
1人1日の解消不能な要確認で**月全体の確定が止まる**ため、この復旧手順は画面の案内文にも明記してある。
案内文（`MILEAGE_AUTO_HELP.no_origin` と `unresolved` のエラー文）を
「打刻を修正してください」に戻してはならない。

★ **対象施設に日付をまたぐ勤務（夜勤など）は存在しない**（2026-08-14 ユーザー確認）。
そのため、集計を `r.date` で日別に束ねる実装で問題ない。
もし将来、日跨ぎ勤務が発生する運用になった場合は、出勤が N日・施設変更が N+1日に記録され、
翌日側が出勤打刻なしの応援打刻だけになって**恒久的に `no_origin`** となり、月全体の確定が止まる。
その時点で日跨ぎの扱いを設計し直すこと。

**打刻修正との関係**

・打刻修正が承認されると `tc5_records` が書き換わるため、**次の集計から自動的に新しい経路になる**（再計算の特別な処理は無い）。
・ただし打刻修正で変えられるのは**時刻だけ**（`index.html` の申請フォームが時刻しか受け付けない）。
　施設は変えられないため、**経路と距離は打刻修正では変わらない**。
・**確定後**に打刻が変わっても、確定済みの金額は**絶対に自動で書き換えない**。
　管理画面「自動集計」タブの「サーバで再集計して確認」（`autoCheck`）が **確定後差異あり**として提示する。
　判定は `/mileage/monthly/{eid}/{ym}.days[].sig`（経路の指紋＝施設名の並び）と km の突き合わせ。
　**時刻だけの打刻修正は経路も距離も変えないため差異にならない**（支給額に影響しないため、意図した挙動）。

**手入力（例外）との優先順位**

承認済みの手入力がある日は、その日の自動集計より**手入力が優先**される。却下された手入力は無効で自動集計へ戻る。
利用OFFにした職員でも、その月に承認済みの手入力が残っていれば**手入力分だけ**は集計・確定される（支給漏れを作らない）。

**共通ロジックはサーバとクライアントで同一ソース**

`api/_lib/mileage-auto.js` の `// ==== SHARED-AUTO-BEGIN ==== 〜 END ====` ブロックは
`index.html` にも**1文字も違わない同一内容**で存在する。表示（クライアント）と確定（サーバ）が
食い違うと画面の金額と支給額がずれるため、`scripts/test-mileage.js` がソース一致を検証している。
**片方だけ変更してはならない。**

**打刻データの読み取り**

・画面表示は**クライアント側で計算する**。
　★ **起動時に全件取得しているわけではない**（2026-08-28 に変更。旧記載は破棄）。
　起動時は当日分だけで、表示対象月の打刻は次の2つが確保する。
　・職員画面: `punchScreenRecordRange()` が `mileage.myMonth` まで範囲を広げる
　・管理画面: `ensureAdminRecordRanges()` が `mileage.adminYm` の月を取得する
　**この2つの拡張を削除してはならない。削除すると集計が黙って空になる（支給額に直結）。**
・**確定（`closeMonth`）と確認（`autoCheck`）はサーバが打刻を読み直して計算する。画面の数値は受け取らない。**
・サーバから `/honomi` を触ってよいのは **`api/_lib/mileage-punch.js` だけ**。同ファイルは
　**読み取り専用**（`dbPut`/`dbPatch`/`dbPatchRoot` を1つも呼ばない）で、参照するのは
　`tc5_records` と `tc5_correction_requests` の2つだけ。**`tc5_staff` / `tc5_pins` は参照しない**
　（誰でも書けるため、本人特定に使うと成りすましが成立する。社員番号の正本は `/mileage/identity` のまま）。
・**サーバ側（`api/_lib/mileage-punch.js`）は従来どおり `tc5_records` を全件取得する。**
　呼び出しは月次確定・確認のときだけなので、通信量は既存の日次バッチと同程度に収まる。
　★ 2026-08-28 に `database.rules.json` へ `honomi/tc5_records/.indexOn: ["date"]` を追加した（下記）。
　　クライアント側は日付範囲取得へ移行したが、**サーバ側は全件取得のまま変更していない**。

**全件取得の実測値（2026-08-14 本番で実測。推測ではない）**

| 項目 | 実測 |
|---|---|
| `tc5_records` 件数 | **3,869 件** |
| JSON サイズ | **744,919 B ＝ 0.71 MB** |
| データ期間 | 2026-04-01 〜 2026-08-14（約4.5か月） |
| `tc5_correction_requests` 件数 | 52 件 |

増加ペースは約 10,300 件／年（約 1.9 MB／年）。**現在の 10 倍（約 7 MB・運用7年相当）でも
CPU は 0.2 秒未満**（合成データ実測: 31,000件=4.29MB で 34.5ms、104,000件=12.1MB で 130ms）で、
律速は転送量のみ。クライアント側のタイムアウト 30 秒に対して十分な余裕がある。
→ **現時点で `vercel.json` の `maxDuration` 明示や取得範囲の限定は不要**。
再検討が要るのは、打刻件数が **4 万件（約 7 MB）を超えたとき**。

★ **`orderBy="$key"` + `startAt` で取得範囲を絞る最適化を採用してはならない（2026-08-14 実測で否定）。**
`index.html` のコード上は `"rec_" + Date.now() + "_" + …` を生成するが、**本番の実データは
その形式に1件も一致しない**。実測した ID の形は次のとおりで、3系統が混在している。

| ID の形（数字→9・英小文字→a に伏せた表記） | 件数 |
|---|---|
| `a9999999999999` | 1,007 |
| `9999999999999` | 733 |
| UUID 形式 | 約 2,129（すべて異なる形） |

さらに **辞書順は時系列順にならない**（3,869 件中 **1,285 件が順序逆転**）。
`startAt` で絞ると**大量のレコードが黙って欠落し、そのまま支給額になる**。
現行実装は `timestamp` で並べ替え、`id` は同時刻のときのタイブレークにしか使っていないため影響を受けない。
**`id` に時系列の意味を持たせる変更を入れてはならない。**

**施設名と地点マスタの対応**

**施設マスタの正本は `/honomi/master/locations`**（配列。要素は `{name, token}` の2キーのみ）。
**ID を持たず、名前が実質のキー**である。追加と削除しか無く、**改名機能は存在しない**
（`index.html` の拠点トークン管理。改名は「削除＋追加」になる）。

`/mileage/places/{id}.facility` に打刻の施設名を入れて対応づける（管理画面「地点・区間距離」タブで選択）。

**二重入力は「施設マスタから取り込む」で無くす（2026-08-14 追加）**

管理画面「地点・区間距離」タブのボタンで、施設マスタの施設をそのまま地点として登録できる。
未対応の施設だけが対象で、**同じ施設を二重に作らない**（判定は `mileageFacilityLinkState`）。
同じ判定で「複数の地点が同じ施設名を握っている」状態も検出し、**競合として赤バナーで明示する**
（`placeMap` はその施設を未対応にするため、「取り込み済み」と表示すると押しても何も起きない行き止まりになる）。

★ **旧施設名（`aliases`）を使うときは「打刻の施設」も必ず設定する**（サーバが `alias_without_facility` で拒否する）。
`facilities` が非空になると `placeMap` は**地点名フォールバックを行わない**ため、
「打刻の施設」未設定のまま旧名だけ入れると、地点名で対応していた施設が黙って対象外になる。

★ **サーバから `/honomi/master/locations` を読む方式にしてはならない。**
ただし理由を取り違えないこと。**入力データの信頼度はどちらでも同じ**である
（画面が持つ `masterFacilities` も同じ `/honomi/master/locations` から読んでいる。
2026-09-06 以降、`master` の**書き**は役割トークンが要るようになったが、**読み**は匿名でも通る）。
**「クライアント経由だから安全」ではない。**

実際に効いている防御は次の2つだけである。

1. **管理者が確認ダイアログで対象施設名を全件見て承認する**（人の目が必ず1回入る）
2. **`savePlace` がサーバ側で再検査する**（`normText` 正規化・`duplicate_name`・`duplicate_facility`）

サーバが自動で取り込む方式にすると **1 が消える**。それが避けたい差分である。
`scripts/test-mileage.js` が「サーバは `master/locations` を読まない」ことを固定している。

取り込み件数には上限（50件）を設けている。`master/locations` は誰でも書けるため、
上限が無いと**大量注入された施設名がそのまま特権書込（`savePlace`）の回数に変換され**、
管理者の書込枠（10分300回）を枯渇させて承認・月次確定まで止められる。
取り込みループは 429 を受けたら即中断する。

**取り込みは逐次で行う。並列化してはならない。**
`savePlace` の重複検査は `loadPlaces()` のスナップショットに対する read-modify-write であり、
並列にすると複数が同時に検査を通過して**同名の地点が2件でき、その施設が `placeMap` で
丸ごと未対応になる**（集計が黙って壊れる）。初期設定で1回だけの操作なので、待ち時間より整合性を優先する。

打刻施設ではない地点（本社など）のために、**手動の地点追加は残す**。削除しないこと。

**改名への耐性：旧施設名（`aliases`）**

施設を改名すると、過去の `tc5_records.workFacility` は**旧名のまま残る**（打刻は書き換えない）。
`/mileage/places/{id}.aliases`（配列・最大10件）へ旧名を入れると、**過去打刻と新規打刻が同じ地点として集計される**。
集計側は `facility` と `aliases` を合わせた `facilities` だけを見る（`mileageAutoFacilitiesOf`）。

・`aliases` を持たない既存データはこれまでどおり動く（後方互換）
・現在名と旧名の**両方**について他地点との重複を検査する（`duplicate_facility`）。
　片方だけ検査すると「A地点の現在名＝B地点の旧名」が成立し、その施設が未対応になる
・**改名しても打刻データを書き換えてはならない。** 打刻はその時点の事実として保持する
未設定の地点は**地点名と施設名が完全一致する場合だけ**自動で対応する。
同じ施設名を2つの地点へ割り当てることはできない（`duplicate_facility`。どちらか推測せず、割り当て自体を拒否する）。

### データの置き場所（重要）

**RTDB のルート直下 `/mileage`。`/honomi` の外＝`database.rules.json` に未定義＝デフォルト拒否。**

`/honomi` は 2026-09-06 まで `.read`/`.write` = `auth != null` で、匿名でも全部読み書きできた。
いまは役割トークンが要るが、**`tc5_records` と `tc5_pins` は打刻のために匿名のまま開けてある**。
そこへ置くと職員が開発者ツールから「自分の利用ON/OFFフラグ」「区間距離」「km単価」「承認状態」を
書き換えられる。したがってこの機能のデータは `/honomi` に置かない。

```
/mileage/_meta                                   {createdAt, version}
/mileage/settings                                {ratePerKm, roundMode, updatedAt, updatedBy}
/mileage/enabled/{employeeId}                    true            ← 利用許可。未存在＝OFF（既定は必ずOFF）
/mileage/identity/{subject}                      {employeeId, name, ...}  ← ★認証サブジェクト→社員番号の唯一の正本
/mileage/staff/{employeeId}                      {name, subject, ...}     ← 社員番号→氏名（管理画面・CSVの表示元）
/mileage/places/{placeId}                        {name, facility, aliases[], order, active, createdAt, updatedAt}
                                                 ← aliases = 旧施設名（改名前）。最大10件
                                                 ← facility = 打刻の施設名（自動集計の対応づけ）
/mileage/legs/{fromId}__{toId}                   {km, updatedAt, updatedBy}   ← ★方向別が正本
/mileage/requests/{YYYY-MM}/{employeeId}/{reqId} 申請1件（1日1件。reqId = "d_YYYYMMDD" で冪等）
/mileage/monthly/{employeeId}/{YYYY-MM}          月次確定スナップショット（給与計算の正本）
                                                 days:[{date,km,source,sig,route}] ← 確定後差異の検知に必須
/mileage/closings/{YYYY-MM}                      {closedAt, closedBy, ratePerKm, roundMode, ...}
/mileage/audit/{YYYY-MM}/{logId}                 変更履歴（誰が・いつ・何を）
```

### 「誰の申請か」の決め方（★ここを間違えると成りすましが成立する）

`/api/auth/staff` が発行するトークンの uid は `"s:" + subjectKey(氏名)` である。
これを社員番号へ変換する対応表を **`/mileage/identity/{subject}` に置き、`tc5_staff` は一切参照しない。**

**`tc5_staff` から引いてはならない理由**：`tc5_staff` は `/honomi` 配下にあり、
**読みは匿名でも通る**（2026-09-06 以降、書きは役割トークンが要る）。そこから社員番号を引くと、

1. 攻撃者が `tc5_staff` へ「任意の氏名 ＋ 被害者の社員番号」の行を足す
2. その氏名で PIN を登録して staff ロールのトークンを取る
3. サーバが被害者の社員番号として解決する

という経路で、「自分の申請だけ」も「利用ONの職員だけ」も同時に破れる。
**body の申告を使わないだけでは足りない。参照先が誰でも書ける場所ではいけない。**

対応表を書くのは `setEnabled`（管理者が「使用する／しない」を設定する操作）だけである。

- **`/authz/pins/{subject}` が存在する（＝実在するログイン）職員にしか割り当てない**（`pin_not_registered`）。
  PIN未登録の氏名を有効化すると、その氏名のPINを第三者が先に登録して当人として入れてしまう。
- 同じ氏名に別の社員番号を割り当てようとすると `identity_conflict` で拒否する（同姓同名の取り違え防止）。
- **社員番号の訂正は `rebindFrom` を付けた1回の呼び出しで行う。**
  「旧番号をOFF → 新番号をON」の2段にすると、片方だけ成功したときに
  「OFFにはなったが新番号では有効化できない」復旧不能な状態が残る（実際にその実装で作り込んだ）。
- 改名では、古いサブジェクトの対応を必ず外す（資格が二重に残らないようにする）。
- **サブジェクト導出用の氏名を `trim` してはならない。** ログイン側（`/api/auth/staff`）は
  `tc5_staff` の氏名をそのまま `subjectKey` へ通すため、片方だけ正規化すると
  当人だけが恒久的に 403 になり、原因が画面から分からない形で壊れる。
- **改名にともなう付け替えでは PIN 実在検査を行わない。** 改名時は `renamePinInAuthz`
  （`/api/auth/pin-set`）と本APIが並行して走り、こちらが先に着地すると
  「PIN未登録」という事実と違う失敗になり、当人が無言で利用不可になる。
  同じ社員番号が別サブジェクトへ紐づいている＝そのログインの実在は前回確認済み、と扱う。
- **利用を止める操作（OFF）も PIN 実在検査で止めない。** 権限の剥奪は常に通す。

### 既知の残存リスク（未解消・運用で認識しておくこと）

1. **`tc5_staff` が誰でも書けることに由来する経路は完全には塞げていない。**
   第三者が `/honomi/tc5_staff` へ氏名を足し、`/api/auth/pin-set` の初回登録経路で
   自分でPINを登録したうえで、**管理者がその行の「ONにする」を押してしまう**と、
   その人物が当該社員番号の申請者として扱われる。
   緩和として、`setEnabled` の応答に `pinUpdatedAt`（PINの登録時刻）を含め、
   **48時間以内に登録されたPINの職員を新規に有効化するときは管理画面が警告する**。
   根本解決は `/honomi` の Rules 強化か `pin-set` 初回登録の本人確認であり、本機能の範囲外。
   → **見覚えのない氏名を「ONにする」前に必ず本人確認すること。**
2. **社員番号を付け替えると、旧番号の申請・月次確定データは旧番号のまま残る。**
   同じ月に旧番号の承認済み申請があると、月次確定・労務士CSVに
   **同じ氏名で2行（旧番号・新番号）**が並ぶ。金額は壊れないが給与側で名寄せが要る。
   → **付け替えは月初（その月の申請が無い時点）に行うこと。**
   旧番号は `/mileage/retired/{旧番号}` へ退役登録され、他の職員へ再利用できない。
3. **利用をOFFにしても `/mileage/identity` と `/mileage/staff` の行は残る**（`enabled` だけ消える）。
   OFF後も「以前使っていた職員」として管理画面に出る。掃除の運用は未定義。
4. **`/mileage/audit` を API から返してはならない。** 監査ログには `claims.t`
   （管理者・閲覧用トークンのハッシュ先頭16桁）が入る。Rules 未定義でクライアントからは
   読めないが、API で返すとハッシュを外へ出すことになる。
5. **利用設定の送信が失敗したときの再送はメモリ上のフラグ（`mileage.syncFailed`）に依存する。**
   失敗のアラートを見た管理者がその場で保存し直さず、画面を再読み込みするとフラグが消える。
   その場合は「移動距離」タブの利用職員一覧で ON/OFF を付け直すこと。
6. **PIN登録時刻の警告は誤検知しうる。** 本人が最近PINを変更した／久しぶりにログインして
   旧形式PINが自動昇格した場合も `updatedAt` が更新されるため、48時間の警告が出る。
   警告＝不正ではない。「見覚えのない氏名かどうか」で判断すること。
7. **自動集計の金額は「打刻データと同じ信頼度」までしか保証できない（2026-08-14 追加）。**
   `/honomi/tc5_records` は打刻のため**匿名でも書ける**ままなので、匿名認証を通せば書き換えられる
   （2026-09-06 の変更でもここは塞いでいない。塞ぐと打刻ができなくなるため）。
   応援打刻を正本にする以上、打刻を偽造されれば距離も偽造される。
   ★ **これは本機能で新たに増えたリスクである。「偽造すれば賃金も動くから移動距離だけの追加リスクにはならない」
   というのは誤りなので、その理由で許容してはならない**（2026-08-14 の監査で訂正）。
   `facilityChange` は `clockIn` / `clockOut` を伴わないため**総労働時間を変えない**（施設別の内訳にしか効かない）。
   したがって「利用ON職員の氏名＋`facilityChange`＋遠い施設」を1件書くだけで、
   **賃金には一切現れずに移動距離だけを水増しできる**。
   従来の手入力方式は金額の根拠が `/mileage/requests`（サーバ経由でしか書けない）＋管理者の1件承認だったため、
   改ざん可能な領域が金額の入力元に入ったこと自体が新しい。
   → 担保は運用側にしかない。**管理者は月次確定の前に「自動集計」タブで日別の経路を必ず目視すること。**
   特に **身に覚えのない応援（施設変更）が入っていないか**を見る。確定してしまうと金額は焼き込まれる。
   根本解決は `/honomi` の Rules 強化であり、本機能の範囲外。
8. **利用ONの職員の氏名が `/mileage/staff` と `tc5_records.staff` で食い違うと、集計が黙って0件になる（2026-08-14 追加）。**
   改名時は `setEnabled` と打刻の `staff` 書き換えの両方が走るため通常は一致するが、
   片方だけ失敗すると「移動があるのに何も出ない」状態になり、画面からは移動が無い日と区別できない。
   → 管理画面「自動集計」タブは**氏名を解決できない職員を明示する**が、
   氏名が解決できていて打刻が0件の場合は区別できない。改名直後は日別の内容を確認すること。

### 出荷前に実機で必ず確認すること（静的確認では代替できない）

- 利用OFFの職員に移動距離メニューが出ないこと／URL直打ちでも操作できないこと
- 利用ONの職員が申請〜承認〜月次確定〜労務士CSVまで通ること
- **応援打刻の実機確認（2026-08-14 追加）**:
  出勤（ナナイロ）→ 施設変更（ハルイロ）→ 退勤（ハルイロ）と打刻して、
  職員画面と管理画面に **ナナイロ → ハルイロ → ナナイロ** の経路と距離が出ること
- **打刻忘れの日**（出勤打刻なしで施設変更だけ）が **起点未確定** になり、確定できないこと
- 未登録区間・地点マスタ未対応の施設がある日が **距離未登録** になり、0km で確定されないこと
- 打刻修正申請を承認したあと、経路と距離が新しい打刻に合わせて変わること
- **確定後**に打刻を変えても確定済みの金額が変わらず、「サーバで再集計して確認」で
  **確定後差異あり**として出ること
- **氏名と社員番号を同時に変更したあと**、旧番号の `enabled` と旧サブジェクトの `identity` が
  残っていないこと（残ると第三者に旧番号の権限を取られる）
- 有効化済みの職員を**改名**した直後に、本人が新しい氏名でログインして移動距離が使えること
- 共有端末でスタッフを切り替えたとき、前の職員の申請一覧が残らないこと

`database.rules.json` は**変更していない**（`/mileage` はルール未定義のまま＝クライアントから到達不能）。

### API（唯一の窓口）

**`POST /api/mileage`**（`api/mileage.js`。共通ロジックは `api/_lib/mileage.js`、
自動集計の計算は `api/_lib/mileage-auto.js`、打刻の読み取りは `api/_lib/mileage-punch.js`、
標準区間距離と取り込み計画は `api/_lib/mileage-standard.js`）。
読み取りも書き込みもすべてここを通す。`index.html` から `/mileage` へ `authFetch` することはない。

権限は `api/mileage.js` の `ACTIONS` テーブルが正本。役割は Firebase Custom Token の claims から取る。

| role | 取得元 | できること |
|---|---|---|
| `s`（職員） | `/api/auth/staff` | 自分の申請の参照・作成・更新・削除のみ。**利用ONが必須** |
| `a`（管理者） | `/api/auth/admin` | マスタ・単価・利用ON/OFF・承認・月次確定・要確認/差異の確認（`autoCheck`）・標準区間距離の取り込み（`importLegs`）。`S.adminSessionValid` 必須 |
| `v`（労務士） | `/api/auth/share` | **確定済み月の参照のみ（完全読み取り専用）** |
| `d`/`x`/匿名 | — | 全 action 拒否 |

- 職員の**社員番号はトークン → `/mileage/identity` から解決する**（`resolveStaff`）。
  body の申告も `tc5_staff` も使わない（上記「誰の申請か」の節を参照）。
- `r:"a"` を特権として扱うため、`isValidAdmin()` が **`S.adminSessionValid(claims)` を通している**
  （管理者PIN・管理者URL変更で失効した旧セッションを締め出す既存の取り決めに従う）。
- `r:"v"`（労務士）は **呼び出しのたびに `viewerTokens` の有効性を引き直す**（`isValidViewer`）。
  Firebase の refresh token は「トークンを無効化した」だけでは失効しないため、
  これをしないと閲覧用URLを止めても支給額を読み続けられる。
- 書込系 action にはハード上限（10分窓・職員60/管理者300）を設けている。
  監査ログ `/mileage/audit` は RTDB に TTL が無く自動削除されないため、無制限に増やさせない。
- `api/_lib/google.js` の `dbRequest` ルート直下ルーティングと `allowedRootTops()` に `mileage` を追加済み。

### 標準区間距離（7施設42方向）— `api/_lib/mileage-standard.js`

初期設定用に、7施設の全方向 **42本** を `STANDARD_LEGS` として持つ。出所は2つ。

| 区間数 | 出所 |
|---:|---|
| 30 | 現行Excel `■通勤距離（穂乃味）.xlsm` の `baseT` シート（行=出発地／列=到着地）の実測値 |
| 12 | ハルイロ関連。`baseT` に距離が無く、**2026-08-14 にユーザーが実測値を確認**（往復同値もユーザー確認） |

Excel 上の「貝塚」＝現在の「ミュゲ貝塚」、「春木」＝現在の「ミュゲ春木」（2026-08-14 ユーザー確認）。

★ **非対称な値を「対称のはずだ」と補正してはならない。** 実測で次の2組は方向で距離が違う。

・ハーベスト → ミュゲ貝塚 = **5.3** ／ ミュゲ貝塚 → ハーベスト = **11.9**
・ハーベスト → ミュゲ春木 = **11.9** ／ ミュゲ春木 → ハーベスト = **13.9**

★ **0km を入れてはならない。** 0 は「距離が無い」ではなく「未登録」として扱うのが本機能の前提。

**取り込み（`importLegs`）の安全性は「何を書かないか」で決まる**

管理画面「地点・区間距離」タブ →「標準区間距離を取り込む」（初期設定用のため既定は折りたたみ）。
`apply:false` で差分だけ取得し、管理者が確認してから `apply:true` で登録する。

| status | 挙動 |
|---|---|
| `new` | 未登録。**これだけを書く** |
| `same` | 既に同値。書かない → 何度実行しても結果が変わらない＝**冪等** |
| `conflict` | 既存値が違う。**絶対に自動上書きしない**。一覧に出して管理者が個別に判断する |
| `no_place` | 施設に対応する地点が無い／一意に決まらない。書けないので報告だけ |

`conflict` を上書きしないのは、**現地で実測して直した値が初期データで巻き戻り、しかもそれが誰にも見えない**ため。

### 業務仕様（現行Excel `■通勤距離（穂乃味）.xlsm` を実測して決定。推測ではない）

⚠ **ファイル名の訂正（2026-08-14）**: 旧記載の `■移動距離申請（谷村）.xlsm` は**存在しない**。
数式・単価の根拠は `C:\Users\user\Dropbox\■通勤距離（穂乃味）.xlsm`（`J5=16`／`R8=IF(Q8="","",$J$5*Q8)`／
`CEILING(x,"0:30")` の全 `#REF!` が一致）。
一方、下表の**日別・月別の金額**は同名の **PDF**（`労務・経理/0,労務関連/勤怠データ/■移動距離申請（谷村）.pdf`
および `谷村移動距離申請3月.pdf`）が出典で、Excel には該当シートが無い。**2つの出典を1ファイルとして混記していた。**
端数処理「丸めない」の結論は変わらない（提出済みPDF上で支給額が `105.6` `198.4` 等と1円未満を保持している）。

| 項目 | 実測値 | 実装 |
|---|---|---|
| km単価 | **16 円/km**（月次シート `J5`） | 既定値16。管理画面で変更可。コードに固定しない |
| 距離表 | `baseT` シートの行=出発地／列=到着地のマトリクス | 同じ構造。**方向別が正本** |
| 方向別の根拠 | ハーベスト→貝塚 **5.3** / 貝塚→ハーベスト **11.9**、ハーベスト→春木 **11.9** / 春木→ハーベスト **13.9** と**非対称な組が実在** | 対称と決め打たない。管理画面に「逆方向にも同じ距離を登録」の補助操作あり |
| 距離の取得 | 地点を順に選び `INDEX`+`MATCH` で自動取得 | 同じ。職員は距離を入力しない |
| 日別金額 | `=IF(Q="","",$J$5*Q)`（単価×その日の距離） | 同じ |
| 月合計 | `=SUM(...)` の単純合計 | 同じ |
| **端数処理** | **行っていない（1円未満を小数のまま保持）**。実測で確定 → 下記 | **設定値**（四捨五入／切り捨て／切り上げ／丸めない）。**既定は「丸めない」＝現行Excelと同じ** |

**端数処理の実測根拠（推測ではない）**

- 日別金額 `R列 = IF(Q="","",$J$5*Q)`、月合計 `K39 = SUM(R8:R38)`。**どちらにも丸め関数が無い。**
- キャッシュ値も小数のまま保存されている:
  - 日別: `105.6` / `89.6` / `198.4` / `201.6` / `252.8` / `390.4`
  - 月合計: 202603=`1379.2` / 202604=`2590.4` / 202605=`2339.2` / 202606=`1782.4` / 202607=`2604.8`
- 表示書式は `##.0"Km"`（小数1桁。金額欄に `"Km"` が付いた書式の誤り）。
  **「整数に見せている」のではなく、本当に小数を保持している。**
- シート内に `CEILING` が48箇所あるが、**すべて `CEILING(x,"0:30")` ＝ 勤務時間の30分丸め**で、
  しかも全て `#REF!` エラー。移動距離の金額とは無関係。
- 距離は0.1km刻みなので、`16円/km` では末尾が `.0` / `.5` 以外のとき必ず1円未満が発生する。
  上記のとおり実データで発生しており、Excelはそれを丸めずに支給額としている。

- 丸める設定にした場合は「**月合計距離 × 単価**」に対して**1回だけ**行う（日ごとに丸めると日の分け方で金額が変わるため）。
- 円単位で支給したい場合だけ管理画面「単価・端数」で変更すること。変更しても**確定済みの月の金額は変わらない**。

### 金額が後から変わらない仕組み

- 申請には確定時の**区間距離と地点名をスナップショット**する。
- 承認時は現在のマスタで引き直す（＝承認＝この内容で確定）。差異は監査ログへ残す。
- **月次確定（`closeMonth`）で `totalKm` / `ratePerKm` / `roundMode` / `amount` をスナップショット**する。
  以後 `settings` を変えても確定済み月の金額は動かない。CSV も再計算せずこのスナップショットを出力する。
- **km単価が一度も保存されていない状態では確定できない**（`rate_not_configured`）。
  誰も決めていない既定値で給与データを作らせない。
- 申請は `reqId = "d_YYYYMMDD"` で冪等。「検索してから INSERT」にすると同時実行で同じ日の申請が
  2件でき、月合計＝支給額が二重になるため、日付から決定的に導出して上書き更新にしている。
- 確定済みの月は職員も管理者も申請を変更できない（`month_closed`）。

### 画面

- **職員（打刻画面）**: 利用ONのときだけ「🚗 移動距離」カードを表示。**主画面は当月の自動集計**
  （日別の経路・距離・要確認、月合計距離、支給見込額）。**申請操作は不要**。
  その下に「打刻に無い移動の手入力（例外）」として従来のフォームを残している
  （**未登録区間があると手入力を保存できない＝0kmで保存しない**）。
- **管理者（タブ「移動距離」）**: **自動集計**（既定タブ。要確認の日だけ表示する絞り込み／サーバで再集計して確認）／
  手入力の承認／地点・区間距離（マトリクス表示・打刻の施設との対応づけ）／単価・端数／月次確定／利用職員。
  **要確認が残っている月は確定ボタンを押せない**（サーバも 409 `unresolved` で拒否する）。
- **スタッフ管理モーダル**: 「移動距離申請：使用する」チェックボックス。
  ★ **`tc5_staff` には保存しない。** サーバの `/mileage/enabled` が正本（保存時に `setEnabled` を呼ぶ）。
- **労務士（閲覧用URL）**: 「🚗 移動距離（確定済みのみ）」。対象年月を選び、月次CSV／日別明細CSVを自分でダウンロードする。
  日別明細は**確定済みスナップショットの `days`** から作る（未確定・要確認は混ざらない）。
  管理者がファイルを作って渡す運用にはしない。編集系のUIも API もない。

  ★ **閲覧用URLでも役割トークン（`r:"v"`）の確立を必ず待ってから `/api/mileage` を呼ぶ**（2026-08-14 修正）。
  `elevateShareSession` は起動時に走り始めるだけで完了を待たないため、待たずに呼ぶと
  **匿名トークンのまま問い合わせて 403 `forbidden`** になり、正しい閲覧用URLでも
  「データを取得できませんでした。」になる（`mileageBootForViewer` が `_shareElevatePromise` を待つ）。
  職員側 `_elevatePromise` / 管理者側 `_adminElevatePromise` と同じ扱いにする。**この待機を外してはならない。**

  ★ **未確定の月は「取得失敗」と区別して表示する。** サーバは未確定月を **200 / `ok:true` / `closed:false`**
  で返す（エラーにしない）。対象月の候補には**当月が未確定でも入れる**うえで、未確定の月では
  「〜の移動距離は未確定です」と表示し、**ダウンロードボタンを出さない**。
  赤字の「データを取得できませんでした。」は**通信・権限の失敗のときだけ**に使う。

### 運用の流れ

**職員は打刻するだけ → 管理者が「要確認」の日だけ対処 → 管理者が月次確定 → 労務士が対象月を自分でダウンロード**

手入力（例外）を使った日だけ、従来どおり **職員が手入力 → 管理者が承認** が挟まる。

### テスト

`node scripts/test-mileage.js`（依存パッケージなし・送信なし・本番データ非アクセス）。
**移動距離申請に関係する変更では実行必須**（全件 PASS / 0 FAIL でなければ出荷しない）。
金額計算・未登録区間・権限マトリクス（労務士に書込系が入らないこと）・クライアントとサーバの計算一致・
CSV形式を固定する。テスト件数は増減するため固定値を規範にしない。

自動集計の追加後は、次も同じテストで固定している。
・応援打刻からの経路生成（起点→応援先→起点／複数施設／timestamp順／削除済みの除外）
・起点未確定・距離未登録・未対応施設を推測で確定しないこと
・手入力・打刻修正待ちとの優先順位、確定後差異の検知
・**共有ブロック（SHARED-AUTO）がサーバと `index.html` で1文字も違わないこと**
・`mileage-punch.js` が書き込み関数を1つも持たず、`tc5_records` / `tc5_correction_requests` しか参照しないこと

---

## 有給取得履歴（管理者画面・2026-08-30）

管理者画面「有給管理」→ 有給残日数管理の職員行「▼ 詳細表示」を開くと、付与明細の下に
**「有給取得履歴」**が出る。職員ごとに「いつ有給を取得したか」を過去分まで追える。

### 正本と抽出条件（新しいデータは作らない）

・**正本は `tc5_paid_leave_requests` のみ。履歴用のデータを二重保存しない。**
　新しい Firebase パス（`tc5_paid_leave_history` 等）は作っていない。表示のたびに組み立てる。
・抽出は `index.html` の `// ===== PL-HISTORY-BEGIN/END =====` ブロックの
　**純関数 `plBuildLeaveHistory(requests, opts)`** が担う（DOM も通信も持たない）。
・突き合わせは **社員番号（`getReqStaffId`）が申請・付与行の両方にあるときは番号一致だけ**を採用する。
　どちらかが無い旧データのときだけ氏名でフォールバックする。
　★ 氏名だけで突き合わせると**同姓同名の取得履歴が混ざる**。この優先順を緩めてはならない。

### 実績と予定を分ける（★ここを変えると残日数と食い違う）

| 区分 | 条件 | 表示 |
|---|---|---|
| `taken`（有給取得履歴） | `status==="approved"` かつ 取得日 **≦ 今日（JST）** | 既定表示。取得日の降順 |
| `planned`（取得予定） | `status==="approved"` かつ 取得日 **> 今日（JST）** | 別枠・別ラベル。近い日から |
| `pending`（申請中） | `status==="pending"` | **別枠・別ラベル**。実績に混ぜない |
| `other` | `rejected` / `withdrawn` / `canceled` / **未知の status** | 既定は非表示。「取消・却下を表示」で開く |

★ **境界は `buildFutureApprovedLeaveMap`（残日数表示）と同じ「JSTで今日以前＝消化済み」**である
（`plTodayJst = getTodayJSTStr()`）。ここを変えると**画面の残日数と履歴の日数が食い違う**。
★ **pending を「取得済み」として出してはならない。**
★ **未知の status を黙って捨ててはならない**（データ異常を隠さない）。

### 複数日申請は取得日単位に展開する

1申請で複数日を申請できる（`leaveDates`）ため、履歴は**取得日1件＝1行**に展開する。
1日あたりの消化量は **1日固定**（承認時の FIFO 消化が `leaveDates.length` 日で行われるため）。
**半休は現行仕様に存在しない**（申請フォームに日数の入力欄が無い）。
取得日が無い異常データは**日数を推測せず無視する**。
1申請が過去日と未来日にまたがる場合は、日ごとに実績／予定へ振り分ける。

### 誤読させないための3点（★ここを外すと支給に直結する事故になる）

**1. 「実績 合計」は本システムで承認した分だけである。「付与日数 − 残日数」とは一致しない。**

`remainDays` には**運用開始前にすでに消化していた分（carryIn）**が織り込まれている
（付与登録モーダルが付与日数と別に残日数を入力できるため。本番実測で 40付与行中14行・最大10日）。
その分には申請データが無いので、**履歴には原理的に現れない**。
履歴セクションは付与明細（各行に「修正」ボタンがある）と同じカードに並ぶため、
「履歴が足りない＝残日数が誤っている」と読んで残日数を修正すると、**取得済みの有給が復活する**。
そのため見出しに「本システムで承認した分のみ」と明記し、`title` で理由を出している。
★ **この文言を外してはならない。** 履歴と残日数を一致させる方向の実装もしてはならない。

**2. 「取得予定」の日数はサマリー行の「残 N日（M）」の M と一致しないことがある。**

・履歴側 … 承認済みかつ明日以降の**取得日の実数**
・サマリー側 M … `buildFutureApprovedLeaveMap` が**付与行へ戻せる量で上限を切った値**
　（期限切れ・削除済みの付与行、`consumedSnapshot` 欠落では戻し先が確定せず M が小さくなる）

境界（JSTで今日以前＝消化済み）は同じで、違うのは上限クリップの有無だけである。
★ **どちらかに合わせて片方を書き換えてはならない。** 画面に注記を出して区別している。

**3. 改名すると付与行が旧氏名・新氏名の2グループに分裂する。履歴は2つ目以降を出さない。**

スタッフ改名（`index.html` のスタッフ管理）は `tc5_records` / `tc5_approvals` / `tc5_pins` を
付け替えるが、**`tc5_paid_leave_balances.staffName` と `tc5_paid_leave_requests.staffName` は
書き換えない**（既存挙動）。付与行のグループキーは氏名なので、改名後に新規付与すると分裂する。
一方 `normalizePaidLeaveBalance` は両方の `staffId` を同じ `employeeId` に解決するため、
社員番号で突き合わせる履歴は**両グループへ同じ全履歴と同じ合計日数**を出してしまい、
「2倍取得している」ように読める。
そのため `_plHistSidSeen` で社員番号を記録し、**2つ目以降のグループでは履歴を出さず参照先だけ示す**。
★ この抑止を外してはならない。

### 既存ロジックへは触っていない

`remainDays` の正本定義・`grantedDays`・`usedDays`・FIFO消化（`applyFifoConsumption` /
`reverseFifoConsumption`）・`buildFutureApprovedLeaveMap`・承認/却下/引戻処理は**未変更**。
履歴は完全に表示専用で、入力配列（`paidLeaveRequests` のキャッシュ）も書き換えない。

### 通信量（初回取得を増やしていない）

`paidLeaveRequests` は**有給管理タブへ入った時点で既に全件取得済み**（`selAdminTab("paidleave")`）。
履歴はそのメモリ上のデータから組み立てるだけで、**詳細を開いても追加の通信は発生しない**。
「さらに表示」「取消・却下を表示」も再描画するだけで通信しない
（`plHistMore` / `plHistToggleVoid` / `plToggleBalDetail` はいずれも `authFetch` を呼ばない）。
★ **履歴のために `tc5_records` や `tc5_paid_leave_*` の取得を増やしてはならない。**
管理画面の初回取得件数を削減した 2026-08-28 の変更を逆行させないこと。

### 表示（スマホ優先）

・**横長テーブルにしない。** 1件＝1〜2行の縦型（日付・日数・状態を1行目、申請/承認日時を2行目）。
・★ **履歴ブロックは付与明細の表の「外」に置く**（`overflow-x:auto` の閉じ `</div>` の直後、
　「新規付与を追加」の前）。付与明細を開くと表が `min-width:640px` になるため、表の中へ入れると
　スマホ幅で 640px の桁に引きずられて**右端の文字が切れる**。
　★ **`position:sticky` ＋ `width:calc(100vw - …)` で左端に固定する回避策を採ってはならない。**
　引き算する固定値がカードの実パディング・スクロールバー幅と連動せず、**320px 実測で文字が欠けた**。
　積み先は `plHistBlocks`、幅指定は `max-width:620px` だけにして、幅はカードに任せる。
・表の外へ出したぶん、見出しに氏名を出す（「有給取得履歴：〇〇」）。複数職員の詳細を同時に開いても
　どの職員の履歴か取り違えない。**この氏名表示を外してはならない。**
・既定は直近 **10件**（`PL_HIST_PAGE`）＋「さらに表示」で 10件ずつ追加。
・表示状態（`plHistLimit` / `plHistVoid` / `plBalDetailOpen`）は**メモリのみ。Firebase へ保存しない**。

### テスト

`node scripts/test-paid-leave-history.js`（依存パッケージなし・送信なし・本番データ非アクセス）。
**有給取得履歴・`plBuildLeaveHistory`・実績/予定の境界に関係する変更では実行必須**
（全件 PASS / 0 FAIL でなければ出荷しない）。テスト件数は増減するため固定値を規範にしない。
0件／1件／多数／複数日申請／各 status／未知 status／未来日／同姓同名／旧形式データ／
正本を書き換えないこと／表示系ハンドラが通信・書込をしないことを固定する。

---

## tc5_records の取得範囲（2026-08-28）

**クライアントは `tc5_records` を全件取得しない。画面が表示する期間だけを取る。**
**管理・労務士画面の入口でも全件を取らない**（2026-08-28 の第2段で変更。旧記載の「管理・労務士は全期間を1回だけ取得」は破棄）。

`database.rules.json` に `honomi/tc5_records/.indexOn: ["date"]` を追加済み。
RTDB REST は索引が無い `orderBy` を **400 で拒否する**ため、この索引が前提になる。
索引指定だけで `.read` / `.write` は変更していない（権限は不変）。

### 本番実測（2026-08-28）

| 範囲 | 件数 | サイズ |
|---|--:|--:|
| 全件 | 4,267 | 1,038,792 B |
| 当月（2026-08） | 783 | 230,104 B |
| 当日 | 29 | 10,501 B |
| 最古日の取得（`orderBy="date"&limitToFirst=5`） | 5 | 約 700 B（実測: 1件なら 141 B） |
| `tc5_approvals`（全件） | 1,717キー | 54,471 B |

`date` は全件が `YYYY-MM-DD` 形式で存在し、**全件が `timestamp` の JST 日付と一致**する（欠損 0 / 不正形式 0 / 不一致 0）。
データ月は **2026-04〜2026-08 で欠落なし**（最古の打刻日 = 2026-04-01）。

### 取得済み範囲は「区間の集合」で持つ（`_recIvs`）

★ **単一の連続区間にしてはならない。** 管理画面で 8月 → 4月 と月を飛ばして選んだだけで、
あいだの 5〜7月まで巻き込んで取得することになる。
`ensureRecordsRange(from,to)` は `_recGaps()` で**未取得の部分だけ**を取りに行く。

★ **同時に走らせる範囲取得は常に1本だけ**（`_recRangePromise`）。月を切り替えるたびに並走させると、
通信の細い端末で取り合いになる。失敗時は 5 秒あけて再試行する。

★ 月末を `"-31"` で表す上限値（例 `"2026-06-31"`）が混ざるため、隣接判定には `_recNextDay()` を使う。
`Date` へ素で渡すと `"2026-07-02"` になり、**7/1 が抜けているのに「連続している」と誤判定する**。

### 月・年の「取得すべき上限日」は当日で頭打ちにする（`recMonthEnd` / `recYearEnd`）

★ **当月を `"YYYY-MM-31"` まで要求してはならない。**
打刻画面の取得範囲は `punchScreenRecordRange()` が返す `…〜当日` で終わるため、当月末には決して到達しない。
`recordsMonthKnown("当月")` を `-31` で判定すると**永久に偽**になり、
移動距離ONの職員全員が当月のあいだずっと「打刻データを読み込めていません」の赤バナーを見ることになる
（再読み込みしても消えない）。`recMonthEnd()` / `recYearEnd()` を必ず通すこと。

### どの画面がどの範囲を使うか

| 契機 | 範囲 | 関数 |
|---|---|---|
| 起動時 | **当日のみ** | `loadPunchRecords()` → `applyPunchRecords()` |
| スタッフのログイン後 | 直近14日（＋開いているカードに応じて当月・賄いの表示月・**移動距離の選択月（最大12か月前まで遡る）**） | `punchScreenRecordRange()` → `ensureRecordsRange()` |
| 管理・労務士・スタッフ別詳細の入口 | **表示中の月／年／当日だけ** | `ensureAdminRecordRanges()` |
| 承認漏れサマリー・有給付与の自動算出 | 全期間（**月ごとに分割**して背景取得） | `ensureRecordsHistory()` |
| スタッフ改名 | 全期間（必須。`ensureRecordsHistory()` の完了まで実行しない） | `recordsHistoryReady()` |
| 10秒ポーリング（管理画面） | **当日のみ** | `listenFirebase()` 内 |
| 朝出勤確認「最新状態に更新」 | **当日のみ** | `fetchRecordsRange(_d,_d)` → `mergeRecordsRange` |
| 日付が変わったとき | 当日を取り直す | `refreshRecordsIfDayChanged()` |

`ensureAdminRecordRanges()` がタブごとに要求する範囲:

| タブ | 範囲 |
|---|---|
| 勤怠一覧 / CSV出力 / 応援集計 / スタッフ管理 / マスター管理 | `selMonth` ＋ `selDate` ＋ 当日 |
| 賄い一覧 | ＋ `mealAdminMonth` |
| 修正履歴 | ＋ `adjLogMonth` |
| 月別出勤日数 | ＋ `monthlyDaysYear` の 1/1〜`recYearEnd()`（当年は当日まで） |
| 移動距離 | ＋ `mileage.adminYm` |
| 有給管理 | ＋ 全期間（`ensureRecordsHistory` を即座に開始） |
| 労務士（viewer） | `reviewMonth` ＋ `selDate` |
| スタッフ別詳細 / CSVプレビュー | `selMonth` |

### 月／年セレクタの選択肢は「最古の打刻日1件」から作る

★ **選択肢を作るために全件を取得してはならない。**
`orderBy="date"&limitToFirst=5` で運用開始日を取り（実測: 1件で 141 B）、
★ **1件だけ見てはならない**。RTDB の順序規則上 `date` を持たない子が先頭へ来得るため、
弾いてしまうと運用開始月が永久に確定せず、フェイルクローズが解除不能になる。
取得した中から**最小の有効な日付**を採る。
`recordsMonthOptions()` / `recordsYearOptions()` が **運用開始月〜当月**を機械的に列挙する。
最古日は localStorage `tc5_records_oldest` へ保存し、次回起動では即座に使う。

★ **運用開始月には2つの関数がある。取り違えてはならない。**

| 関数 | 値 | 用途 |
|---|---|---|
| `recordsOperationStartMonth()` | **サーバで確認済みの最古日だけ**（未確定なら空文字） | 「その月に打刻が無い」と**断定してよいかの判断** |
| `recordsOldestMonth()` | サーバ値 ∪ メモリ上の `records` の最古月 | **月・年セレクタの選択肢を作るときだけ** |

取得範囲を絞ったことで `records` の最古月は真の運用開始月より新しくなり得る。
これを判断の根拠に使うと、その前の月を「運用開始前＝打刻なし」と誤断定して
**有給の付与日数が取込値（`monthlyDaysImport`）へ黙って落ちる**。
`recordsMonthKnown()` と `_recHistoryStep()` は必ず `recordsOperationStartMonth()` を使う。

候補が減る後退を起こさないため、次を必ず併合する。

・メモリ上の `records` に存在する月／年（前回セッションの端末キャッシュを含む）
・呼び出し側が指定した「選択中の月」（`selMonth` / `reviewMonth` / `mealAdminMonth` など）

★ **固定値を根拠なく埋め込んではならない。** 下限は必ず実データ（最古の打刻日）から取る。

対象箇所: 労務士の年／月セレクタ・スタッフ別詳細・CSVプレビュー・管理画面共通・勤怠一覧の年・
月別出勤日数の年・賄いの月・修正履歴の月（下限）。

### 承認漏れサマリーだけが全期間を必要とする（期間分割で対応）

`getUnapprovedSummary()` / `getNotificationCounts()` / 勤怠一覧の未承認一覧は
`isPastDate(r.date)`（＝**過去日すべて・下限なし**）を対象にしている。
**UI に期間の文言は一切なく**、業務要件として「全期間」が明示されているわけではないが、
**勝手に期間を切っていない**。対象期間は変更前と同一である。

代わりに取得方法だけを変えた。`ensureRecordsHistory()` が

1. 入口の描画から 0.4 秒あけて開始する（表示中の月の取得を優先する）
2. 当月 → 過去へ **1か月ずつ**（実測 約230KB/月）取得する
3. **1か月が着地した瞬間に次の月へ連鎖する**（`_recHistFetch` の `.then` から `_recHistoryStep()`）
4. 運用開始月まで到達したら `_recFull=true` にして確定する

★ **進み方の正本は「完了 → 即連鎖」であって、見張りタイマーではない（2026-09-09 変更）。**
2.5 秒間隔のタイマー（`HISTORY_TICK_MS`）が 1回につき 1か月しか発行していなかったため、
回線がどれだけ速くても **「2.5秒 × 月数」の待ち時間が必ず発生**していた。
**この連鎖を外して「見張りタイマー任せ」に戻してはならない。**

計測は `node scripts/bench-history-scan.js`（依存なし・送信なし・本番データ非アクセス）で追試できる。
`index.html` の RECORDS-RANGE ブロックを実タイマーで動かし、
ネットワークを「RTT ＋ 共有帯域」で模擬する（並列にしても総帯域は増えない）。
件数・転送量は本節冒頭の本番実測値を使う。**本番の管理画面での実測ではない。**

3回ずつ実行した実測（2026-09-09）:

| 6か月・1,043KB | 修正前 | 修正後 |
|---|--:|--:|
| 集計完了（RTT 80ms / 10Mbps） | 14.14〜14.18s | **2.25〜2.26s** |
| 集計完了（RTT 250ms / 1.5Mbps） | 14.77〜14.86s | **10.53〜10.55s**（転送律速） |
| 表示月確保（初期画面が操作可能になるまで） | 0.33s / 0.98s | 0.32s / 0.98s |
| スキャン中に月を切り替えたときの表示待ち | 0.53s / 1.94〜2.12s | 0.54〜0.76s / **2.06〜3.53s** |
| 通信回数 / 転送量 | 6回 / 1,043KB | 6回 / 1,043KB |

★ **低速回線でスキャン中に月を切り替えると、以前より待たされることがある**（実測 2.0秒 → 最大 3.5秒）。
理由は避けられない性質による。変更前は 2.5秒間隔のタイマーのせいで背景が**大半の時間なにも流していなかった**ため、
その空きを表示用の取得が使えていた。空待ちを消すということは、背景が**連続して帯域を使う**ということである。
「集計を速く終わらせる」と「背景の帯域を減らす」は同時に満たせない。

その代わり、**競合しうる時間帯そのものが 14.8秒 → 10.5秒へ短くなっている**。
また同時取得は 1本のままなので、**瞬間的に流れる本数は変更前を超えない**（上限は「2倍待つ」で頭打ち）。
これ以上を求めるなら転送量そのものを減らすしかない（下記「通信量は減っていない（減らせなかった理由）」）。

★ **月切替の待ち時間はブレが大きい**（同じ条件で 2.0〜6.4秒の幅が出た）。
この指標でミリ秒を比べてはならない。見るべきは桁と傾向だけである。

### `HISTORY_CONCURRENCY` は 1（安易に上げてはならない）

改善のほぼ全量は**空待ちを消したこと**から来ており、並列化の寄与は小さい。
同じスクリプトでの実測（集計完了 / 10Mbps・1.5Mbps）:

| 同時取得 | 10Mbps | 1.5Mbps |
|---|--:|--:|
| **1本（現在値）** | 2.26s | 10.49s |
| 2本 | 2.05s | 9.85s |
| 3本 | 1.82s | 9.27s |

一方で背景の同時取得を増やすと、その帯域は**管理者が待っている取得から奪う**。
変更前も背景の飛行は常に 1本だったので、**1本に留めるかぎり
「瞬間に流れる本数」は変更前を超えない**（待ち時間の悪化は「2倍待つ」で頭打ちになる）。
数%の短縮のために対話操作をさらに遅くする取引はしない、という判断である。
（背景が**連続して**帯域を使うことによる悪化は残る。上の表の★を読むこと。）

★ 上げてよいのは、施設の実回線が**遅延律速**（帯域に余裕があり RTT が大きい）だと
**実測で分かったとき**だけである。推測で上げてはならない。
帯域律速の回線では並列化しても集計完了はほとんど縮まず、対話操作だけが遅くなる。

★ **対象期間・判定ロジックは 1 行も変えていない。** 変えたのは待ち時間だけであり、
承認漏れの件数・有給の付与日数は変更前と一致する（`scripts/test-unapproved-summary.js` が固定）。

★ **過去分の取得は表示用（`ensureRecordsRange`）とは別のレーンで行う。**
表示用の単一実行（`_recRangePromise`）へ相乗りすると、月を切り替えたときに
**画面が待っている取得が背景の集計の後ろへ並ばされる**。
逆に背景を優先させないため、`_recRangePromise` が飛行中のあいだは新規の過去分取得を始めない。

★ **取得に失敗したら `_LAZY_RETRY_MS` だけ間隔を置く**（`_recHistRetryAt`）。
連鎖のまま再試行すると、通信断のあいだ撃ち続ける。

★ **ハングしたときの逃げ道を、表示用と過去分の両方に置く。** `authFetch` にタイムアウトが無いため、
**1本ハングしただけで集計が永久に止まり、「集計中です…」が消えなくなる**
（`recordsHistoryReady()` に依存する「全件承認」と有給付与日数の自動算出が、
リロードするまで復旧しない）。基準はどちらも `RECORDS_RANGE_STUCK_MS`。

・表示用: `_recDisplayFetchBusy()` … `_recRangePromise` が飛行中でも、停滞していれば過去分を再開する
・過去分: `_recHistBusy(ym)` / `_recHistActiveN()` … 停滞した取得は**同時実行の席を占有しない**ものとして数え、
　その月を取り直せるようにする

★ **片方だけに逃げ道を置いてはならない。** 過去分レーンに置かないと、
`HISTORY_CONCURRENCY` 本ハングした時点でスキャンが恒久停止する
（**現在値は 1 なので 1本で止まる**。並列度を下げたぶん、この逃げ道は旧より重要になっている）。`_recHistInflight` は真偽値ではなく発行時刻（`{at}`）を持つこと。

★ **`done()` は「自分が登録した記録だけ」を消す**（`_recHistInflight[ym]===tok`）。
遅れて着地した前回の応答が、取り直した新しい取得の記録を消さないための識別であり、
多重呼び出しでも在庫がずれない（冪等）。

★ **1回の取得は 1か月分だけにする。** 数か月をまとめて 1リクエストにすると、
低速回線で「最初の 1か月が出るまでの時間」が伸び、進捗も見えなくなる。

★ **表示用レーンは、背景が取得中の月を二重に取りに行かない**
（`ensureRecordsRange` 内の `_recHistBusy(monthKey(f))` 判定）。
二重取得は約230KBの重複転送になるうえ、表示用の単一実行枠（`_recRangePromise`）を塞ぐため、
**背景スキャンまでその重複が終わるまで止まる**。
・抑止するのは**月まるごとの要求だけ**（`f` が月初）。`selDate` などの単日要求（数KB）まで
　待たせると、スキャン中は日付を選ぶたびに背景の着地を待つことになる。
・背景が停滞（`RECORDS_RANGE_STUCK_MS` 超）すれば表示用が自分で取りに行く。**待ち続けない。**
・月の途中から始まる gap（例: 当月の一部をすでに取得済み）は**抑止対象外**である。
　重複転送は残るが、どちらも新鮮なデータなので**正しさへの影響はない**。
・この判定は `_recRangePromise` / `_recRangeLastTry` を消費しない位置に置くこと
　（消費すると、抑止しただけで 5秒の再試行ゲート（`_LAZY_RETRY_MS`）に引っかかる）。

★ **追い越された古い応答は捨てる**（`_recHistFetch` の `var mine=(_recHistInflight[ym]===tok);`）。
停滞で席を空けたあとは同じ月の 2本目が飛びうる。後勝ちにすると、
**新しい方が埋めた範囲を 30秒以上前のスナップショットで塗り替え、しかも covered 済みなので
二度と取り直されない**（承認漏れ・有給の付与日数が古い値で固まる）。
`ensureRecordsRange` の同名ガード（`if(_recRangePromise!==p)return;`）と**対称に置くこと**。

### 通信量は減っていない（減らせなかった理由）

上記は**待ち時間**の削減であって、**転送量（1.1MB）と通信回数（月数ぶん）は変わっていない**。

・RTDB REST には**列の射影（欲しいフィールドだけ返す）が無い**。
　承認漏れの判定に必要なのは (日付, 氏名, 削除フラグ, 施設) だけだが、打刻レコード全体が返る。
・`_recIvs`（取得済み区間）を端末へ保存して次回起動で再利用すれば通信は 0 にできるが、
　**別の管理者が過去日を編集した場合を検知できない**ため、承認漏れの判定結果が変わりうる。
　`RULES.md` のフェイルクローズ方針に反するので採用していない。
・残る手段は **Vercel 側に管理者専用の集計 API を足して (日付, 氏名) の索引だけを返す**こと
　（推定 1.1MB → 約 50KB）。ただし
　**管理画面の承認漏れ表示が Vercel の可用性に依存する**ようになるため、
　仕様判断としてユーザーの承認が要る。**推測で実装してはならない。**

★ **確定するまで「承認漏れ 0 件」と断定してはならない。** `recordsHistoryReady()` が偽のあいだは
画面に「集計中」を必ず出す（ヘッダのバナー・未承認一覧の見出し・通知タブの単位表示）。
この表示を消してはならない。

★ スキャンは `screen==="admin"` のときだけ進む（打刻画面の通信を奪わない）。
`writePolicy==="sandbox"`（スタッフテスト画面）では走らせない。

### 承認漏れ・打刻漏れの集計は (日付, 氏名) 索引で引く

`getNotificationCounts()` は、未承認の組ごとに `records` 全件を `filter` して
その日の打刻をかき集めていた。**O(未承認の組数 × 全件)** であり、
全期間を取得したあと（＝この画面の通常状態）は 1回の描画で数百万回の走査になる。
10秒ポーリングのたびに再描画されるため常時効き、打刻件数が増えるほど悪化する。

走査 1回で `(日付, 氏名) → 打刻[]` の索引を作ってから引く方式へ変えた
（合成データ 7,977件・全件未承認の最悪ケースで 20回あたり 1,428ms → 670ms）。

★ **索引に積む順序は `records` の並び順のままにする。**
`validateAttendanceRecord()` は `sr.find()` で最初の `clockIn` / `clockOut` を採るため、
並び順を変えると**打刻漏れの判定理由が変わる**。

★ **索引のキーは `日付 + "\u0001" + 氏名` とする。** 承認キー（`日付__氏名`）を流用しない
（氏名に `__` を含む値が来ても組を取り違えないため）。

あわせて、同じ関数に残っていた次の 2つの無駄も外した（**どちらも件数を変えない**）。

1. **`notifyIds` の全走査を廃止した。** 対象（`!deleted` かつ `isPastDate`）・重複排除キー
　（`date+"__"+staff`）・除外条件（`approvals[key]`）が `getUnapprovedSummary()` と 1文字も違わず、
　**同じ集合を数え直していただけ**だった。`getUnapprovedSummary().total` を再利用する
　（`records` の全走査が描画あたり 1回減る）。
2. **索引を直近30日窓に限った。** 引かれるのは窓を通った (日付, 氏名) だけで、
　`sr` は必ず同じ日付の行しか含まないため、窓で絞っても中身は変わらない。

★ **「同じ値だから再利用しているだけ」であって、数え方を変えたのではない。**
`scripts/test-unapproved-summary.js` が
・素朴版（`records` 全件 filter）との完全一致（8パターン）
・変更前の `notifyIds` 全走査との**恒等性**（8パターン）
・通知合計 ＝ 未承認 ＋ 修正申請 ＋ 有給
・30日より古い打刻があっても打刻漏れ判定が変わらないこと
・30日より古い未承認は「未承認」に数え続けること（**対象期間を狭めていない**）
を固定している。ここを緩めてはならない。

### 未取得の期間を「0件」と誤認させない（フェイルクローズ）

範囲取得にしたことで「まだ読んでいない期間」が生じる。次の判定は取得済みを確認してから行う。

| 判定・書き込み | ガード |
|---|---|
| 有給付与日数の自動算出（`getAttendanceDaysForMonth`） | `recordsMonthKnown(ym)`。未取得の月は `src:"none"`（データ不足）にする。**取込値（`monthlyDaysImport`）へ落とすと付与日数が黙って変わる**。★ **この判定は `cnt>0`（その月の打刻件数）より先に行う**。月が「部分的に」取得済みのときに半分の日数を `src:"punch"`（確信値）として返さないため |
| 日付が変わったとき（`refreshRecordsIfDayChanged`） | `_recIvs=[]` と `_recFull=false` の**両方**を戻す。片方だけだと、前日に立った `_recFull` が翼日も真のまま残り、当日分しか取っていない新しい月を「全期間取得済み」と誤認する |
| 「全件承認」（未承認一覧） | `recordsHistoryReady()` が偽のあいだはボタン文言を「表示中の分を承認」に変え、確認ダイアログで件数を明示する |
| 「打刻を追加」の氏名候補 | 在籍スタッフ（`staffList`）を必ず併合する。`records` だけだと「その月に打刻が無い在籍スタッフ」が候補から消える |
| スタッフ改名（全期間の `r.id` を PATCH） | `recordsHistoryReady()`。偽なら実行せず案内を出す |
| 移動距離の自動集計の警告表示 | `recordsMonthKnown(mileage.adminYm / mileage.myMonth)` |
| `mealSelectAttendance()`（賄いの出勤日全選択） | `recordsRangeCovers()`（当月） |
| `saveApprovals()`（`tc5_approvals` の全体PUT） | `approvalsLoaded` |
| `mdSaveCell()`（月別出勤日数） | `monthlyDaysLoaded` |
| `mealToggleDate()` / `mealSelectAttendance()` | `mealsLoaded` |

★ `recordsMonthKnown()` の判定順を変えてはならない。
　①`_recFull` → ②未来月 → ③**実際に取得済み（`recordsRangeCovers`）** → ④運用開始月が未確定なら false → ⑤運用開始前なら true。
　③を後ろへ回すと、管理画面を一度も開いていない端末（＝打刻端末）では `ensureRecordsOldest()` が走らず、
　移動距離カードの「打刻データを読み込めていません」が**常時点灯**して本当の未取得を検知できなくなる。
　運用開始月より前と未来月は「打刻が存在しない月」として真を返す。
これを外すと、入社が運用開始より前のスタッフの付与日数が従来と変わる。

### 変えてはならないこと

★ **日付の基準を `fmtDateKey()`（端末ローカル日付）から変えてはならない。**
打刻を書くとき（`_execPunch` の `nr.date`）と画面が絞り込むとき（`r.date===dk`）が同じ関数なので、
取得範囲と表示範囲が食い違わない。ここに `getTodayJSTStr()` を混ぜると、端末TZがJSTでない場合に
「画面が探す日付」と「取ってきた日付」がずれて打刻が消える。

★ **`ensureRecordsRange` / `ensureRecordsHistory` は `render()` から呼ばれる。**
取得済みなら通信しない・失敗時は間隔をあけて再試行する、というガードを外すと
通信断のあいだリクエストを撃ち続ける。

★ **サーバから `tc5_records` を取り直す箇所では必ず `mergeRecordsRange()` を通す。**
`records=arr` の全置換をしてはならない（未送信打刻が消え、`_recIvs` とも整合しなくなる）。
`mergeRecordsRange` が `punchOutboxMergeInto()` を呼ぶことで未送信打刻を守っている。

★ **サーバ側（`api/_lib/mileage-punch.js`）は従来どおり `tc5_records` を全件取得する。**
月次確定・確認のときだけなので変更していない。クライアントが対象月だけを取得しても、
サーバも `d.slice(0,7)!==ym` で同じ月に絞っているため**計算結果は一致する**。

★ **`orderBy="$key"` + `startAt` で範囲を絞る最適化を採用してはならない（2026-08-14 実測で否定）。**
本番の実データは ID が3系統混在し、**辞書順が時系列順にならない**（3,869件中1,285件が順序逆転）。
`startAt` で絞ると大量のレコードが黙って欠落する。範囲取得は必ず `date` で行う。

---

## index.html（app shell）の Cache Storage キャッシュ（2026-08-28）

**再訪問のたびに index.html（gzip 約231KB）を丸ごと再ダウンロードしていたのをやめた。**

### 変更前の問題（実測）

`sw.js` の navigate ハンドラが `fetch(new Request(req,{cache:'no-store'}))` で
**毎回 HTTP キャッシュをバイパス**していたため、リロードのたびに 236,171 B を取得していた。
GitHub Pages は `ETag: W/"…"` と `Cache-Control: max-age=600` を返しているが、この方式では活きない。

### 変更後の方式（stale-while-revalidate）

1. ナビゲーションは **Cache Storage から即座に返す**（ネットワーク転送 0）。
2. 裏で **HEAD** により配信中の `ETag`（無ければ `Last-Modified`）を確認する。
3. 変わっていたら本体を GET してキャッシュを入れ替え、`postMessage({type:'APP_UPDATE_AVAILABLE'})` で画面へ通知する。
4. 画面は**既存の更新バナー**（「新しいバージョンがあります。再読み込みしてください。」）を出す。
   **自動リロードはしない**（打刻・入力中に画面を奪わない）。

### ローカル実測（GitHub Pages と同じヘッダを返す検証サーバ）

| | 変更前 | 変更後 |
|---|--:|--:|
| 初回訪問の index.html 転送 | 236,171 B | 240,795 B ×2（プリフェッチぶん）＝ 481,590 B |
| **2回目以降の index.html 転送** | **236,171 B** | **0 B**（`deliveryType: "cache-storage"`） |
| 2回目の `domInteractive` | 108 ms | **56 ms** |
| 2回目の `loadEventEnd` | 594 ms | **109 ms** |
| 2回目のサーバ側リクエスト | GET 236,171 B | HEAD ×2（0 B）＋ `sw.js` 304 |

初回だけ二重取得になるのは、**Chromium ではナビゲーションの応答を Service Worker の `fetch` から
HTTP キャッシュ経由で再利用できない**ため（`default` / `force-cache` のいずれでもネットワークへ行くことを実測で確認）。
**2回訪問した時点で合計転送量はプリフェッチしない場合と同じ**になり、初回直後からオフライン起動できる利点が残る。

### 「速くなったが最新版に更新されない」を防いでいる仕組み

★ **`_appVersionTag` の HEAD 監視だけでは不十分になった。**
起動時に HEAD で取る ETag は常に「配信中の最新版」であり、**いま動いている HTML の版とは限らない**
（Cache Storage から古い版を返している可能性がある）。
そのため「古い HTML を実行中」を検知できるのは **Service Worker だけ**であり、
`_swSaysStale` フラグ（SW からの `APP_UPDATE_AVAILABLE` を受けて立つ）が唯一の経路になる。
**このフラグと `checkAppVersion()` の先頭の判定を外してはならない。**

多重の安全弁:

・**ナビゲーションのたびに HEAD で版を確認する**（1リクエスト・0バイト）
・オンライン復帰・タブ復帰で `CHECK_APP_UPDATE` を SW へ送る
・**版が取得できないときは必ず本体を GET する**（「取れない＝更新なし」と扱わない）
・`sw.js` の `CACHE_NAME` を上げれば `activate` で旧キャッシュを全削除する（最後の安全弁）

### 通知の取りこぼし対策（`__served_version`）

キャッシュを入れ替えたあとに再確認しても、比較相手がキャッシュ（すでに新版）だと差が無いことになる。
そのため **「最後に画面へ返した版」を Cache Storage の `\`/timecard/__served_version\`` へ残し**、
`CHECK_APP_UPDATE` ではそちらと比べる。これが無いと、postMessage を取りこぼしたタブは
**二度と更新に気づけない**（打刻端末は開きっぱなしなので1セッション＝数日になりうる）。
保存するのは ETag 文字列だけで、個人情報は含まない。

★ **ETag の弱い印（`W/`）は比較前に外す**（`normVersion`）。
GitHub Pages は同じ内容でも `Accept-Encoding` によって印が付き外れする
（2026-08-28 実測: gzip なら `W/"6a9102ae-d926d"`、identity なら `"6a9102ae-d926d"`）。
**HEAD と GET の ETag は同じ `Accept-Encoding` なら一致することを実測済み**。

★ **HEAD が `!ok` のときは必ず本体を GET して確かめる**。405 を返す中継プロキシのある経路で
「何もしない」とすると、その端末は二度と更新に気づけない。

### 「後で」を無効化しない

★ Service Worker は**ナビゲーションのたび・画面復帰のたび**に通知しうる。
そのため `_appUpdateDismissedAt` を見て、「後で」を押してから 5分は再表示しない。
`checkAppVersion()` の `_swSaysStale` 判定も**間隔ゲートのあと**に置く。
この2箇所のどちらかをゲートの前へ戻すと、打刻端末では画面復帰のたびにバナーが戻り、
実質「後で」が効かなくなる（実機で確認済み）。

### キャッシュしてよいもの／絶対にしないもの

★ **Cache Storage へ入れるのは配信物だけである。**
`cache.put` を呼ぶのは app shell の1箇所だけ、`cache.addAll` は `OFFLINE_URLS` の1箇所だけ、という
**ホワイトリスト方式**にしてある（除外リスト方式にすると、新しい API を足したときに黙って漏れる）。

・Firebase Realtime Database（勤怠データ）・Identity Toolkit / securetoken（認証）・
　`timecard-rho.vercel.app/api/*`（通知・移動距離）は **一切キャッシュしない**
・`OFFLINE_URLS` は同一オリジンの静的アセットのみ（`manifest.json` とアイコン3種）。**HTML を入れない**
・**キャッシュキーは `/timecard/` に正規化する**。`?admin=` / `?token=` / `?demo=` などのクエリは
　キーに含めない（トークンがキャッシュに残らない／クエリごとにエントリが増殖しない）。
　権限判定は従来どおり実行時に `location.search` で行うため、正規化しても挙動は変わらない
・`manual.html` など app shell 以外のナビゲーションは**素通し**する（index.html を返してはならない）
・`ok` でない応答（404・エラーHTML）は保存しない

### 打刻データとの責務分離

打刻の端末保存は **IndexedDB（`timecard_punch_outbox`）** のままで、Cache Storage とは無関係。
`sw.js` は打刻データに一切触れない。

### 変更時の注意

★ **`sw.js` を変更したら必ず `CACHE_NAME` を上げる**（`timecard-vN`）。
これが「配信済みの古い app shell を確実に捨てる」唯一の安全弁になる。

### 既知の制限（未解消・運用で認識しておくこと）

1. **管理画面のセッション合計の転送量は減っていない。**
   承認漏れバナーは全タブ共通のヘッダにあるため、`ensureRecordsHistory()` はタブに関係なく走る。
   入口で待たされる量は 1,038,792B → 230,245B になったが、開いたままにすれば合計はほぼ同じ。
   ★ 2026-09-09 に**完了までの時間**は 14.3秒 → 2.5秒（シミュレーション実測）へ短縮したが、
   **転送量そのものは変えていない**（上記「通信量は減っていない（減らせなかった理由）」）。
   ★ これを減らすには「承認漏れの対象期間を限る」という**業務判断**か、
   Vercel 側の集計 API の追加が要る。推測で変えてはならない。
2. **月別出勤日数タブは選択年を1リクエストで取る。** 現在は5か月分だが、
   12か月分になると約2.8MB の単一リクエストへ成長する。その時点で月単位分割へ変えること。
3. **`tc5_approvals` は10秒ポーリングで毎回全件（54,471B）取得している**（今回の対象外）。
   現在のポーリング転送量の約56%を占める最大の残存コスト。
   ★ **読み取りだけを月に絞ってはならない。** `saveApprovals()` がノード全体 PUT なので、
   読みだけ絞ると承認1件で**他月の承認が全消滅する**。順序は必ず
   ①`saveApprovals` を per-key 書き込みへ → ②読み取りを月範囲へ。
4. **分割取得のあいだ、月ごとに `records` 全件を localStorage へ書き戻す**（月数回）。
   実測 6.4ms/回（デスクトップ・4,267件）。低速端末では体感しうる。
5. **打刻件数が現在の約4〜5倍（約5MB）で localStorage のクォータに達する**。
   `_lsSet` は無言で失敗するため、その時点で端末キャッシュの保持方式を見直すこと。
6. **初回訪問だけ index.html を二重取得する**（上記の実測表）。2回訪問した時点で相殺される。

### テスト

`node scripts/test-records-range.js`（依存パッケージなし・送信なし・本番データ非アクセス）。
期間取得と app shell キャッシュの両方を固定している。
全期間スキャンについては「同時取得が `HISTORY_CONCURRENCY` を超えない」
「背景の同時取得は変更前（1本）を超えない」「新しい月から順に発行する」
「完了のたびに次の月へ連鎖する（見張りタイマーを待たない）」「表示用の取得が飛行中なら始めない」
「表示用が停滞したら過去分を再開する」「取得失敗を連鎖で撃ち続けない」
「過去分がハングしても停滞した月そのものを取り直せる／その間は確定しない」
「背景が取得中の月を表示用が二重に取りに行かない／単日要求は待たせない」
「追い越された古い応答が新しいデータを巻き戻さない」を固定する。

承認漏れ・打刻漏れの**判定結果**は `node scripts/test-unapproved-summary.js` が固定する。
**`tc5_records` の取得範囲・管理画面の入口・月/年セレクタ・`sw.js` に関係する変更では実行必須**
（全件 PASS / 0 FAIL でなければ出荷しない）。テスト件数は増減するため固定値を規範にしない。

---

## 打刻の端末永続保存と自動再送（2026-08-26）

打刻は**押した瞬間に端末（IndexedDB）へ保存し、その保存成功を確認してから**画面を「打刻済み」にする。
サーバ送信は非同期で、結果を待たない。実装は `index.html` の
`// ===== PUNCH-OUTBOX-BEGIN =====` 〜 `END` ブロックと `_execPunch`。

### 経路

```
打刻ボタン → doPunch()（既存の重複・順序チェック。_punchSaving 中は受け付けない）
           → _execPunch()  ※async
                ├ eventId 採番・実打刻時刻の確定（nr.timestamp/time/date）
                ├ await punchOutboxCommit(nr)      … IndexedDB へ保存
                │    └ false なら「打刻を保存できませんでした」＝打刻失敗。records に積まない
                ├ records.push(nr) / localStorage "tc5_records"（従来の打刻キャッシュ）
                ├ punchMsg / render()              … ここで画面が「打刻済み」
                └ punchOutboxFlush("punch")        … 送信。結果を待たない
```

★ **この3つの順序を崩してはならない。**

1. **サーバ応答を待って画面を更新してはならない。** 通信速度が打刻の体感時間に出る。
2. **IndexedDB への保存完了前に画面だけ打刻済みにしてはならない。**
   端末にもサーバにも無い打刻を「済んだ」と見せることになる。
3. **IndexedDB へ保存できなかったときは打刻失敗として扱う。**
   localStorage やサーバ直送へフォールバックしない（整合性を優先する）。
   `_execPunch` の `if(!_saved){…return;}` を消してはならない。

管理者の修正・追加（`saveRecord` / `patchRecord`）は従来どおりで、キューを通さない
（既にサーバにあるノードを編集する操作なので、存在確認つき再送とは意味が違う）。

### 保存構造（IndexedDB）

| 項目 | 値 |
|---|---|
| DB 名 | `timecard_punch_outbox`（version 1） |
| ストア | `punches`（`keyPath: "eventId"`） |
| エントリ | `{eventId, rec, state, createdAt, retryCount, lastAttemptAt, lastError}` |
| `state` | `"pending"`（送信待ち）/ `"syncing"`（送信中） |
| 送信成功時 | エントリを削除する（サーバに正本があるため端末に残さない＝ストアを小さく保つ） |

・`rec` は `tc5_records` へ書くレコードそのもの。
・**`localStorage` はキューの保存先として使わない。** 旧実装（2026-08-26 初版）が使っていた
　`tc5_punch_outbox` キーは、起動時に読み込んで IndexedDB へ移送したうえで削除する（移行処理）。
　`tc5_records` の端末キャッシュは従来どおりのキーを使う（本変更では触っていない）。
　★ ただし 2026-08-28 以降、中身は**全件ではなく取得済み区間分だけ**である（`mergeRecordsRange`）。
・**送信前に `state:"syncing"` を永続化する。** 送信中にアプリが終了しても、
　起動時に `pending` へ戻して再送できる（存在確認つきなので二重登録しない）。

### 二重登録が起きない理由

送信先は必ず `/tc5_records/{eventId}.json` への **PUT**。`eventId` は打刻時に端末が採番した
一意キーで、RTDB は「パスが主キー」なので、同じ `eventId` を何度 PUT しても1ノードを
上書きするだけで行は増えない。**これがサーバ側の一意制約の実体**であり、
PostgreSQL の `PRIMARY KEY` + `INSERT ... ON CONFLICT DO UPDATE` と同じ効果を構造が与える。

★ **`POST`（push）でサーバに採番させてはならない。** 再送のたびに別ノードができて支給に直結する。
★ **再送時に `eventId` を採り直してはならない。**

2回目以降の送信は **GET で存在確認してから書く**。これにより
「サーバには保存されたがクライアントが成功応答を受け取れなかった」打刻を再送しても、
上書きせず完了扱いにする（**管理者がその間に修正していても巻き戻さない**）。
★ **初回送信では存在確認しない**（毎回の打刻で通信を2倍にしないため）。
`punchOutboxFlush` が `retryCount` を増やす前に `isRetry` を判定して渡している。

`database.rules.json` は変更していない（変更は人間の確認が必要）。

### 時刻

| フィールド | 意味 |
|---|---|
| `timestamp` / `time` / `date` | **実際に打刻ボタンを押した時刻（端末時刻）。正本。再送しても変えない** |
| `serverReceivedAt` | サーバ（RTDB）が受け取った時刻。`{".sv":"timestamp"}` でサーバ側が入れる |
| `eventId` | 打刻イベントの一意キー（= `id` = 保存先ノード名） |

8:03 打刻 → 10:15 送信でも、正式な打刻時刻は **8:03**。
`tc5_records` に `created_at` 相当は無いため、サーバ受信時刻は `serverReceivedAt` を新設して持つ。
**追加フィールドはこの2つだけ**（`eventId` / `serverReceivedAt`）。既存行は両方とも持たない＝許容。
`serverReceivedAt` を含む PUT が 4xx で拒否された場合だけ、そのフィールドを外して1回だけ書き直す
（サーバ受信時刻は付加情報であり、打刻本体を落としてまで守らない）。

### 再送

契機: **打刻直後 / アプリ起動時・ページロード時 / `online` / `visibilitychange`（復帰）/ `pageshow` /
打刻画面の表示 / 10秒ポーリング**。

・間隔は 5s → 10s → 20s → 40s → 80s → 160s → 300s（上限）。
・**認証エラー（401/403）は最短60秒・上限10分**。叩いても通らないため通常より長く待つ。
　401/403 を成功扱いにせず、打刻も消さない。トークンは既存の更新周期で自動的に更新される。
・**通信復旧・起動・画面復帰はバックオフを待たない**（最低3秒だけあける）。
・**多重送信しない**: グローバルの実行中フラグ（`_punchOutboxFlushing`）＋各エントリの `"syncing"` 状態。
　`punch` / `online` / `visible` / `startup` が同時発火しても PUT は1回。
・通信断が出た時点でその回は打ち切る（順序維持・無駄な再試行の抑制）。
・**送信順は実打刻時刻の昇順**。オフライン中に 出勤→休憩開始→休憩終了→退勤 と打っても、この順で送る。

### 警告のしきい値と、その値にした理由

**最古の未送信が10分以上経過、または retryCount が8以上**のときだけ、
打刻画面とスタッフ選択画面に「⚠ 未送信の打刻があります（N件）」を出す。
通常の一時的な通信断では**何も表示しない**（送信中・送信待ち・同期中の常時表示はしない）。

根拠（2026-08-26 に本番実測した値から決めた。機械的に選んでいない）:

| 実測 | 値 |
|---|---|
| `tc5_records` 件数 | 4,188 件（2026-04-01 〜 2026-08-26） |
| 1日あたりの打刻 | 約 28 件（147日で 4,188 件） |
| 打刻あたりの端末滞在時間 | 約1.5秒（`punchTimer` で打刻後1.5秒でスタッフ選択画面へ戻る） |

・打刻した本人は**1.5秒後には画面を離れる**ため、数分後に出す警告は本人には届かない。
　警告の受け手は「次にその端末を使う人」と管理者であり、**早く出しても誰の役にも立たない**。
・一方で誤警報の代償は大きい（職員が管理者へ連絡する）。
　現場で起きる一時的な断は Wi-Fi ローミング（〜30秒）・ルータ再起動（1〜3分）で、
　**3分を超える断は「一時的ではない」**と判断できる。10分はその倍以上の余裕を取った値。
・1日28件・25人の運用では、実際の障害1回につき警告は1回出れば足りる。
　打刻ごとに出るような短いしきい値にすると、通信が不安定な日は終日警告が出続ける。
・`retryCount>=8` は時計がずれている端末向けの保険（バックオフ上、8回目は約10分に相当）。

**警告が出ていても打刻はできる**（バナー表示のみ。ボタンは止めない）。

### 端末に保存できないとき

`punchOutboxCommit` が false を返す＝打刻失敗。「打刻を保存できませんでした」を出し、
`records` にも積まない（＝画面は打刻前のまま。利用者はもう一度押せる）。
IndexedDB が開けない端末では、打刻画面とスタッフ選択画面に
**「⚠ この端末では打刻を保存できません」**を先出しする（押してから初めて気づく事態を避ける）。

★ この設計は「端末に保存できない端末では打刻できない」ことを意味する。
　プライベートブラウジング等で IndexedDB が使えない場合が該当する。
　**localStorage へのフォールバックは意図的に持たない**（要件どおり整合性を優先）。

### 未送信打刻を消さないための約束

サーバから `tc5_records` を取り直す箇所では、必ず `punchOutboxMergeInto(arr)` を通す。
通さないと**サーバ応答で `records` を差し替えた瞬間に未送信打刻が画面から消える**。
現在の直接呼び出しは2つ（起動時 `initFirebase`／`mergeRecordsRange()` の中）。
10秒ポーリングと朝出勤確認の「最新状態に更新」は `mergeRecordsRange()` 経由で間接的に通っている。
**`tc5_records` の再取得を新しく足すときは、同じ処理を必ず入れること。**
`punchOutboxMergeInto` は**サーバ側に同じ `eventId` があれば触らない**
（ローカルの古い内容でサーバの正本＝管理者修正後の値を上書きしない）。

### スタッフテスト画面・管理者デモ・閲覧用URLでは動かさない

`punchOutboxEnabled()` は `writePolicy==="full" && !viewerMode` のときだけ真。
テスト打刻を端末へ残して本番へ再送しないための境界であり、**緩めてはならない**。
無効時は従来どおり `saveRecord()` へ委譲する（sandbox の擬似成功・デモの遮断がそのまま働く）。

### 起動が通信断で止まらないようにした変更

・`initFirebase` 冒頭の `await getAuthToken()` を try/catch で包んだ。
　以前は通信断で例外になり `firebaseError=true` → **「通信エラーです」画面でスタッフを選べず、打刻自体ができなかった**。
・上記で通信断時もスタッフ選択へ到達できるため、`staffPinsLoaded` を追加した。
　`tc5_pins` を**取得できていない状態では「PIN未登録」と断定できない**ため、PIN新規登録へ進めず案内だけ出す。
　★ これが無いと、PIN未取得のまま新規登録して `staffPins={本人だけ}` を `/tc5_pins` へ全置換PUTし、
　**他スタッフのPINを全消しする**（`saveData("tc5_pins", …)` は全体PUT）。この経路は変更前から存在した。
・`tc5_pins` の取得は `r.ok` を確認する（「取得成功して不在」と「取得失敗」を区別する）。

### 既知の制限（未解消・運用で認識しておくこと）

1. **完全オフラインでの「アプリ再起動後の新規ログイン」はできない。**
   PIN照合材料（`tc5_pins`）は仕様上 localStorage へ保存しない。4桁PINのハッシュは総当たりで復元できるため、
   端末保存は認可の後退になる。オフライン時は案内を出して止める。
   **すでにログイン済み（打刻画面を開いたまま）の打刻・ページ更新後の未送信保持・復旧後の自動送信は動作する。**
2. **端末時刻がずれていると打刻時刻もずれる**（変更前から同じ）。
   `serverReceivedAt` と突き合わせれば事後に検出できる（`scripts/verify-punch-events.js` の判定4）。
3. **未送信のまま端末を初期化・ブラウザデータを削除すると打刻は失われる。** 端末保存の性質上避けられない。
   警告表示が唯一の検知手段なので、**しきい値を緩める変更をしてはならない**。
4. **IndexedDB が使えない端末では打刻できない**（上記「端末に保存できないとき」）。

### DB 変更・rollback・verify

・**migration ファイルは無い**（Firebase RTDB はスキーマレス。Type C）。
　増えるのは新規打刻に付く追加フィールド2つ（`eventId` / `serverReceivedAt`）だけで、
　既存行への一括更新は**不要かつ行ってはならない**。
・**rollback**: コードを revert するだけでよい。既に書かれた `eventId` / `serverReceivedAt` は
　既存の集計・修正・監査が参照しないため残っていても無害。データの巻き戻しは不要。
・**verify**: `node scripts/verify-punch-events.js`（読み取り専用）。
　`FIREBASE_API_KEY` / `FIREBASE_DATABASE_URL` を環境変数で渡す。
　ノード名と `id` の一致・`eventId` の重複・サーバ受信時刻の妥当性を確認する。
　★ `tc5_records` には**旧実装（全件を配列で PUT していた時代）の行がキー＝配列添字で 1,727 件**残っている。
　これは既知の既存データで、verify は判定対象から除外する（2026-08-26 実測）。

### テスト

`node scripts/test-punch-outbox.js`（依存パッケージなし・送信なし・本番データ非アクセス）。
**打刻に関係する変更では実行必須**（全件 PASS / 0 FAIL でなければ出荷しない）。
テスト件数は増減するため固定値を規範にしない。

---

## 管理者による勤怠編集（未打刻の日の新規作成・2026-09-07）

管理者は**出勤・退勤の打刻が1件も無い日でも**、時刻セルをクリックして入力し、
勤怠レコードを作成できる（スタッフ別詳細＝月別の職員別勤怠画面／勤怠一覧＝日別画面のどちらでも）。

打刻レコードは**1打刻＝1ノード**（`/tc5_records/{id}`）で、打刻が無い日はレコードが存在しない。
したがって未打刻の保存は既存レコードの更新ではなく**新規作成**になる（`index.html` の `save()` の
`else` 枝が `generateRecordId()` で採番して PUT する）。この経路は変更前から存在した。

★ 旧実装ではスタッフ別詳細だけが `hasData`（その日の打刻が1件以上）を編集セルの描画条件にしていたため、
打刻ゼロの日は編集できなかった。2026-09-07 にこの条件を外した。

### 未取得の月では未打刻セルを編集させない（★外してはならない）

スタッフ別詳細は `mRecsStaff`＝**メモリ上の `records`** しか見ておらず、`ensureAdminRecordRanges()` は
非同期で取りに行くだけで描画を待たない。したがって月を切り替えた直後・通信が遅い端末では、
**サーバに打刻がある日も「−」に見える**。

そこで入力すると、`save()` の重複防止（`records.find(...)`）が**メモリ検索なので空振り**し、
同じ日に2つ目の `clockIn` ノードができる。`calcMs()` は時系列走査で `cin` を後勝ちに上書きするため、
**実働時間＝賃金の基礎が変わる**（承認漏れ集計・月別出勤日数・有給付与日数の自動算出にも波及）。

★ 特に危険なのは**当月**である。起動時に取得済みなのは当日だけ（`loadPunchRecords()`）なので、
月半ばに当月を開くと「当日だけ打刻あり・それ以前は未打刻」という**もっともらしい画面**になり、
過去月の全日空欄と違って気づけない。

そのため `var monthKnown=recordsMonthKnown(selMonth);` を判定し、偽のあいだは

・未打刻セル（`rec` なし）を**非編集の「−」に落とす**
・テーブル上部に赤バナーを出す（`AGENTS.md`「未取得の期間を『0件』と誤認させない」に従う）

打刻済みセル（`rec` あり）は `data-id` を持ち上書き更新になるため、未取得でも編集してよい。

### 表示（勤怠一覧と表現をそろえる）

以下は**スタッフ別詳細の中での規約**である。勤怠一覧（日別）は休憩ペアが0件のときに
下線の無い `--:-- → --:--` を非編集で出すため、`−`＝入力できない、は画面をまたいでは成立しない。
判別の実体は**下線の有無と `time-cell` クラスの有無**である。

| 表示 | 意味 |
|---|---|
| `08:30` ＋下線 | 打刻あり。クリックで修正できる |
| `--:--` ＋下線 | 未打刻。**クリックで入力できる** |
| `−`（下線なし） | **入力できない**（承認済みの日／この月が未取得／当日より後の日） |

★ 表示文字は `--:--` か `−` のどちらかにすること。`attachTimeCellHandlers()` は
`if(cur==="--:--"||cur==="−")cur="";` でこの2つだけを空欄として扱う。他の文字にすると既存値として拾われる。

★ 勤怠一覧（日別）の未打刻セルと同じ表現（`--:--` ＋実線）にそろえてある。
この画面だけ別の表現（破線・`min-width` など）にしてはならない。同じ操作が2画面で別に見える。

★ 休憩列は td 側の `hasData?brkDisplay:'<span>−</span>'` を `brkDisplay` へ単純化した。
編集可否は `dBrkPairs.length>0` のままなので**実挙動は変わらない**（打刻ゼロの日は非編集の `−`）。
打刻ゼロの日の休憩セルの見た目が、打刻はあるが休憩が無い日と揃うだけである。

### 当日より後の日は編集させない（★外してはならない）

`recMonthEnd()` は**当月の取得上限を当日で丸める**。したがって当月の「明日〜月末」は
どの経路でも取得されず、未来月は `recordsMonthKnown()` が無条件で真を返す。
つまり `monthKnown` が真でも、**当日より後の日は「未取得」である**。

端末時計が進んだ端末の打刻など未来日付のレコードは実在しうるため、ここを開けると
上と同じ二重ノードができる。そこでガードは `if(!rec&&(!monthKnown||dk>recToday()))` とし、
**当日より後の未打刻セルも非編集の `−`** にしている。
打刻済みセルは `data-id` を持つため未来日でも編集できる（上書き更新なので重複しない）。

★ 勤怠一覧（日別）には `selDate` に対する同種のガードが無く、取得完了前や未来日で
同じ二重登録が成立しうる（本変更より前からの既存の穴。今回は触っていない）。

### 既知の制限（未解消・運用で認識しておくこと）

1. **管理者が作成した出勤打刻は `workFacility` を持たない。**
   `index.html` の新規レコード生成は `{id, staff, type, date, time, timestamp, manualAdd, editedByAdmin, ...}` だけで、
   施設を入れない（管理者が入力していない値を推測で埋めないため）。結果として
   ・`calcMsByFacility()` の施設別按分・応援集計で施設が空になる
   ・移動距離の自動集計で**起点未確定（`no_origin`）**になる
   　（★ **その日に施設変更（応援）打刻がある場合に限る**。`mileageAutoDayRoute` は
   　`if(!b.hasMove)return out;` で、移動が無い日は `status:"none"` として確定を止めない）
   ★ **止まるのは「その職員」ではなく「その月」全体である。**
   `closeMonth` は年月単位で全職員を計算し、`comp.unresolved > 0` なら 409 `unresolved` を返す
   （`api/mileage.js`）。**1職員・1日の要確認で、その月の確定が全職員ぶん止まる。**
   「その人だけ後回しにする」ことはできない。
   復旧は「打刻に無い移動の手入力（例外）」を職員が登録し管理者が承認する経路だけである
   （打刻修正申請の承認では `workFacility` が入らないため解消しない。移動距離の節を参照）。
   ★ **管理者は代理で手入力を作成できない。** `ACTIONS.saveRequest` は `["s"]`（職員ロールのみ）
   なので、打刻を作った管理者が自分で後始末できず、必ず当該職員のログインが要る。
   ★ 既存レコードの `workFacility` を後から設定できる管理UIは存在しない。
   これは日別画面からの作成でも同じ既存挙動だが、月内全日ぶんの導線が増えたぶん発生しやすい。

2. **休憩は時刻セルからは新規作成できない。**
   スタッフ別詳細・日別一覧のどちらも `dBrkPairs.length>0` / `brkPairs.length>0` のときしか
   編集セルを描画しない。休憩打刻が1件も無い日に休憩を足すには、
   管理画面の**「➕ 打刻を追加」フォーム**（`breakStart` / `breakEnd` を選べる）を使う。
   1組できれば以後は時刻セルから編集できる。
   ★ `calcMs()` は既定の休憩控除を持たず、実在する `breakStart`/`breakEnd` の差分しか引かない。
   打刻忘れの日に 08:30〜17:30 だけを入れると**実働 9:00（休憩0分）**で確定する。
   休憩の入力漏れに注意すること。**既定の休憩控除を実装で足してはならない**（既存の計算ルールを変えない）。

3. **管理者デモ（`?demo=`）ではこの経路に到達できない。**
   `_applyDemoUiLock()` が全 `.time-cell` に `pointer-events:none` を付け、
   「➕ 打刻を追加」も `DEMO_BLOCK_SELECTORS` の `#btn-add-rec` で `disabled` になる。
   なおコード上は `records.push(nr)` が `saveRecord()` の `demoWriteBlocked()` より前にあるため、
   将来この UI ロックを外すと幻のレコードが次のポーリングまで画面に残る（`_lsSet` には到達しない）。

4. 承認済み（`approvals[dk+"__"+nm]`）の日は従来どおり読み取り専用。編集するには先に引き戻す。

5. **未取得の月では、サマリー（出勤日数・総実働時間・残業時間）と「一括承認」が
   取得済みの日だけを対象にする**（本変更より前からの既存挙動。`totalMs` 等はメモリ上の
   `records` から計算し、一括承認も `monthKnown` を見ていない）。
   誤読を防ぐため赤バナーを**サマリーバーの上**へ置き、集計値がまだ確定でないことを明記している。
   ★ この順序を入れ替えてはならない。数字を先に読ませると「出勤日数1日」を確定値と受け取る。

### テスト

`node scripts/test-admin-attendance-edit.js`（依存パッケージなし・送信なし・本番データ非アクセス）。
**管理者の勤怠編集・時刻セルの描画条件・`attachTimeCellHandlers` の保存処理に関係する変更では実行必須**
（全件 PASS / 0 FAIL でなければ出荷しない）。テスト件数は増減するため固定値を規範にしない。
未打刻からの新規作成、既存の打刻・他職員・他日・削除済みレコードを壊さないこと、
未取得の月のフェイルクローズ、承認済み日が読み取り専用のままであること、
休憩セルが打刻ゼロの日に編集可にならないことを固定する。

---

## 打刻時の顔撮影（スタッフ別ON/OFF・2026-09-12）

スタッフ管理モーダルの「打刻時の顔撮影」をONにしたスタッフだけ、**出勤・退勤**の打刻時に
インカメラで1枚撮影する。実装は `index.html` の `// ===== FACECAM-BEGIN/END =====` ブロック。

### 目的は心理的な不正抑止だけである

★ **本人確認ではない。** したがって次を行わない。行える実装にもしない。

・顔認証・本人判定をしない
・写真をサーバへ送らない（`fetch` / `authFetch` を呼ばない）
・端末へ保存しない（localStorage / IndexedDB / Cache Storage へ書かない）
・**Blob も dataURL も作らない**（＝残せる形のデータを一切生成しない）
・管理画面から写真を見る導線を作らない

撮影は「キャンバスへ1枚 `drawImage` した時点」で完了し、`finally` でキャンバスを消し、
大きさを 0 にし、`video.srcObject` を外し、カメラのトラックを停止する。
`scripts/test-face-photo.js` がこの破棄と、禁止APIへ触れていないことを機械的に固定している。

### 既定は OFF（フェイルセーフ）

★ 判定は `facePhoto === true` のときだけ ON とする。
**未設定・null・`false`・文字列 `"true"`・数値 1 はすべて OFF。**

・既存スタッフは全員 OFF（`tc5_staff` に `facePhoto` を持たないため）
・新規登録もチェックを入れなければ OFF
・保存時に `true` を書くのはチェックONの分岐だけ。**勝手に ON へ移行する処理を作ってはならない**
・回帰テストが `facePhoto=true` の出現箇所を2か所に固定している（自動ONの混入を検出する）

### 打刻を止めない（★ここを崩してはならない）

呼び出し位置は `_execPunch` の中、**端末保存（IndexedDB）が成功して画面が打刻済みになった後**、
かつ **`punchOutboxFlush("punch")` より前**。`await` しない。

・カメラの起動を待って打刻を遅らせない（即時打刻を壊さない）
・権限拒否・カメラ占有・非対応でも例外を投げず、打刻はそのまま成立する
・端末保存に失敗した打刻では撮影しない（`if(!_saved) return;` が先にある）
・オフラインでも動く（通信を一切しない）

### 撮影の開示と表示（★ ここを崩してはならない）

**開示は画面の「上端」に固定ストリップで出す**（`id="facecam-strip"`）。
文言は撮影の成否で差し替える（DOM を直接書き替えるので `render()` を待たない）。

★★ **下端にも本文にも置けない。** 2026-09-12 に両方を試して却下した経緯があるので繰り返さないこと。
・**下端**は更新バナー（`bottom:0` / z-index 9998。打刻端末では誰も押さないので**出たままになる**）と
　打刻時刻に挟まれて、320px 幅（可視高 約460px）では安全な位置が存在しない。
・**本文（流し込み）**は、打刻完了ブロック自体が可視高を超えるため、未送信打刻バナー・
　お知らせ・氏名の折返しのいずれかで**開示文が画面外へ押し出される**。1.5秒で画面が戻るので
　スクロールして読むこともできない。
・**上端**なら、覆うのは「← もどる」だけである（打刻直後に押す導線ではない）。

**プレビュー（映像）は余裕があるときだけ見せる。**
・幅 264px（`max-width:calc(100vw - 24px)`）、映像 64x48 と文言を横並びにして高さ約64px
・`faceCamHasRoom()` が「**打刻時刻の行**（`id="punch-time-line"`）の下端＋12px より下に収まる」と
　判断したときだけ表示する。基準が取れないときは**出さない**（判定できないなら出さない側へ倒す）
・収まらないときは 1x1・`opacity:0.01` の箱として置く（映像を再生できないと撮影できない端末が
　あるため要素自体は残す）。視覚的には何も出ず、開示は上端のストリップが担う
・更新バナーが出ているときは、その高さぶん持ち上げる（`faceCamBottomPx()`）。
　z-index を上げる対応は不可（`pointer-events:none` なので見えないバナーのボタンが押せてしまう）
・表示は 1300ms（画面遷移 1500ms より短く）、上限 2600ms。`punchTimer` と `goHome()` からも閉じる
・撮影が固まったまま（権限ダイアログ未応答）でも、15秒で再入ガードを解く
　（解かないと、その端末では以後一切撮影されなくなる）

### 撮影しない画面

`writePolicy !== "full"`（スタッフテスト画面）・管理者デモ・閲覧用URL・
出勤退勤以外の打刻（休憩・施設変更）では撮影しない。
表示は `#app` の外（body 直下）へ置く。`render()` で消えないようにするためであり、
`pointer-events:none` で打刻画面の操作を一切ふさがない。最大3秒で必ず閉じる。

### 既知の制限（運用で認識しておくこと）

1. **`tc5_staff` は一般スタッフでも書ける。** したがって次の2つが可能である。
   `/honomi` の `.write` が `r==='s'` を許しているため（移動距離の利用ON/OFFを
   サーバ側 `/mileage/enabled` へ置いたのと同じ問題）。
   - **対象職員が自分の `facePhoto` を消して撮影を無効化できる。**
     ★ **無効化されたことを検知する手段は無い。** この機能の唯一の目的（抑止）はその職員に対して失われる。
     「フラグを消しても得られるものは無い」と書いてはならない（2026-09-12 の監査で訂正）。
   - **第三者が他人の `facePhoto` を ON にできる**（本人が知らないうちにインカメラが起動する）。
     緩和として、撮影表示に「管理者設定により撮影しています」と必ず出している。**この文言を外してはならない。**
   ★ それでも `tc5_staff` に置いているのは、**打刻をオフラインで成立させるため**である。
   サーバ側に置くと、打刻のたびに Vercel へ問い合わせないとON/OFFが分からず、
   通信断で打刻できない（または撮影の可否を推測する）ことになる。
   将来サーバ側へ移す場合も、**打刻のたびに問い合わせる形にしてはならない**
   （起動時に取得してメモリへ保持し、取得できなければ OFF とする形に限る）。
   この判断を読み替える前に、必ず打刻のオフライン要件を確認すること。
2. **IndexedDB が使えない端末では打刻自体ができない**（既存の制限）。撮影の有無は無関係。
3. **カメラを許可していない端末では撮影されない。** 打刻は成立するため、
   「撮影されなかった打刻」は存在しうる。撮影の有無を根拠に勤怠を否認してはならない。
4. **初回だけブラウザのカメラ許可ダイアログが出る。** 打刻後の画面遷移（1.5秒）をまたいで
   ダイアログが残ることがある。★ ONにする前に、管理者がその端末で1度カメラを許可しておくこと。
   許可しないまま運用すると、ONのスタッフが打刻するたびにダイアログが出る。
5. **iOS はホーム画面へ追加した PWA でも `getUserMedia` が使える**（iOS 14.3 以降）。
   ただし HTTPS 必須。本番は GitHub Pages（HTTPS）なので満たしている。

### ONにする前にやること（運用手順）

1. **対象職員へ事前に周知する。** 写真を残さない実装でも、カメラが起動し顔画像を一時的に取得する。
   本人が理由を知らないまま起動する状態を作らない（撮影表示にも「管理者設定により撮影しています」と出る）。
2. **その端末でカメラを1度許可しておく。** 許可しないままONにすると、打刻のたびに
   ブラウザの許可ダイアログが出る。
3. ONにするのはスタッフ単位。既定はOFFで、管理者が明示的に入れたときだけ有効になる。

### テスト

`node scripts/test-face-photo.js`（依存パッケージなし・送信なし・本番データ非アクセス・カメラも使わない）。
**顔撮影・スタッフ管理モーダルの当該項目・`_execPunch` の結線に関係する変更では実行必須**
（全件 PASS / 0 FAIL でなければ出荷しない）。テスト件数は増減するため固定値を規範にしない。

---

## 施設端末の持ち出し検知（2026-09-12）

施設に置いてある打刻用スマホが施設の設定範囲外へ持ち出されたとき、管理者へ LINE 通知を1回出す。

| 層 | 実装 | 役割 |
|---|---|---|
| 管理画面 | `index.html` の `// ===== DEVWATCH-BEGIN/END =====` | 監視ON/OFF・基準位置・許容半径・登録端末の表示だけ |
| 管理API | `api/device.js` | 管理者ロール専用。設定の読み書きと登録コードの発行 |
| 端末API | `api/device-report.js` | 監視アプリ専用（`register` / `report`）＋**定期実行の入口**（`sweep`） |
| 共通ロジック | `api/_lib/device.js` | 距離・継続判定・重複抑止・LINE本文・送信 |
| 監視アプリ | `devicewatch/`（Expo / iOS・Android） | 位置と権限状態を報告するだけ |
| 定期実行 | 外部スケジューラ（cron-job.org）→ `POST /api/device-report` `action:"sweep"` | **管理画面を開かなくても確定・通知させる主経路**。1分間隔 |

### 新しい管理画面は作っていない

既存「マスター管理」タブの「拠点トークン管理」の各施設行の中に、折りたたみで
「端末持ち出し監視」を足しただけである。★ **履歴画面・端末管理画面・設定画面を新設してはならない。**

### データの置き場所（★ここを動かしてはならない）

**RTDB のルート直下 `/devmon`。`database.rules.json` に未定義＝デフォルト拒否**で、
打刻端末・職員・労務士からは直接読み書きできない（`/mileage` と同じ方式）。

```
/devmon/_meta                  {createdAt, version}
/devmon/settings               {dwellSec, staleSec, defaultRadiusM}  ← UIは持たない。未設定なら既定値
/devmon/facilities/{fkey}      {name, lat, lng, radiusM, enabled, basedOn,
                                createdAt, updatedAt, updatedBy}
/devmon/enroll/{CODE}          {fkey, name, setBaseFromDevice, expiresAt, createdAt, createdBy}
                               ← 24時間で失効。期限切れは次の発行時にまとめて削除する
/devmon/devices/{deviceId}     {fkey, label, platform, tokenHash,
                                state, pendingSince, notifiedAt,
                                lastSeenAt, lastDistM, lastAccM,
                                lastJudgedAt, unjudgedNotifiedAt, staleNotifiedAt,
                                permState, permDetail, permNotifiedAt,
                                createdAt, createdAtMs, revoked, revokedAt, revokedBy}
```

★ `lastJudgedAt`（位置判定が成立した時刻）と `createdAtMs` は「判定できていない検知」の土台である。
消すと、位置を送らない端末が永久に正常扱いになる。`lastAccM` は管理画面の診断用。

★ **`/honomi` 配下や `master/locations` へ置いてはならない。** `/honomi` は打刻のために
`tc5_records` / `tc5_pins` / `master/locations` を匿名または一般スタッフへ開けてある。
そこへ監視設定を置くと、**監視対象の端末自身が「監視OFF」「基準位置＝自宅」「範囲内」と書けて、
検知そのものを無効化できる**（朝の未打刻判定の時刻を `master/locations` へ持たせないのと同じ理由）。

★ **施設の緯度・経度は既存データに存在しなかった**（`master/locations` は `{name, token}` の2キーのみ、
`/mileage/places` にも座標なし。2026-09-12 に実測）。二重管理ではなく新規に持たせている。
施設の対応づけは**施設名の正規化（NFKC＋空白除去）から導く16桁のキー**で行う。
施設マスタに改名機能が無い（改名＝削除＋追加）ため、**改名した施設は監視設定を作り直す必要がある。**

### 判定はすべてサーバ側にある

★ アプリは「位置と権限状態」を報告するだけで、持ち出し確定・重複抑止・通知の判断を持たない。
アプリを書き換えても判定を変えられないようにするため。**この分担を崩してはならない。**

★ **経過時間はサーバ受信時刻で測る。** 端末が申告した時刻（`at`）は判定に使わない
（端末時刻を進めるだけで「3分継続した」と偽れるため）。`evaluateReport` は `rep.at` を読まない。

★★ **ただし位置そのものは端末の自己申告である。** 判定をサーバへ置いた目的は
**重複通知の抑止と端末時刻の偽装対策**であって、**位置の真正性の保証ではない**。
改造クライアント・エミュレータ・OSの位置偽装で「範囲内」を偽ることはできる。
移動距離申請の `tc5_records` と同じ種類の残存リスクであり、「サーバで判定しているから安全」と
読み替えてはならない（2026-09-12 の監査で明記）。
代わりに**偽っても痕跡が残る**ようにしてある（位置を送らない・粗い測位だけを送る端末は
「位置を判定できていません」として通知される）。

| 判定 | 内容 |
|---|---|
| 範囲外 | `距離 − min(測位誤差, 100m) > 許容半径` |
| 持ち出し確定 | 最初に範囲外を受け取った時刻から **既定180秒**以上経過。**次の報告**か、サーバ側のスイープで確定する |
| 再通知 | しない。範囲内へ戻ると内部状態が戻り、次の持ち出しで再び通知する（復帰通知は出さない） |
| 粗い測位 | 誤差 300m 以上は判定に使わない。★ このとき**継続計測を消さない**（粗い測位を挟んで3分計測をリセットさせない） |
| 測位誤差の申告なし | 差し引き上限（100m）として扱い、**判定は行う**。★ 判定スキップにすると「`acc` を省略すれば検知されない」抜け道になる |
| 監視OFF・基準位置未設定 | 判定しない。推測で「範囲内」にもしない |
| 権限の喪失 | 「常に許可」でなくなったら1回だけ通知。「常に許可」へ戻ると再アーム（★ `permState` と **`permDetail` の両方**を消す。消さないと管理画面が永久に赤のままになる）。★ **通知は監視ONの施設だけ**。状態の記録は OFF でも行う（管理画面の表示を正確に保つため） |
| 受信途絶 | 既定24時間。1つの途絶につき1回だけ通知 |
| **位置を判定できていない** | 報告は届くが判定が成立しない状態が既定24時間続いたら通知。★ 下記 |

端末の状態（`/devmon/devices/*/state`）は4つ。**"inside" を既定値にしてはならない。**

| state | 意味 |
|---|---|
| `unknown` | 登録直後。位置を1度も判定していない（管理画面は「未判定」） |
| `inside` | 判定して範囲内 |
| `pending` | 判定して範囲外だが、まだ継続時間に達していない（管理画面は「範囲外を確認中」） |
| `outside` | 持ち出し確定・通知済み |

★ **「判定できていない」を正常として表示してはならない（フェイルクローズ）。**
位置を送らない・測位が粗すぎる報告は `lastSeenAt` だけを更新するので、受信途絶にはならない。
判定が成立した時刻（`lastJudgedAt`）を別に持ち、それが古い端末は
管理画面で赤く出し、しきい値を超えたら LINE で通知する。
これが無いと、**監視が死んでいるのに管理画面が緑（監視中）のまま**になり、
偽装（位置を送らない改造クライアント）も屋内の測位不良も検知できない。

★ **スイープが「端末が沈黙しても確定させる」経路である。** 範囲外を1回報告した直後に
電源を切る・機内モードにするのが持ち出し時の自然な行動であり、
次の報告を待つ設計だけでは**通知が永久に出ない**。経過はサーバ受信時刻で測っているので、
端末の追加報告は本来不要である。

スイープが走る契機は次の3つ。**判定の実装は `runSweep` だけ**で、二重実装は無い。

| # | 契機 | 位置づけ | 稼働状態（2026-09-13） |
|---|---|---|---|
| ① | 外部スケジューラ（cron-job.org）→ `POST /api/device-report` `action:"sweep"`（**1分間隔**） | **主経路**。管理画面を誰も開かなくても、範囲外3分で LINE が来るのはこれによる | 登録済み・**毎分発火している**（Vercel ログで実測）。⚠ ただし本文が JSON として読めておらず **400 `bad_json`** のため**未達**（2026-09-13） |
| ② | 端末の報告時（`SWEEP_MIN_INTERVAL_MS` で間引く） | 保険 | 稼働 |
| ③ | 管理画面を開いたとき（`/api/device` の `bootstrap`） | 保険 | 稼働 |

⚠ **①が登録されるまでは、下記「既知の制限1」の旧挙動（端末1台の施設では管理画面を開くまで
通知が出ないことがある）がそのまま残る。** 登録と初回の 200 応答を目視確認してから
「稼働」へ書き換えること（7時分の cron と同じ扱い。本書冒頭「確認できた事実のみ」に従う）。

★ **②③ を外してはならない。** ①はリポジトリ外の設定で、止まっても GitHub Actions のように
失敗が見えない（cron-job.org 側の実行履歴でしか気づけない）。朝の未打刻通知を
cron と外部スケジューラで二重化しているのと同じ考え方である。

★ **報告の機会に走らせるスイープは、1インスタンスあたり59秒に1回までへ間引く**
（`SWEEP_MIN_INTERVAL_MS`）。
★ **定期実行の間隔（60秒）の直下に置く。** 45秒だと「cron の直後15秒の窓」へ入った端末報告が
冗長な全件スイープを重ねる（実測: 7台で 720回/日・50台で 1,440回/日）。59秒なら窓が1秒に縮み、
同条件の実測で **0回/日**になる。cron 停止時も、端末の報告間隔120秒 ≥ 59秒なので保険は
全報告で走る（遅れは最大14秒増）。
★ 継続時間の最小値 `MIN_DWELL_SEC`＝60秒より必ず短く保つ。同値以上にすると、
dwell を最小へ下げたときに確定が最大2倍遅れる（`Math.min` の第2項がこれを構造的に保証する）。
毎報告で走らせると、1端末の評価のために毎回 `/devmon/facilities` と `/devmon/devices` を
全件読むことになる（端末50台で概算130MB/日。RTDB は打刻と共有している）。
しきい値は確定=180秒・途絶=24時間なので、60秒間隔で取りこぼしは生じない。
間引いた回は自施設1件だけを読む。
★ `/devmon/settings` は管理UIを持たず変更が極めて稀なので、**5分**だけウォームインスタンス内で
使い回す（`SETTINGS_TTL_MS`）。
★ **TTL は呼び出し間隔より必ず長くする。** 60秒にしていたが、定期実行が60秒間隔・端末報告が
120秒間隔なので**命中率が実測0%**で、キャッシュが一度も効いていなかった。
★ 無期限にしてはならない（設定変更が永久に反映されなくなる）。
★ キャッシュするのは `dwellSec` / `staleSec` だけである。**認可に関わる値
（端末トークン・`revoked`・施設の `enabled`/座標/半径・管理者ロール）は毎回新鮮に読む。**

### 定期実行（管理画面を開かなくても通知する）— 2026-09-13

`POST /api/device-report` に `action:"sweep"` を足した。**新しいエンドポイントは作っていない。**

```
POST https://timecard-rho.vercel.app/api/device-report
Content-Type: application/json
{"action":"sweep","key":"<DEVICE_SWEEP_KEY と同じ値>"}
```

★ **判定は `D.runSweep` をそのまま呼ぶだけ**で、この経路に独自の判定ロジックは無い
（`sweepPlan` / 距離計算 / LINE本文の生成をここへ書いてはならない）。

**なぜ別エンドポイントにしないか**

1. Vercel の Serverless Function は Hobby プランで **12本が上限**で、現在 **11本**ある。
   endpoint を1本増やすと上限に張り付き、次の機能追加で deploy そのものが落ちる。
2. 外部スケジューラは `Origin` を送らないため、`api/_lib/http.js` の `guard`（Origin 必須）は使えない。
   `api/device-report.js` の `guardApp`（Origin を見ない・CORS を返さない）がそのまま要件に合う。

**なぜ Vercel Cron を使わないか**

Hobby プランの Cron は **1日1回**しか実行できない（頻度を上げるには Pro が必要）。
GitHub Actions の `schedule` も、このリポジトリでは **1時間45分〜2時間22分遅れる**ことを実測している
（上記「朝出勤確認 LINE通知」の表）。1分間隔を満たせるのは外部スケジューラだけである。

**認証（★ 順序を入れ替えてはならない）**

```
① 共有鍵の照合（環境変数 DEVICE_SWEEP_KEY・定数時間比較・RTDB へ0往復）
② IP単位のレート制限（10分窓60回。鍵が漏れたときの上限）
③ 設定・施設・端末の取得 → runSweep
```

★ `sweep` だけ**鍵の照合をレート制限より前**に置いている。これは
「認証より前に重い取得をしない」という同ファイルの不変条件を**弱めるものではなく、より厳しい順序**である
（鍵を知らない相手は RTDB へ1往復も起こせない）。逆にレート制限を先に置くと、
鍵を知らない攻撃者のリクエストが `bumpAndCount` の2〜3往復へそのまま変換されてしまう。

★ 鍵は **32文字以上をコード側で強制する**（`SWEEP_KEY_MIN_LEN`）。鍵の誤りにはレート制限が
掛からないため、総当たりに耐えるのは鍵の長さだけである。**この値を下げてはならない。**
★ 鍵は**前後の空白と改行を無視して照合する**。環境変数やジョブ本文に改行が1文字混ざるだけで
恒久的に 403 になり、しかも 503（未設定）と区別できず切り分け不能になるため。
★ `DEVICE_SWEEP_KEY` が未設定・32文字未満のときは **503 `sweep_not_configured`** を返す。
黙って何もしない状態を作らないため（外部スケジューラの実行履歴に失敗として残る）。

★ 鍵は **`register` / `report` には一切効かない**。端末の認証は従来どおり端末トークンである。

**本文が JSON でないときは 400 `bad_json` を返す（★ 500 にしてはならない）— 2026-09-13**

⚠⚠ **Vercel の body パーサは `Content-Type: application/json` で解析に失敗すると、
`req.body` を参照した時点で例外を投げる。** これを handler の汎用 `catch` へ落とすと
**500 `server_error`** になり、「クライアントの本文が壊れている」ことが運用者から見えなくなる。

実際に起きた（2026-09-13）。外部スケジューラの本文が JSON でなかったため `action:"sweep"` が
**毎分 500** を返し続け、応答本文は `{"error":"server_error","cid":…}` だけで原因へ辿れなかった。
Vercel の runtime ログには `[device-report] <cid> Invalid JSON` が毎分1件出ていた。

対処は `readJsonBody` / `bodyFingerprint`（`api/device-report.js`）。

| 本文 | 応答 |
|---|---|
| 正しい JSON オブジェクト | 通常処理へ |
| 前後に空白／改行 | **受け付ける**（Vercel の `JSON.parse` 自体が JSON の空白を許すため、そもそも例外にならない） |
| BOM 付き | **400 `head=unread`**。★ 下記のとおり本番では `req.rawBody` が無いため回復できない |

⚠⚠ **本番の Vercel は `req.rawBody` を提供しない（2026-09-13 実測）。**
form 形式の本文を本番へ投げたときの応答は `{"error":"bad_json","hint":"len=~48-63 head=unread"}` で、
`head` が `word` ではなく `unread` だった。つまり**解析に失敗した本文の中身はサーバ側から見えない**。

したがって本番で得られる指紋は実質 **`len=~N-M head=unread` だけ**である。

・`head=brace` / `percent` / `word` / `quote` などの分類は、**`req.rawBody` を提供する実行環境
　（および回帰テスト）でのみ現れる**。本番の切り分けには使えない。
・代わりに**長さ**で切り分ける。本文の固定部分は既知なので、鍵の長さ `L` に対して
　　正しい JSON `{"action":"sweep","key":"…"}` … **27 + L** バイト
　　form 形式 `action=sweep&key=…` … **17 + L** バイト
　の差（10バイト）から判別できることが多い。
・★ **この差を埋めるために本文のストリームを自前で読む実装を足してはならない。**
　本文は handler 起動前にプラットフォームが消費しており、ハングの危険と引き換えに
　得られるのは診断情報だけである（400 を返すという本質的な挙動は既に正しい）。
　診断が必要なら、運用者に外部スケジューラの設定画面を確認してもらうのが確実で安全。
| form 形式（`action=sweep&key=…`）・URL エンコード・末尾カンマ等 | **400 `bad_json`** ＋ 指紋 |
| 本文なし・空 | 従来どおり 400 `bad_action` |

★ **本文の不正はクライアントエラーである。** 400 で返し、原因が分かる指紋を添える。
★ **直すのは BOM と前後の空白だけにする。** form 形式や URL エンコードを黙って受け付けると
入力形式を2つ持つことになり、`guardApp` の Content-Type 検査（415）が意味を失う。
★ 判定は **レート制限より前**に置く（壊れた本文で RTDB を1往復も叩かせない）。
従来も `req.body` の例外は `bumpAndCount` より前に起きていたので、露出は増えていない。

★★ **指紋（`hint`）に本文の内容を含めてはならない。** 本文には共有鍵と端末トークンが入るため、
先頭数文字でも出せば資格情報が漏れる。出してよいのは次だけである。

```
len=<バイト長> head=<brace|bracket|percent|quote|word|bom|other|empty|unread>[ form=1]
```

・`len` は Content-Length として経路上どこでも見える値なので、新たな露出にはならない
・`head` は**先頭1文字の分類**のみ。`form=1` は `=` の有無のみ
・`hint` は応答にも入れる。**呼び出し元が自分の送った本文について知るだけ**なので露出にならず、
　運用者が外部スケジューラの画面だけで自己診断できる（ログ閲覧が要らない）

★ `api/device.js` / `api/mileage.js` / `api/auth/*` も `req.body` を同じ形で読んでおり、
**同じ潜在欠陥がある**（不正な本文で 500 になる）。呼び出し元が自前の `index.html` なので
実害は出ていないが、共有の `api/_lib/http.js` の `guard` へ入れると全エンドポイントへ波及するため
今回は `device-report.js` に限った。未対応事項として記録している。

**コスト（1分間隔・スタブで往復数を実測。推測ではない）**

定常状態（ウォーム・通知なし）の1回あたり **RTDB 往復 5〜6回**
（`devmon/settings` は5分キャッシュ。cron 60秒間隔なら**命中率80%** ＝ 8割が5往復・2割が6往復）。

| # | 種別 | パス | 由来 |
|---|---|---|---|
| 1 | PATCH | `ratelimit/<slot>/dvm_s`（increment） | レート制限 |
| 2 | GET | `ratelimit/<slot>/dvm_s/<ip>` | レート制限 |
| 3 | PUT | `ratelimit/<slot-2>`（null。期限切れの掃除） | レート制限 |
| 4 | GET | `devmon/settings` | 5分キャッシュ |
| 5 | GET | `devmon/facilities` | — |
| 6 | GET | `devmon/devices` | — |

| 項目 | 1回 | 1日（1440回） |
|---|--:|--:|
| RTDB 読み取り | 4（キャッシュ命中時は3） | 約 5,000 |
| RTDB 書き込み | **2**（すべてレート制限の記帳） | 2,880 |
| 転送量（7施設7台） | 約 4.3 KB | 約 6.2 MB |
| 転送量（上限 50/50） | 約 31 KB | 約 45 MB |
| Vercel 実行 | 1 | 1,440（Hobby の無料枠に十分収まる） |

★ 参考: 管理画面を1枚開くと `tc5_approvals` の10秒ポーリングだけで **19.6 MB/時**になる。
定期実行の最悪日（45 MB）は管理画面 約2.3時間ぶん、現実値（6.2 MB）は約19分ぶんに相当する。
`/honomi`（打刻）へは1往復も触らない。

★ **往復の3件（書き込みの全部）はレート制限の記帳である。** これは「鍵が漏れたときの上限」
という認可要件に由来するので削らない（`RULES.md`「Performance FAIL条件」の FAIL除外）。

★ **間隔を短くしてはならない。** 確定のしきい値は180秒なので、30秒間隔にしてもコストが倍になる
だけで平均の遅れは 210秒 → 195秒（7%）しか縮まらない。逆に負荷を下げたい場合の調整弁は
**120秒**（コスト半減・平均の遅れ +30秒）で、安全性は一切下がらない。300秒は最悪8分になり
盗難アラートとしての目的を損なうので不可。

**設定（リポジトリ外。Agent からは変更しない）**

1. Vercel の環境変数 `DEVICE_SWEEP_KEY`（32文字以上のランダム文字列）を Production へ登録して再デプロイ
2. cron-job.org に「毎分・POST・上記の JSON」を登録

★ どちらもリポジトリ外なので、**本書の禁止事項どおり Agent からは設定しない**。人へ依頼する。

★ **端末アプリの報告間隔は 120秒**（`REPORT_THROTTLE_MS`）。これがレート制限に対する歯止めで、
最終報告時刻は端末へ**永続化**する（背景タスクは新しい JS コンテキストで起こされることがあり、
メモリだけで持つと 0 に戻って間引きが効かなくなる）。

★ `distanceInterval`（**位置更新で起こされる距離のしきい値。ジオフェンスの半径とは別物**）は **200m**。
・小さくすると起床が増える（電池のみ。報告回数は上の120秒で頭打ち）
・大きくすると、**施設の数百m先へ持ち出してそのまま置かれた場合に2回目の報告が発生せず**、
　確定がサーバ側スイープ（＝他端末の報告か管理画面の表示）だけに依存する
許容半径（既定150m）と同程度にして、検知の確実性を優先している。

★ ジオフェンスの出入りは間引かない（継続計測の開始時刻そのものなので落とせない）。
サーバの最短間隔（10秒）で弾かれた場合だけ、12秒待って1回やり直す。

★ **監視OFFでも端末側の監視を止めてはならない。** `report()` を呼ぶ自動契機は
ジオフェンスと位置更新の2つだけなので、止めると端末は新しい設定を取りに来る手段を失い、
**管理者が監視をONに戻してもアプリを開くまで再開しない**。OFF のあいだはサーバ側が判定・通知の対象から外す（位置の判定・持ち出し確定・受信途絶・
判定不能・**権限の通知**のすべて）。届いた報告で進むのは `lastSeenAt` と、権限状態の**記録**だけである。
アプリは起動・復帰時に監視が落ちていれば自分で張り直す（`ensureWatching`）。

★ **通知は「範囲外になってから3分後ちょうど」には来ない。** 「3分以上経ったあとの最初の報告」で出る。
移動中は位置更新が届くため通常は数分だが、**遅れることがある**。この性質を「3分で通知する」と
書き換えてはならない。

★ **通知の種類ごとに「取り逃し」と「重複」のどちらへ倒すかを決めてある。逆にしてはならない。**

| 通知 | 印を付ける順序 | 理由 |
|---|---|---|
| 持ち出し検知（exit） | **送信できてから**確定（`state:"outside"` / `notifiedAt`）を書く | 先に書くと、巻き戻しに失敗した時点で `evaluateReport` も `sweepPlan` も拾わなくなり、**その持ち出しのアラートが完全に失われる**。重複は許容し、取り逃しを避ける |
| 受信途絶・判定できていない | **印を先に付けてから**送る | Vercel は同時実行でスケールするため、送ってから印を付けると同時に走ったスイープが同じ集約通知を何通も出す。取り逃してもしきい値で再評価されるので、重複を避ける |

★ 同じ継続についての再確定は、前の持ち出しの通知（`notifiedAt`）が今の継続より新しいときだけ抑止する。
**同じ持ち出しで LINE を送れたのに確定の書き込みに失敗した場合は、スイープが再送する**
（＝重複しうる）。持ち出し検知は取り逃しより重複を選ぶ、という上の表の方針どおりである。
受信途絶・判定できていない側は逆で、印を先に付けるため重複しにくいが、
**再アームはしきい値ぶんの時間差で判定する**（印だけで抑止すると、印を戻せなかった時点で
二度と通知されなくなる）。
★ 継続開始より新しい位置判定が無い端末は確定させない（監視OFF・基準位置変更をまたいだ
古い観測で、現在位置を見ずに通知しないため）。

★ **基準位置・半径・ON/OFF を保存したときは、その施設の端末を次のように扱う。**
・`state` が `outside`（持ち出し確定済み）以外の**全端末**を `pendingSince:null` / `state:"unknown"` へ戻す。
　★ `pendingSince` を持つ端末だけでは足りない。旧基準で `inside` だった端末が
　「古い判定のまま緑（監視中）」で残る。
・`unjudgedNotifiedAt` を now にして**しきい値ぶんの猶予**を与える。
・`staleNotifiedAt` の猶予は「保存時点でまだ沈黙していない端末」だけに与える。
　★ `lastSeenAt` は監視OFFでも進むので、すでに沈黙している端末は**本物の異常**である。
　無条件に猶予すると、その通知を最大しきい値ぶん（計48時間）遅らせてしまう。
　★ 監視OFFのあいだ `lastJudgedAt` は進まないので、OFF が長引いたあと ON へ戻すと、
　その場のスイープが即座に「位置を判定できていません」を送ってしまう
　（ON にした本人が誤アラートを受ける）。

### 通知は既存の LINE 基盤を再利用する

★ **通知基盤を新設していない。** 既存の Messaging API push と同じ環境変数
（`LINE_CHANNEL_ACCESS_TOKEN` / `LINE_TO_ID`。`api/line-notify.js` と共通）を使う。

```
【施設端末 持ち出し検知】
ミュゲの泉
9/12 20:15
施設の設定範囲外へ移動しました。
```

施設名は本文へ入れる前に制御文字・改行・ゼロ幅・双方向制御を落とす。
落とさないと、施設名に改行を混ぜて**偽の行（例「範囲内へ戻りました」）を本文へ差し込める**。

★ **受信途絶と「位置を判定できていない」は1通へまとめる**（`buildDigest`）。
端末ごとに送ると端末数に比例して外部通信が増える（N+1）。
**持ち出し検知（exit）はまとめない**（どの施設かを即座に伝える必要があるため）。

### 端末の登録

1. 管理画面で監視ON（または基準位置だけ）を**保存**する
2. 「端末の登録コードを発行」（8桁・24時間・1台ぶん）
3. 施設のスマホで監視アプリにコードを入力 → 位置情報を「**常に許可**」
4. 基準位置が未設定なら、**発行時に指定していた場合だけ**登録した端末の現在地を基準位置にする
   （誤差100m超なら基準位置を設定せず、その旨を返す。★ 粗い位置で基準を決めない）

★ 端末トークンは**登録時に1度だけ返し、以後はハッシュで突き合わせる**（定数時間比較）。
管理APIは端末トークンもそのハッシュも返さない。

★ **登録コードは発行時の1回だけ表示する**（`bootstrap` でも返さない）。閉じたら再発行する
（未使用コードは20件まで。期限切れは次の発行時にまとめて掃除する）。

★ 管理画面の端末一覧には **最終受信・位置判定の時刻・直近の距離** を出す（1端末＝2段で表示する。
1行へ詰めると 320px 幅で要素が散り、「（できていません）」が別の端末の行に見える）。
「監視中（緑）」を名乗る条件に「直近に位置判定が成立していること」を必ず含める。
★ **基準位置・半径が欠けている施設は「監視ON（基準位置が未設定）」（赤）で出す。**
サーバの `facilityReady()` と同じ条件を鏡写する。欠けているとサーバは一切判定しないので、
「判定待ち（待てば直る）」に見せてはならない。
★ **1度も位置を判定していない端末、および判定をやり直す状態（`state:"unknown"`）の端末が
あるときは緑にしない**（「監視ON（判定待ち）」）。判定不能の通知しきい値は24時間なので、
これが無いと設置直後の1日だけ「常に許可」を取れていない端末が正常に見える。
基準位置・半径・ON/OFF を保存するとサーバが該当端末を `unknown` へ戻すため、
`lastJudgedAt` だけを見ていると「判定していないのに監視中（緑）」になる。
★ **権限の異常（赤）は「判定待ち」「範囲外を確認中」（橙）より先に出す。**
後ろへ置くと、原因の提示が最大24時間遅れる。
★ 孤立した監視設定と表記ゆれの衝突は**描画のたびに**作り直す。取得時だけに計算すると、
拠点を追加した直後に衝突警告が出ず、削除後も孤立警告が残る。

★ **`register` は登録コードの実在だけで通る。** 管理者が発行したコードなので設計上は問題ないが、
コードを外部へ渡すと第三者の端末を登録できる。コードは口頭・手渡しで扱うこと。

### 報告1回の処理順序（★ 入れ替えてはならない）

`/api/device-report` は Origin を見ない＝**誰でも無認証で叩ける**。RTDB は打刻と honomi-board と
同居しているため、無認証の段階で重い取得を許すと打刻＝賃金側へ負荷が波及する。

1. **IP単位のレート制限**（登録 20回/10分・報告 120回/10分）。認証より前に必ず通す唯一のゲート
2. 端末IDの形式検査 → **端末1件だけを取得**してトークンを定数時間比較（ここで初めて認証が成立）
3. 同一端末の連打は、**すでに読み込んだ `lastSeenAt` との差**で弾く（追加の往復を使わない）
4. 認証後に設定・施設・端末一覧を**並列で**取得し、判定・保存・通知・スイープを行う

★ レート制限のキーをリクエスト本文の `deviceId` だけにしてはならない。
攻撃者が毎回ランダムな `deviceId` を送るだけで上限を素通りできる。

### 端末APIを別エンドポイントにしている理由

ネイティブアプリは `Origin` ヘッダを送らないため、`api/_lib/http.js` の `guard`
（Origin 不在を拒否する）をそのまま使えない。`api/device-report.js` は代わりに次を守る。

・Cookie を一切使わない（＝CSRF が原理的に成立しない）
・**CORS 応答ヘッダを一切返さない** → ブラウザからのクロスオリジン呼び出しは
　`application/json` のプリフライトが失敗して届かない
・認証は端末トークン。**レート制限は登録・報告ともIP単位**（登録 20回/10分・報告 120回/10分）。
　同一端末の連打は、すでに読み込んだ `lastSeenAt` との差（10秒）で弾く
　★ レート制限のキーをリクエスト本文の `deviceId` にしてはならない（毎回変えれば素通りできる）

★ **この2つを1つのエンドポイントへまとめてはならない。** まとめると、
管理者操作の Origin 検査を外すか、アプリを弾くかのどちらかになる。

### 既知の制限（未解消・運用で認識しておくこと）

1. **範囲外になる前から沈黙している端末は検知できない**（電源を切ってから持ち出す等）。
   OSの機能を使う以上避けられない。
   ★ 一方、**範囲外を1回でも報告したあとに沈黙した場合は定期実行のスイープが確定させる**
   （端末が1台だけでも、管理画面を開かなくても通知される）。
   ⚠ **ただしこれは①が実際に通るようになってからである。** ①が 400 / 403 / 503 を返している間は、スイープが走るのは
   「他の端末が報告したとき」と「管理者がマスター管理タブを開いたとき」だけなので、
   **端末が1台しか無い施設では管理画面を開くまで通知が出ないことがある。**
2. **位置情報を切られた／アプリを削除された瞬間は検知できない。** アプリが次に起動・復帰した
   ときに権限の異常を報告する。それも無ければ受信途絶（既定24時間）または
   「位置を判定できていません」として現れる。
3. **定期実行（cron-job.org）の設定はリポジトリ外にある。** 止まっても GitHub Actions のように
   失敗が見えず、**cron-job.org 側の実行履歴（HTTP ステータス）でしか気づけない**。
   止まった場合は保険の②③（端末の報告時・管理画面を開いたとき）だけが残るため、
   端末1台の施設では確定が遅れる。
   ★ `DEVICE_SWEEP_KEY` を Vercel から消すと **503** を返して**確定の主経路が止まる**
   （保険の②③だけが残る）。環境変数を整理するときに消さないこと。
   ★ 定期実行の設定も環境変数も Agent からは変更しない（本書の禁止事項）。人へ依頼する。
4. **iOS は静止している端末への背景実行を保証しない。** 受信途絶のしきい値を24時間にしているのは
   このため。**短くすると毎日のように誤警報になる。**
5. **Android を Play ストアで公開する場合、背景位置の利用理由について Google の審査がある。**
   内部配布（内部テスト・APK 直接配布）なら審査は不要。**iOS の実機配布には Apple Developer Program が必要。**
6. **施設を改名すると監視設定が孤立する**（施設マスタに改名が無く、削除＋追加になるため）。
   拠点を削除したときは設定と端末を自動で無効化するが、**改名では引き継がれない。**
   ★ 孤立した設定は「拠点トークン管理」の**警告枠（拠点マスタに無い端末持ち出し監視の設定）**へ
   一覧表示し、そこから「監視を解除」できる。これが無いと、孤立した設定は画面のどこにも現れず、
   監視をOFFにも端末の解除にもできないまま通知が出続ける（**管理導線の喪失**）。
   施設マスタは一般スタッフでも書けるため、この状態は職員の操作でも作れる。**この警告枠を外してはならない。**
   ★ 施設名の正規化（NFKC＋空白除去）で同じキーへ寄る拠点（例「ハルイロ」と「ハル イロ」）も
   衝突として赤く警告する。放置すると同じ設定が2行に出て、片方の保存が他方を上書きする。
7. **1回の報告でレート制限カウンタのために RTDB へ2〜3往復する**（既存 `bumpAndCount` の仕様）。
   報告は端末あたり15分間隔程度なので許容している。
8. **集約通知は施設名だけを並べる。** 同一施設に複数端末があると、受信途絶・判定不能の
   集約で同じ行が並んで区別できない（例「・ミュゲの泉（24時間以上）」×2）。
   持ち出し検知の書式を保つため端末名は入れていない。台数が増えたら見直す。
9. **同時実行での重複通知を完全には防げない。** 受信途絶・判定不能は「印を先に付ける」ことで
   ほぼ防いでいるが、RTDB に compare-and-set が無いため理論上は残る。持ち出し検知は
   意図的に重複側へ倒している（上記の表）。
   ★ 定期実行が1分間隔になったため、**LINE は送れたのに確定の書き込み（`confirmPatch`）だけが
   失敗し続ける**場合、放置すると持ち出し検知が最短1分おきに再送される。
   **確定を送信前に書く形へ変えてはならない**（それは通知の恒久的な喪失を招く）ので、代わりに
   `runSweep` が **「この継続（`deviceId + "@" + pendingSince`）の LINE はこのインスタンスから
   既に送れている」** をメモリに記録し、送信を繰り返さず**書き込みのやり直しだけ**を行う。
   ・キーに `pendingSince` を含めるのが要点である。**別の持ち出しは必ず `pendingSince` が違う**ので、
   　復帰後の再持ち出しを取りこぼさない。抑止するのは「すでに管理者へ届いた同一内容」だけ。
   ・**送信が失敗したときは記録しない**（記録すると通知の取り逃しになる）。
   ・記録は**最大200件・TTL 1時間**（`EXIT_SENT_MAX` / `EXIT_SENT_TTL_MS`）。上限を超えたら
   　最古から落ちる。端末上限は50台なので、生きている継続が押し出されることは実運用で起きない。
   　TTL で必ず失効するので、この保険が通知を恒久的に飲み込むことはない
   　（＝確定の書き込みが1時間以上失敗し続ける場合は、同一継続で1時間に1回まで再送する）。
   ・これは保険であって正本ではない（インスタンスが入れ替われば失われる）。正本は RTDB の
   　`notifiedAt` のまま。したがって重複が完全に 0 になるわけではない。
   ・**報告の経路（`handleReport`）の exit 送信はこの記録を通らない。** そこで確定の書き込みが
   　失敗した継続は、次のスイープが1通だけ重複させる（その1通が記録を作るので毎分再送にはならない）。
   ★ **LINE の push 枠は朝の未打刻通知（`scripts/morning-check.js`）と同じ資格情報・同じ枠である。**
   枠を食い潰すと業務上より重い通知まで沈黙するため、cron-job.org の実行履歴を監視すること
   （コード側の上限だけに頼らない）。見るべき値は次のとおり。
   ・**`stales` / `unjudged` が毎回 0 より大きいまま**続いていないか
   　★ **`sent` では検知できない。** LINE 自体が落ちている場合 `sent` は 0 のままで、
   　裏では送信試行と RTDB 書き込みが回り続ける（実測 2,880試行・5,760書込/日）。
   ・HTTP ステータスが 200 以外（403＝鍵違い／503＝未設定／500＝サーバ側の失敗）
   ★ 集約通知（受信途絶・判定できていない）側のこのループには `_exitSent` の抑止が効かない
   （抑止は持ち出し検知だけ）。安全側（取り逃し防止）を優先した結果であり、是正しない。
   ★ `sweepPlan` は形式が不正な端末ID（`isDeviceId` を満たさないキー）を対象にしない。
   `patchDevice` が必ず throw するため、1件混ざると「送れるが確定は永久に書けない」＝
   毎分の再送が**決定論的に**成立する。`markDevices` と対称に保つこと。
10. **登録コードは「発行時点で基準位置が未設定」のときだけ端末の現在地を基準位置にする。**
   発行後に管理者が手入力した基準位置を、古いコードでの登録が上書きすることはない
   （`base_already_set` を返す）。端末が基準位置を書いた場合は `updatedBy` に `device:<id>` を残す。
11. ⚠ **Vercel の Serverless Function が 11 本になった**（`api/` 配下の `.js`。直下5本＋`api/auth/`6本。2026-09-12 時点）。
   Hobby プランの上限は 12 本である。**次にエンドポイントを増やすときは、プランの実際の上限を
   確認してから足すこと。** 上限を超えると deploy そのものが失敗する。

### 専用アプリ（`devicewatch/`）

★ **勤怠アプリ全体をネイティブ化していない。** 持ち出し監視だけの1画面アプリで、
打刻・勤怠一覧・スタッフ一覧・履歴画面を持たない。個人データを扱わない。

★ **`package.json` を置いていない。** 依存の版は `npx expo install` が SDK に合わせて決める。
版を固定すると SDK と不整合な版が入り、ビルドが通らない／背景位置が動かない。
手順は `devicewatch/README.md` を正とする。

★ `App.js` は **JSX を使わず `React.createElement`** で書いてある。ビルド環境が無い本リポジトリでも
`node --check` で構文検査できるようにするため。**JSX へ書き換えてはならない**（検査手段が消える）。
⚠ ただし `import` を含むため、`node --check` が通るのは **Node 22.7 以降**（ESM 構文の自動検出）である。
GitHub Actions の `node-version: "20"` では `SyntaxError` になる。CI で検査対象にしていないので
現状は問題ないが、**ワークフローへ追加するなら Node のバージョンを上げること。**

⚠ **GitHub Pages はリポジトリ直下を丸ごと配信するため、`devicewatch/` のソースは公開される。**
資格情報を置いてはならない（現在はAPIのURLだけで、`index.html` が既に公開している値と同じ）。
Vercel へは配信しない（`.vercelignore` で除外済み）。

### テスト

`node scripts/test-device-watch.js`（依存パッケージなし・送信なし・本番データ非アクセス。I/O 関数は呼ばない）。
**持ち出し検知・`/devmon`・`api/device*.js`・管理画面の監視UIに関係する変更では実行必須**
（全件 PASS / 0 FAIL でなければ出荷しない）。テスト件数は増減するため固定値を規範にしない。
距離計算・継続判定・重複抑止・GPS誤差（`acc` 省略の抜け道を含む）・サーバ時刻での計測・
監視OFF時のフェイルクローズ・権限喪失の1回通知・受信途絶の1回通知・
**端末が沈黙してもスイープで確定すること**・**位置を判定できていない状態の検知**・
LINE本文（改行注入と集約）・権限マトリクス・`/devmon` がクライアントから到達不能であること・
端末APIが業務データを読まないこと・**認証より前に重い取得をしないこと**を固定する。

★ 定期実行については、1分間隔のスイープを**実際に回して**
「端末が沈黙していても3分で確定すること」「同一持ち出し中は1440回回しても通知が1回だけであること」
「3分未満で範囲内へ戻った外出は何回回しても通知しないこと」「復帰後の再持ち出しで再通知すること」
「再外出は今回の時刻から3分を測ること」「監視OFF・基準位置未設定では確定しないこと」を固定する。
入口については `sweepKeyError` を抽出して**値で**固定する（鍵なし・空・前方一致・非文字列・
超長文字列を拒否すること／未設定なら503／定数時間比較／鍵を応答とログへ出さないこと）、
および「鍵の照合がレート制限より前にあること」「`runSweep` へ委譲していて独自判定を持たないこと」
「保険の②③を外していないこと」「鍵は32文字以上を強制すること」
「振り分けに catch-all の else が無いこと（4つ目の action が鍵照合を通らずスイープへ落ちない）」
「定期実行は間引きの対象にしないこと」を固定する。

★★ さらに `POST /api/device-report` の **handler を実際に動かして**固定する。
そのために、テストの冒頭で `api/_lib/google.js`（RTDB・OAuth・外部HTTP）を require キャッシュで
スタブへ差し替えている。**この差し替えは `api/_lib/*.js` を require する前に行うこと**（後では効かない）。
狙いは2つある。

1. ソース走査と純粋関数だけでは、**結線の壊れ**（`guardApp` / action の振り分け / 応答の形 /
   鍵照合の位置）を検出できない。特に「管理画面を開かずに LINE が1通出ること」は
   handler を動かさないと確認できない。
2. 本ファイルの契約「送信なし・本番データ非アクセス」を**仕組みで保証する**。
   実装が誤って I/O を呼んでも、本番やLINEへ出る前にスタブで止まる。

handler で固定するのは、`guardApp`（GET=405 / 非JSON=415 / OPTIONS=403 / CORS 非返却 / `no-store`）、
**鍵が未設定・誤りのときに RTDB へ1往復も起こさないこと**、応答が件数だけであること、
**沈黙した端末を管理画面を開かずに確定させて LINE を1通送ること**、同一持ち出し中は何回叩いても
送らないこと、**確定の書き込みだけが失敗しても毎分送り直さないこと**（かつ書き込みのやり直しはすること）、
**送信が失敗した持ち出しは次のスイープで必ず送り直すこと**（取り逃さない）、
別の持ち出し（`pendingSince` が違う）は抑止しないこと、
監視OFF・基準位置未設定では確定しないこと、`sweep` の鍵で `register` / `report` が通らないこと。

★ 書き込みと LINE を意図的に失敗させる区間があるため、その区間だけ `console.error` を黙らせている
（実装のログ出力は変えていない）。区間を抜けたら必ず戻すこと。

★ 加えて、`index.html` の `DEVWATCH` ブロックを**実際に動かして**
「取得に失敗しても再取得を撃ち続けない」「プロトタイプを触る施設名で壊れない」
「孤立設定と表記ゆれ衝突を拾う」「判定できていない端末を『監視中』と表示しない」
「入力中は再描画で止める」を固定する。ソース走査だけにすると、これらの回帰を検出できない。

---

## 使用技術

- **フロントエンド**: 単一 `index.html`（バニラ JS、ビルドなし）。Firebase JS SDK 10.12.0（`firebase-app-compat` / `firebase-messaging-compat`、CDN）。
- **DB/認証**: Firebase **Realtime Database**（REST `…/honomi/<path>.json` を `authFetch` で読み書き）。認証は Firebase Identity Toolkit（`accounts:signUp` + `securetoken` リフレッシュ）でトークンを取得しヘッダ付与。
- **Push**: Firebase Cloud Messaging（FCM。`FCM_MESSAGING_SENDER_ID` / `FCM_VAPID_KEY` を `index.html` に保持）。
- **サーバー側 API**: Node.js（`https` モジュール）製 Vercel サーバーレス関数（`api/line-notify.js`・`api/discord-notify.js`）。
- **バッチ/通知スクリプト**: Node.js（`scripts/*.js`、GitHub Actions の `node-version: "20"` で実行）。
- **インフラ**: GitHub Pages（静的配信）、Vercel（通知API）、GitHub Actions（cron/手動）、Firebase（RTDB/Auth/FCM/Storage）。
- `firebase.json` は `database`（`database.rules.json`）と `storage`（`storage.rules`）のみ定義。**`hosting` は未定義**（＝Firebase Hosting は使っていない）。

---

## 実在する npm scripts

- **なし**（`package.json` が存在しないため npm scripts は無い）。
- したがって `npm run build` / `npm test` / `npm run lint` など**存在しないコマンドを使わない**。

---

## build / lint / test / deploy 手順

- **build**: ビルド工程は**無い**（静的 `index.html`。トランスパイル/バンドルなし）。
- **lint**: lint 設定・コマンドは**未整備**（ESLint 等の設定ファイルなし）。「lint して」と言われても存在しないコマンドを実行しない。
- **test**: テストフレームワーク（Playwright / Jest 等）・smoke・`agent:ship` は**未整備**。実施可能な確認は手動の構文チェック・dryRun・下記の回帰テストのみ:
  - JS 構文: `node --check scripts/morning-check.js`（同様に `notify-check.js` / `fcm-check.js` / `api/line-notify.js` / `api/discord-notify.js` / `sw.js`）
  - JSON 構文: `node -e "JSON.parse(require('fs').readFileSync('manifest.json','utf8'))"`（`database.rules.json` も同様）
  - **移動距離申請の回帰テスト（依存パッケージなし・送信なし・本番データ非アクセス）**: `node scripts/test-mileage.js`。金額計算・端数処理・未登録区間・権限マトリクス（労務士に書込系が入らないこと）・クライアント/サーバの計算一致・CSV形式を検証する。**移動距離申請（`api/mileage.js` / `api/_lib/mileage.js` / `index.html` の移動距離モジュール）に関係する変更では実行必須**（全件 PASS / 0 FAIL でなければ出荷しない）。テスト件数は増減するため固定値を規範にしない。
  - **管理者トークン状態の回帰テスト（依存パッケージなし・送信なし・本番データ非アクセス）**: `node scripts/test-admin-token-state.js`。`index.html` の管理者URLトークン「設定状態」判定ブロックを抽出して検証する。**管理者URLトークン・管理者PINの設定状態表示・`/config/adminTokenSet.json` / `adminTokenHash.json` / `adminToken.json` の取得処理に関係する変更では実行必須**（全件 PASS / 0 FAIL でなければ出荷しない）。テスト件数は増減するため固定値を規範にしない。
  - **有給「付与日数の修正」の回帰テスト（依存パッケージなし・送信なし・本番データ非アクセス）**: `node scripts/test-paid-leave-grant-edit.js`。`index.html` の付与日数修正ブロックを抽出し、残日数の再計算式・下限判定・確認表示と保存値の一致・`usedDays` と他の付与行を変更しないことを検証する。**有給の付与日数修正・残日数の計算に関係する変更では実行必須**（全件 PASS / 0 FAIL でなければ出荷しない）。テスト件数は増減するため固定値を規範にしない。
  - **有給取得履歴の回帰テスト（依存パッケージなし・送信なし・本番データ非アクセス）**: `node scripts/test-paid-leave-history.js`。`index.html` の `PL-HISTORY-BEGIN/END` ブロックを抽出し、承認済み実績と未来日の取得予定の分離・pending を取得済みにしないこと・複数日申請の取得日単位への展開・未知 status を捨てないこと・同姓同名の取り違え防止・正本を書き換えないこと・表示系ハンドラが通信/書込をしないことを検証する。**有給取得履歴に関係する変更では実行必須**（全件 PASS / 0 FAIL でなければ出荷しない）。テスト件数は増減するため固定値を規範にしない。
  - **打刻の端末永続保存・自動再送の回帰テスト（依存パッケージなし・送信なし・本番データ非アクセス）**: `node scripts/test-punch-outbox.js`。`index.html` の `PUNCH-OUTBOX-BEGIN/END` ブロックを抽出し、IndexedDB 保存の成否・二重登録防止・実打刻時刻の不変性・syncing の復旧・多重送信の抑止・認証切れ・警告条件・旧 localStorage キューからの移行・sandbox/デモ/閲覧用での無効化・`index.html` 側の結線を検証する。**打刻に関係する変更では実行必須**（全件 PASS / 0 FAIL でなければ出荷しない）。テスト件数は増減するため固定値を規範にしない。
  - **tc5_records の期間取得と app shell キャッシュの回帰テスト（依存パッケージなし・送信なし・本番データ非アクセス）**: `node scripts/test-records-range.js`。`index.html` の `RECORDS-RANGE-BEGIN/END` ブロックを抽出し、区間集合の境界条件・未取得部分だけの取得・管理画面の入口で全件取得しないこと・月単位の分割スキャン・月/年セレクタの選択肢・フェイルクローズ、および `sw.js` が app shell 以外（Firebase / 認証 / API）を Cache Storage へ入れないことを検証する。**`tc5_records` の取得範囲・管理画面の入口・月/年セレクタ・`sw.js` に関係する変更では実行必須**（全件 PASS / 0 FAIL でなければ出荷しない）。テスト件数は増減するため固定値を規範にしない。
  - **管理者の勤怠編集（未打刻日の新規作成）の回帰テスト（依存パッケージなし・送信なし・本番データ非アクセス）**: `node scripts/test-admin-attendance-edit.js`。`index.html` のスタッフ別詳細の時刻セル描画と `attachTimeCellHandlers` の保存処理を抽出し、未打刻からの新規作成・既存打刻の非破壊・未取得の月のフェイルクローズ・承認済み日の読み取り専用を検証する。**管理者の勤怠編集・時刻セルの描画条件に関係する変更では実行必須**（全件 PASS / 0 FAIL でなければ出荷しない）。テスト件数は増減するため固定値を規範にしない。
  - **承認漏れサマリー・通知件数集計の回帰テスト（依存パッケージなし・送信なし・本番データ非アクセス）**: `node scripts/test-unapproved-summary.js`。`index.html` の `UNAPPROVED-SUMMARY-BEGIN/END` ブロックと `validateAttendanceRecord` / `isPastDate` を抽出し、過去日の未承認だけを数えること・当日を数えないこと・承認済みを誤検出しないこと・打刻の無い日や削除済みだけの日を承認漏れにしないこと・出勤のみ／退勤のみ／時刻欠落／勤務時間異常の判定・**索引版が素朴版（`records` 全件 filter）と完全一致すること**・正本を書き換えないことを検証する。**承認漏れの集計・通知件数・打刻漏れ判定に関係する変更では実行必須**（全件 PASS / 0 FAIL でなければ出荷しない）。テスト件数は増減するため固定値を規範にしない。
  - **承認漏れ集計の所要時間の計測（回帰テストではない・依存パッケージなし・送信なし・本番データ非アクセス）**: `node scripts/bench-history-scan.js`。`index.html` の `RECORDS-RANGE` ブロックを実タイマーで動かし、ネットワークを「RTT ＋ 共有帯域」で模擬して、集計完了・表示月確保・月切替の待ち時間を測る。第1引数に別の `index.html` を渡すと修正前と比較できる。**合否は判定しない**（AGENTS.md に載せた数値の追試用）。
  - **打刻イベントの適用確認（本番データを読むだけ・書き込みなし）**: `node scripts/verify-punch-events.js`。`FIREBASE_API_KEY` / `FIREBASE_DATABASE_URL` を環境変数で渡す。`eventId` の重複・ノード名との一致・サーバ受信時刻の妥当性を確認する。
  - **朝出勤未確認の施設別判定時刻の回帰テスト（依存パッケージなし・送信なし・本番データ非アクセス）**: `node scripts/test-morning-check.js`。`scripts/morning-check.js` の `MORNING-CHECK-HOURS-BEGIN/END` ブロックを抽出し、施設ごとの判定時刻の決定（**施設マスタ側の値で判定時刻を動かせないこと**・施設名の正規化・表記ゆれ重複の寄せ・継承プロパティを引かないこと）、6時の回と7時の回が排他であること（二重通知の不在）、改名・表記ゆれの検出、施設名のサニタイズ（LINE 本文への改行・制御文字の注入対策）、**公開 Actions ログへ氏名・施設トークン・LINE 宛先を出さないこと**、`CHECK_HOURS` の各時刻に対応する cron と `case` 分岐が `morning-check.yml` に在ること（導出で検証）、および既存の未打刻判定・LINE送信経路を変えていないことを検証する。**朝出勤未確認 LINE通知・判定時刻・`morning-check.yml` の cron に関係する変更では実行必須**（全件 PASS / 0 FAIL でなければ出荷しない）。テスト件数は増減するため固定値を規範にしない。
  - **打刻時の顔撮影の回帰テスト（依存パッケージなし・送信なし・本番データ非アクセス・カメラも使わない）**: `node scripts/test-face-photo.js`。`index.html` の `FACECAM-BEGIN/END` ブロックを抽出し、既定OFF（`facePhoto===true` 以外はすべてOFF）・出勤退勤だけ撮影すること・sandbox/デモ/閲覧用では撮影しないこと・**撮影した画像を保存も送信もしないこと（fetch / localStorage / IndexedDB / Cache / Blob / dataURL を使わない）**・撮影後の破棄とトラック停止・カメラ拒否でも打刻を止めないこと・`_execPunch` の結線（端末保存の成功後・送信前・await しない）・**勝手にONへ移行する処理が無いこと**を検証する。**顔撮影・スタッフ管理モーダルの当該項目・`_execPunch` の結線に関係する変更では実行必須**（全件 PASS / 0 FAIL でなければ出荷しない）。テスト件数は増減するため固定値を規範にしない。
  - **施設端末の持ち出し検知の回帰テスト（依存パッケージなし・送信なし・本番データ非アクセス）**: `node scripts/test-device-watch.js`。`api/_lib/device.js` の純粋関数を直接呼び、距離計算・入力値の正規化・3分継続での確定・持ち出し中の再通知抑止と範囲内復帰での再アーム・GPS誤差の差し引きと粗い測位の扱い・**サーバ受信時刻で経過を測ること**・監視OFF/基準位置未設定でのフェイルクローズ・権限喪失の1回通知・受信途絶の1回通知・LINE本文（施設名への改行注入を含む）・権限マトリクス（管理APIは管理者だけ）・**`/devmon` が `database.rules.json` に無い（クライアントから到達不能）こと**・端末APIが業務データ（`tc5_*` / `master/locations`）を読まないことを検証する。加えて、**`POST /api/device-report` の handler を実際に動かして**（冒頭で `api/_lib/google.js` をスタブへ差し替えている）定期実行 `action:"sweep"` の結線を検証する（鍵の照合・未認証では RTDB へ1往復もしないこと・管理画面を開かずに確定して LINE を1通送ること・確定の書き込みが失敗しても毎分送り直さないこと・送信が失敗した持ち出しは次で必ず送り直すこと・**本文が JSON でなければ 500 ではなく 400 `bad_json` を返し、指紋に鍵を含めないこと**）。**持ち出し検知・`/devmon`・`api/device*.js`・管理画面の監視UIに関係する変更では実行必須**（全件 PASS / 0 FAIL でなければ出荷しない）。テスト件数は増減するため固定値を規範にしない。
  - 通知ロジック dryRun（送信なし）: `DRY_RUN=true node scripts/morning-check.js`（PowerShell: `$env:DRY_RUN="true"; node scripts/morning-check.js`）。`FIREBASE_API_KEY` / `FIREBASE_DATABASE_URL` 未設定時はスキップ。
- **deploy**:
  - アプリ本体: `git push origin main` → **GitHub Pages が自動デプロイ**（`https://rsb79692-create.github.io/timecard/`）。本リポジトリに Pages 用ワークフローや `CNAME` は無く、ブランチ配信前提（Pages 設定自体はリポジトリ設定側で管理＝リポジトリ内からは設定値まで未確認）。
  - API: `api/*.js` は Vercel（`timecard-rho.vercel.app`）。**`git push origin main` で Production へ自動デプロイされる**（2026-08-14 実測で確定。旧記載の「未確認」を訂正）。`vercel.json` は無いが `.vercel/`（プロジェクトリンク）は存在する。Agent から `vercel --prod` 等の手動デプロイはしない。
    - 確認方法: `vercel ls --prod` で最新 Production が `● Ready`、`vercel inspect <deployment>` または Vercel MCP の `get_deployment` で `meta.githubCommitSha` が push した commit と一致することを確認する（実測時は `source: "git"` / `githubDeployment: "1"` / alias に `timecard-rho.vercel.app` を含むことも確認済み）。
    - ⚠ **GitHub Pages（アプリ本体）と Vercel（API）は別系統で、同じ push から独立に反映される。** 出荷確認は両方を見ること。
  - GitHub Actions のワークフローは push で反映されるが、**ワークフローや Secrets は Agent から変更しない**。

---

## Firebase / Realtime Database / Hosting / GitHub Pages / Vercel の実態

| 項目 | 実態（確認できた事実） |
|---|---|
| Firebase Realtime Database | 使用。データは `…/honomi/` 配下（`tc5_records` ほか）。`database.rules.json` は `honomi` の `.read`/`.write` = `auth != null`。**`honomi/tc5_records` に `.indexOn: ["date"]` あり**（2026-08-28 追加。索引指定のみで権限は不変） |
| Firebase Auth | Identity Toolkit でトークン取得（`accounts:signUp` + securetoken refresh）。詳細な認証方式（匿名 or その他）の Console 設定は**未確認** |
| Firebase Cloud Messaging | 使用（打刻修正申請の Push）。`scripts/fcm-check.js` + `FIREBASE_SERVICE_ACCOUNT_KEY` |
| Firebase Storage | `storage.rules` あり（書類/写真アップロード用と推測されるが詳細は**未確認**） |
| Firebase Hosting | **不使用**（`firebase.json` に `hosting` 定義なし） |
| GitHub Pages | アプリ本体の配信先。`https://rsb79692-create.github.io/timecard/`（コード内 URL から確認） |
| Vercel | API（`api/*.js`：通知・認証・移動距離・施設端末の持ち出し監視）の配信先 `timecard-rho.vercel.app`。**本リポジトリの `main` push から Production へ自動デプロイされる**（2026-08-14 実測。`source: "git"` / `githubDeployment: "1"`） |

---

## cron-job.org / GitHub PAT / 通知系の扱い

- **定期実行の実態**: ⚠ **`schedule` の cron は指定時刻どおりには走らない**（2026-09-11 実測で 1時間45分〜2時間22分遅れ、過去には8時間遅れ。`fcm-notify` は取りこぼしも発生）。**時刻どおりの通知は外部スケジューラからの `workflow_dispatch`** が担っている（`morning-check` の6時分＝毎日 21:03:03 UTC ちょうど）。詳細と7時分の登録内容は上記「主要機能」の朝出勤確認 LINE通知を参照。`morning-check.yml` の cron 2本（`"3 21 * * *"` / `"3 22 * * *"`）は取りこぼし防止の位置づけ。
- **cron-job.org**: 本リポジトリ内に cron-job.org への参照・設定は**見つからない**が、**外部スケジューラの実体は cron-job.org である**（2026-09-11 にユーザーが確認）。`morning-check` を `workflow_dispatch` で起動しており、登録は次の2本:

  | 時刻（UTC） | JST | inputs | 判定時刻 | 状態 |
  |---|---|---|---|---|
  | 21:03 | 06:03 | （空） | 6時 | **稼働中**。毎日 21:03:03 ちょうどに着火（2026-09-11 に `gh run list` で9日連続を実測） |
  | 22:03 | 07:03 | `{"checkHour": "7"}` | 7時 | 2026-09-11 登録。**未発火・実効性は未確認**。初回発火は 2026-09-12 07:03 JST |

  加えて、**施設端末の持ち出し検知の定期実行**を cron-job.org へ登録する（2026-09-13 追加）。
  こちらは GitHub ではなく **Vercel の API を直接叩く**（`workflow_dispatch` ではない）。

  | 間隔 | 宛先 | Body | 目的 | 状態 |
  |---|---|---|---|---|
  | **毎分** | `POST https://timecard-rho.vercel.app/api/device-report` | `{"action":"sweep","key":"<DEVICE_SWEEP_KEY>"}` | 持ち出しの確定と LINE 通知（管理画面を開かなくても出す） | **2026-09-13 登録済み・毎分発火を実測**。⚠ 本文が JSON として読めておらず 400 のまま（本文の修正はユーザー作業） |

  ⚠⚠ **2026-09-13 の障害**: この登録の直後、`action:"sweep"` が**毎分 HTTP 500** を返していた。
  原因は ①cron が送る本文が有効な JSON でないこと ②それを 500 `server_error` として返していたこと
  （`api/device-report.js` の `readJsonBody` で 400 `bad_json` ＋ 指紋を返すよう是正した）。
  **cron-job.org 側の Body を直すまで確定の主経路は通らない。** 応答の `hint` が原因を示す。

  ⚠ `Content-Type: application/json` が必須（無いと 415）。鍵が違えば 403、鍵が未設定なら 503 を返すので、
  cron-job.org 側の実行履歴（HTTP ステータス）で設定ミスに気づける。**2xx 以外の通知を有効にしておくこと。**

  ⚠ 22:03 の行は**登録したという事実だけ**で、発火は確認できていない。確認手順は上記「主要機能」の朝出勤確認 LINE通知を参照し、
  **初回発火のログを目視するまで「稼働確認済み」と扱わない**（本書冒頭の「確認できていない項目は未確認と記載する」に従う）。

  ⚠ この2本は**同一の GitHub 資格情報**で dispatch している。失効すると6時・7時の**両方が同時に静かに止まり**、
  1〜2時間遅れる `schedule` cron だけが残る（Actions は毎日 success のままなので気づきにくい）。

  **外部スケジューラの設定は Agent から変更しない**（変更が必要なら人間に依頼）。
- **GitHub PAT**: 本リポジトリ内に PAT（`ghp_…` / `github_pat_…`）や `api.github.com` / `dispatches` 呼び出しは**見つからない（未確認）**。アプリから GitHub API を叩く実装は確認できなかった。
- **通知系まとめ**:
  - LINE Push: GitHub Actions（`morning-check` / `notify-check`）から直接送信（`LINE_CHANNEL_ACCESS_TOKEN` / `LINE_TO_ID`）。アップロード通知は Vercel `api/line-notify.js`。
  - Discord Webhook: Vercel `api/discord-notify.js`（`DISCORD_WEBHOOK_URL`）。`index.html` がアップロード時に呼ぶ。
  - FCM Push: GitHub Actions `fcm-notify` → `scripts/fcm-check.js`（`FIREBASE_SERVICE_ACCOUNT_KEY`）。
  - 通知のテストは必ず **dryRun**（`DRY_RUN=true` / `workflow_dispatch` の `dryRun`）で行い、実送信は人間の承認を得る。

---

## 有給・打刻・月別出勤日数・通知機能の注意点

- **打刻 / 打刻修正申請**: `tc5_records` / `tc5_correction_requests` / `tc5_approvals` は**本番の勤怠データ**。Agent はこれらのデータを変更・削除しない（読み取り・コードレビューのみ）。
- **有給**: `tc5_paid_leave_requests` / `tc5_paid_leave_balances`。付与・残数の計算は給与に直結するため、ロジック変更は必ず analyst → review を通し、データ自体は触らない。

  ★ **残日数の正本は `remainDays` であり、`grantedDays - usedDays` で再計算してはならない**（2026-08-14 実測で確定）。
  `usedDays` は「このシステムで承認して消化した累計」でしかない。有給付与登録モーダルは
  **付与日数と別に残日数を入力できる**ため（運用開始前にすでに取得済みの分を残日数だけ減らして登録する）、
  その付与行では恒久的に

  ```
  grantedDays − usedDays − remainDays = 登録時点ですでに消化していた日数（carryIn）
  ```

  という差が残る。**本番実測（2026-08-14）では 40 付与行中 14 行がこの状態**で、最大 10日 の差がある。
  例: 付与7日 / `usedDays`3日 / `remainDays`2日 ＝ 登録時点で 2日 消化済み（消化済み合計 5日）。

  ・したがって **「消化済み合計」は `grantedDays − remainDays`** であり、`usedDays` ではない。
  ・付与日数を修正するときの残日数は **`remainDays(現) + (grantedDays(新) − grantedDays(現))`** で求める。
  　`grantedDays − usedDays` で再計算すると **carryIn が残日数として復活し、取得済みの有給が戻る**
  　（2026-08-14 に確認モーダルの表示・保存の両方で発生していた不具合。修正済み）。
  ・`usedDays` は **`applyFifoConsumption` / `reverseFifoConsumption` 以外から書き換えない**。
  　表示用残日数（`buildFutureApprovedLeaveMap`）が「未来日の取得予定を戻す上限」に `usedDays` を使っており、
  　ここを動かすと一覧の「第N回」表示と残日数合計が崩れる。
  ・`normalizePaidLeaveBalance` の `remainDays` 欠落時フォールバック（`granted - used`）は、
  　**値が無い行の既定値補完**であって再計算式ではない。再計算に流用しないこと。
  ・回帰テスト: `node scripts/test-paid-leave-grant-edit.js`
- **月別出勤日数**: `tc5_monthly_days_import`。`scripts/fix-monthly-days-year.js` は**データ補正系の保守スクリプトで副作用がある**ため、Agent から実行しない（人間が確認のうえ実行）。
- **施設端末の持ち出し検知**: `/devmon` 配下（ルート直下）は**検知の成否を決める設定**。基準位置・許容半径・監視ON/OFFを書き換えると持ち出しを検知できなくなる。Agent はデータを変更・削除しない（設定変更は管理画面から人が行う）。`/devmon/devices/*/tokenHash` は端末の資格情報のハッシュであり、値を表示・記録してはならない。
- **移動距離申請**: `/mileage` 配下（ルート直下）は**給与に直結する本番データ**。Agent はデータを変更・削除しない。特に `/mileage/monthly` と `/mileage/closings` は確定済みの支給額スナップショットであり、書き換えると過去月の給与計算が変わる。`/mileage/settings`（km単価・端数処理）の変更も金額に直結するため、コードからの既定値変更を含め人間の確認が必要。
- **通知機能**: 誤送信は実利用者（管理者・スタッフ）に届くため、検証は dryRun 限定。送信先 ID・Webhook・トークンは Secrets/環境変数で管理されており、値を表示しない。

---

## 環境変数名・secret名（名前のみ。値は絶対に表示・記録しない）

> いずれも**名前のみ**。値の表示・出力・記録・commit は禁止。`index.html` 内の Firebase web 設定値（`FB_API_KEY` 等）も**値は引用しない**。

- **GitHub Actions Secrets**: `LINE_CHANNEL_ACCESS_TOKEN` / `LINE_TO_ID` / `FIREBASE_API_KEY` / `FIREBASE_DATABASE_URL` / `FIREBASE_SERVICE_ACCOUNT_KEY`
- **Vercel 環境変数**: `api/` 配下が `process.env` で参照している名前は次の**9件**（2026-09-13 に
  `grep -rho "process\.env\.[A-Z_0-9]*" api/` で実測。値は未確認）。
  `LINE_CHANNEL_ACCESS_TOKEN` / `LINE_TO_ID` / `DISCORD_WEBHOOK_URL` /
  `FIREBASE_SERVICE_ACCOUNT_KEY` / `FIREBASE_DATABASE_URL` / `FIREBASE_PROJECT_ID` /
  `TC_ENC_KEY` / `TC_PIN_PEPPER` / `DEVICE_SWEEP_KEY`
  - ⚠⚠ **この一覧を「未使用の整理」の根拠に使ってはならない**（`AUTH.md` §8：登録済みの変数を
    未使用と判断して削除してはならない）。実際に消すと壊れるものの例:
    `TC_PIN_PEPPER` → `api/_lib/secrets.js` の `pepper()` が throw して **PIN 認証が全滅**。
    `TC_ENC_KEY` → 保存済み PIN の平文が**復号不能**。
    `DEVICE_SWEEP_KEY` → `action:"sweep"` が 503 になり**持ち出し検知の確定の主経路が止まる**
    （保険の経路だけが残る）。
- **クライアント側（`index.html` に埋め込み。Firebase 公開クライアント設定）**: `FB_URL`（Realtime Database URL）/ `FB_API_KEY` / `FCM_MESSAGING_SENDER_ID` / `FCM_VAPID_KEY`
- **スクリプトの実行制御**: `DRY_RUN` / `TEST_NOTIFY` / `TARGET_DATE`（秘匿情報ではないが、挙動に影響）

---

## 禁止事項（全 Agent 共通・厳守）

> 共通の禁止事項・姿勢の正本は [`../_shared_claude/RULES.md`](../_shared_claude/RULES.md)（データ破壊的操作の汎用原則は
> [`../_shared_claude/DB.md`](../_shared_claude/DB.md) の**汎用安全原則のみ**＝上記「DB.md の適用範囲」参照）。以下は timecard 固有のパス・対象を明示した上乗せ。

1. **secret 値を表示・出力・記録・commit しない**（Secrets/環境変数/`index.html` 内 Firebase 設定値を含む）
2. **DB（Firebase Realtime Database）を変更しない**（`tc5_*` データの作成/更新/削除を実行しない）
3. **Firebase データを変更しない**（Storage/RTDB/FCM トークン等のデータ操作をしない）
4. **GitHub Secrets を変更しない**
5. **cron-job.org の設定を変更しない**
6. **GitHub Actions ワークフロー（`.github/workflows/`）を目的外で変更しない**（変更は人間の確認必須）
7. **`database.rules.json`（Firebase Rules）を勝手に変更しない**（本番データに直結。変更は firebase-agent で方針提示し人間が確認）
8. **アプリコードを目的外で変更しない**。特に:
   - `index.html`（変更は方針確認後のみ）
   - `sw.js`（変更時は **`CACHE_NAME` のバージョンアップ必須**）
   - `api/*.js`（Vercel 通知関数）
9. **削除操作禁止**（ファイル削除・`git clean`・`git stash`・`git reset --hard`・`git push --force`・データ削除）
10. **存在しない npm script を使わない**（npm プロジェクトではない。build/lint/test の npm コマンドは無い）
11. **未整備のものを勝手に使わない**: Playwright / smoke / `agent:ship` は**未整備**。使わず、必要なら未整備である旨を報告する
12. **Vercel への手動デプロイ（`vercel --prod` 等）をしない**（連携の有無も未確認）。デプロイは git push → GitHub Pages 自動配信が前提
13. **`/devmon`（施設端末の持ち出し監視）のデータを変更・削除しない**。`/devmon/facilities`（基準位置・許容半径・監視ON/OFF）と `/devmon/devices`（端末トークンのハッシュと状態）は検知の成否そのものを決める。読み取り・コードレビューのみ
14. **`fix-monthly-days-year.js` 等の副作用スクリプトを勝手に実行しない**
15. **判断に迷ったら編集・実行せず停止してユーザーに報告**

---

## QA 手順

`.claude/agents/qa-agent.md` が担当。標準フロー（ビルドが無いため構文・整合チェック中心）:

1. **変更把握**: `git diff HEAD` / `git status`
2. **JS 構文チェック**: 変更した `.js` / `sw.js` に `node --check`
3. **JSON 構文チェック**: `manifest.json` / `database.rules.json`
4. **Service Worker 整合**: `sw.js` の `CACHE_NAME` がファイル変更に合わせて更新されているか、`OFFLINE_URLS` の参照ファイルが存在するか
5. **移動距離申請の回帰テスト**: `node scripts/test-mileage.js`（全件 PASS / 0 FAIL を確認）。**移動距離申請に関係する変更では実行必須**。関係しない変更では実施不要（その旨を報告する）
6. **管理者トークン状態の回帰テスト**: `node scripts/test-admin-token-state.js`（全件 PASS / 0 FAIL を確認）。**管理者URLトークン・管理者PINの設定状態表示・`/config/adminTokenSet.json` / `adminTokenHash.json` / `adminToken.json` の取得処理に関係する `index.html` の変更では実行必須**。1件でも FAIL なら「要修正」とし ship に進まない。関係しない変更では実施不要（その旨を報告する）
7. **有給付与日数修正の回帰テスト**: `node scripts/test-paid-leave-grant-edit.js`（全件 PASS / 0 FAIL を確認）。**有給の付与日数修正・残日数の計算に関係する `index.html` の変更では実行必須**。1件でも FAIL なら「要修正」とし ship に進まない。関係しない変更では実施不要（その旨を報告する）
8. **打刻の端末永続保存・自動再送の回帰テスト**: `node scripts/test-punch-outbox.js`（全件 PASS / 0 FAIL を確認）。**打刻・`_execPunch`・`tc5_records` への書き込み・起動時／polling の打刻取得に関係する `index.html` の変更では実行必須**。1件でも FAIL なら「要修正」とし ship に進まない。関係しない変更では実施不要（その旨を報告する）
9. **tc5_records の期間取得・app shell キャッシュの回帰テスト**: `node scripts/test-records-range.js`（全件 PASS / 0 FAIL を確認）。**`tc5_records` の取得範囲・管理画面の入口・月/年セレクタ・`sw.js` に関係する変更では実行必須**。1件でも FAIL なら「要修正」とし ship に進まない。関係しない変更では実施不要（その旨を報告する）
10. **有給取得履歴の回帰テスト**: `node scripts/test-paid-leave-history.js`（全件 PASS / 0 FAIL を確認）。**有給取得履歴の表示・`plBuildLeaveHistory`・実績/予定の境界に関係する `index.html` の変更では実行必須**。1件でも FAIL なら「要修正」とし ship に進まない。関係しない変更では実施不要（その旨を報告する）
11. **管理者の勤怠編集の回帰テスト**: `node scripts/test-admin-attendance-edit.js`（全件 PASS / 0 FAIL を確認）。**管理者の勤怠編集・時刻セルの描画条件・`attachTimeCellHandlers` に関係する `index.html` の変更では実行必須**。1件でも FAIL なら「要修正」とし ship に進まない。関係しない変更では実施不要（その旨を報告する）
12. **承認漏れサマリー・通知件数集計の回帰テスト**: `node scripts/test-unapproved-summary.js`（全件 PASS / 0 FAIL を確認）。**承認漏れの集計・通知件数・打刻漏れ判定・全期間スキャンに関係する `index.html` の変更では実行必須**。1件でも FAIL なら「要修正」とし ship に進まない。関係しない変更では実施不要（その旨を報告する）
13. **朝出勤未確認の施設別判定時刻の回帰テスト**: `node scripts/test-morning-check.js`（全件 PASS / 0 FAIL を確認）。**朝出勤未確認 LINE通知・施設別の判定時刻・`morning-check.yml` の cron に関係する変更では実行必須**。1件でも FAIL なら「要修正」とし ship に進まない。関係しない変更では実施不要（その旨を報告する）
14. **打刻時の顔撮影の回帰テスト**: `node scripts/test-face-photo.js`（全件 PASS / 0 FAIL を確認）。**顔撮影・スタッフ管理モーダルの当該項目・`_execPunch` の結線に関係する `index.html` の変更では実行必須**。1件でも FAIL なら「要修正」とし ship に進まない。関係しない変更では実施不要（その旨を報告する）
15. **施設端末の持ち出し検知の回帰テスト**: `node scripts/test-device-watch.js`（全件 PASS / 0 FAIL を確認）。**持ち出し検知・`api/device.js` / `api/device-report.js` / `api/_lib/device.js`・`/devmon`・管理画面の監視UIに関係する変更では実行必須**。1件でも FAIL なら「要修正」とし ship に進まない。関係しない変更では実施不要（その旨を報告する）
16. **通知スクリプト dryRun**: `DRY_RUN=true node scripts/morning-check.js`（環境変数未設定ならスキップして報告。実送信はしない）。判定時刻ごとに確認する場合は `CHECK_HOUR=6` / `CHECK_HOUR=7` を付ける
17. **GitHub Actions YAML 確認**: 構文・cron・`secrets` 参照名・`node-version`

総合判定は「出荷可 / 要修正」。要修正なら ship に進まない。

---

## Agent の役割と正しい流れ

| Agent | 役割 | コード変更 |
|---|---|---|
| `analyst-agent` | 変更前の影響範囲調査・修正案提示 | しない |
| `debug-agent` | 障害調査・原因特定・修正方針提示（修正は要確認） | 方針確認後のみ |
| `firebase-agent` | Firebase/PWA/通知/GitHub Actions のインフラ層の調査・確認・修正 | インフラ層のみ・要確認 |
| `review-agent` | 静的コードレビュー（設計・副作用・セキュリティ・品質） | しない |
| `qa-agent` | 構文/JSON/SW整合/通知dryRun/Actions 確認 | しない |
| `ship-agent` | commit → push → GitHub Pages 自動デプロイ | しない |
| `orchestrator-agent` | 上記を統括し順番に実行（失敗時は即停止） | 各 Agent に委任 |

### review-agent → qa-agent → ship-agent の正しい流れ

```
（実装・修正、必要に応じて firebase-agent でインフラ確認）
   ↓
review-agent  ── 静的レビュー（設計・副作用・Firebase Rules整合・SW・通知）。問題あれば修正へ戻す
   ↓ 承認
qa-agent      ── 構文/JSON/SW整合/dryRun/Actions。総合判定「出荷可」
   ↓ 出荷可
ship-agent    ── git add 済み前提で commit → push origin main → GitHub Pages 自動デプロイ
```

- review-agent が「要修正」→ 修正に戻す（qa へ進まない）
- qa-agent が「要修正」→ ship を**開始しない**
- staged ファイルが 0 件、または commit message 未指定なら ship を**開始しない**

---

## commit / push / deploy 確認ルール

- **通常実装は、ユーザーの明示的な停止指示がない限り、commit → push → GitHub Pages 反映確認 → 本番確認まで進める**
  （共通 [`../_shared_claude/RULES.md`](../_shared_claude/RULES.md)「Git・出荷」／[`../_shared_claude/DEPLOY.md`](../_shared_claude/DEPLOY.md)）。
  ローカル確認だけで停止してはならない。**出荷前に共通 `AGENTS.md` の出荷条件（Critical 0 / High 0 / review 完了 /
  QA 完了）を満たしていること**が前提であり、未達なら出荷しない。
  ★ 共通側の「Vercel READY 確認」は本 repo では **GitHub Actions の成否と本番 URL の反映確認**へ読み替える
  （`PROJECT_TYPES.md` Type C）。
  「commit しない」「push しない」「本番へ出さない」「調査だけ」等の**範囲指定があれば従う**。
  ★ **下の「出荷フローに含めない」項目（ワークフロー・Secrets・Firebase Rules・cron-job.org）は
  この自律出荷の対象外である。** 自律で進めてよいのは通常のアプリコード変更に限る。
- `git add` は今回触ったファイルのみ個別に指定（`git add -A` / `git add .` は原則禁止）。commit 前に `git diff --staged` を確認。
- commit message は内容が分かる短い形式（例: `fix: 打刻処理の修正` / `feat: …` / `chore: …` / `docs: …`）。
- **push 先は `origin main`**。`git push --force` は提案しない。remote に差分がある場合は `git pull --rebase` を提案。
- **デプロイは push 後の GitHub Pages 自動反映が前提**（30秒〜数分）。状況は `https://github.com/rsb79692-create/timecard/actions` と本番 URL で確認。**手動 `vercel --prod` はしない**。
- ワークフロー・Secrets・Firebase Rules・cron-job.org の変更は出荷フローに含めない（人間の確認が必要）。
  ★ **`database.rules.json` は honomi-board と同一の Firebase プロジェクト `honomi-timecard` に同居している**
  （`PROJECT_TYPES.md` Type C）。`firebase deploy --only database` はルール全体を置換するため、
  timecard 側から出すと honomi-board の `rooms` / `members` / `config` / `field` /
  `shares` / `shareKeys` / `guestOf` が消え、共有ボードが即座に止まる。
  この repo の自律出荷に Rules のデプロイを含めてはならない。

---

## 完了報告テンプレート

> 共通の報告様式は [`../_shared_claude/REPORT.md`](../_shared_claude/REPORT.md)（起動 agent / agent チェーン /
> 変更ファイル / 検証結果 / commit ID / push・deploy 結果 / 未対応 / リスク）。以下は timecard 版（併用可）。

作業完了時は以下の形式で報告する（該当しない項目は「該当なし」）:

```
■ 作業完了報告

着手前 git status   : （要点。M / ?? の概況）
変更ファイル一覧     : （パス列挙。無ければ「なし」）
確認できた事実       : （箇条書き）
未確認事項           : （箇条書き。無ければ「なし」）
禁止事項の遵守       : Firebaseデータ変更なし / Rules変更なし / Secrets変更なし / cron-job変更なし / 削除なし / secret非表示
QA結果               : 構文 / JSON / SW整合 / dryRun（実施した場合）。未実施なら「未実施」
commit / push        : 実施（commit ID・push先 origin main）/ 未実施（理由。範囲指定・出荷条件未達・失敗のいずれか）
本番反映             : 反映待ち（GitHub Pages 自動）/ 反映済み / 未デプロイ
本番確認             : 実施（確認内容）/ 未確認（理由）
次アクション・要確認 : （残課題・未確認事項。無ければ「なし」）
```
